# Soulvail — Progress

**The log.** Where we are right now, and what each merged task actually did. Updated **in the same PR** as the implementation — never in a separate "update docs" commit.

The map is [ROADMAP.md](ROADMAP.md). The specs are in [tasks/](tasks/).

**Closed milestones are archived, not deleted:** [M0](archive/PROGRESS-M0.md) · [M1](archive/PROGRESS-M1.md). The log below covers the milestone in progress only. This file used to be 381 KB and could not be opened whole by any tool; keeping it to one milestone is what stops that happening again.

---

## Current State

_Updated: 2026-09-12 (M2-13a)_

| | |
|---|---|
| **Milestone** | **M0 — Walking skeleton: complete, tagged `m0`. M1 — Combat feel: complete (21/21), acceptance passed on Editor evidence; `m1` is the owner's to tag.** Now **M2 — Stage loop**. |
| **Last merged task** | **M2-13a** — **`ISaveStore`, the DTOs, and the stream position that makes a resume honest**. Core can now describe a run completely enough to rebuild it, and **not a byte of it touches a disk** — the port is the whole task, so that the review of *what a run is* is separate from the review of *how it is spelled on disk* (M2-13b). `RunSnapshot` carries the mode, the class, the seed, the stage the player had **not started yet**, HP, shield, simulated `RunTime`, a wall-clock `WrittenAt`, and a `RandomState`; `PlayerProfile` carries **one setting** and deliberately no Shards — AR §6's *"a port grows a member when the mechanic that needs it lands"* reads the same way for a save format, and a v1 field nothing reads is one every later migration carries for ever. **Both are `readonly struct`s, and that is load-bearing:** it is what makes AR §10.3's `Task<RunSnapshot?>` legal as `Nullable<T>` without switching nullable reference types on for one file. **`default` carries version 0, which closes AR §18.3's both-ends problem for free** — `CurrentVersion` starts at 1, so the zeroed form no constructor can prevent is the one value no writer can produce and every reader already refuses. No `IsValid`, no second concept. **Ledger row 1's first half landed:** `IRandom` grew `Capture()` and `Restore(in RandomState)` — **on the port, not on `IRandomStream`**, which is the owner's ruling against the row's own literal wording, because a settable position on a stream is reachable from every core system that draws and a determinism bug caused that way reads as a content bug for a week. **Position is not identity, and a resume needs both:** the seed selects which of 2^63 sequences each stream walks, the five words say how far along, and `Restore_IsCompleteAcrossAFreshGenerator` builds a second generator from the seed alone and shows the restore is complete. `Restore` **refuses nothing** — every 64-bit word is a legal position, so the seed/state agreement is checked where both are visible, at `RunSession.Start`. `InMemorySaveStore` joins `FixedRandom` and `FixedClock` on the fakes shelf: it counts its writes and can be told to **fault the returned task** rather than throw from the call, which is how real I/O fails. **Nothing calls any of it**, the same shape M2-01 shipped in and for the same reason. **Eleven deviations, three that change a decision** (six rows had to move to `Tests/Game`; the fake's position clamps; `FailNextWrite` covers `ClearRun`). Details in the [spec's As built](tasks/M2-13a-save-port-and-dtos.md). |
| **Previous task** | **M2-12b** — **the ring that says *something is about to happen here***. The two events that have been describing a circle on the floor to nobody — `SpawnTelegraphed` since M2-05, `EnemyExploded` since M2-08 — finally draw one, and **all three of the director's events now have a subscriber**. `TelegraphRingView` is **one additive quad whose scale is its radius**, and the two rings are the same body told apart by how it is bound: a spawn ring **fills**, a disc growing to the rim over the director's own `TelegraphTime`, so the ring left is the time left and no number is needed (GD §7.1, §9.1 rule 1); a blast ring is drawn at the archetype's real `ExplosionSpec.Radius` on its first frame and **fades**, because the damage was already applied by the corpse that owned it and a ring that filled would promise an explosion that is over. **`TelegraphRings` is `ProjectileViews`' shape with the index taken out, and the absence is the design rule:** M2-05 rule 7 says a telegraph is a promise the director cannot withdraw, so there is **no cancel path and no id to cancel by** — a ring leaves service by running out, and a test row asserts those methods do not exist. **A spawn ring is 1 m, derived rather than chosen:** `MinSpawnSeparation / 2`, because that separation's own note says the thing it prevents is *"two rings on one patch of floor"*, so half of it is exactly the disc the director reserved and two spawn rings can never overlap. **The blast radius is the circle core tested**, off the event rather than out of a catalog, so *"I was outside it"* is a statement about the same circle — GD §12.4's fairness, drawn. Pooled and prewarmed to 8 (three telegraphs at the director's spacing, plus headroom for simultaneous blasts), parented under the scene's own `Decals` root rather than the arena's so a stage swap cannot destroy a ring mid-life, stepped by `RunTicker` on `snapshot.Dt` beside the bolts, and counted in `DebugOverlay` as `rings rented/pooled`. **Eleven deviations, three that change a decision** (registered in `RunScope` and **required**, with a second guard that the prefab's quad is assigned; the radius derived; the fill as scale rather than a shader property). Details in the [spec's As built](tasks/M2-12b-telegraph-rings.md). |
| **Next task** | **[M2-13b](tasks/M2-13b-local-json-save-store.md)** — `LocalJsonSaveStore`: format v1 on disk, an atomic write, and the migration harness. M2-13a settled *what a run is*; this is the review of *how it is spelled* — the JSON, the path under `persistentDataPath`, the write that cannot leave a half-file behind when Android kills the app mid-save, and the refusal that meets a version this build does not understand (rule 5 reads `default`'s version 0 as “never written”). It is also where [ledger row 11](ROADMAP.md#carry-forward-into-m2) is paid: `HapticsSettings.FromStore(ISaveStore)` replaces `FromPlayerPrefs()`, and the `PlayerPrefs` key is abandoned rather than migrated — nothing has shipped, so the population to carry over is the owner's dev machine. `PlayerProfile.HapticsEnabled` exists as of M2-13a and has been waiting for exactly this caller. |
| **What works** | A run plays end to end from a cold start, and the fight is two-sided. Boot → Menu → tap Descend → a run with a player, eight Husks, a HUD and a death screen. **Core owns every outcome**; Unity reports facts and renders events. Concretely: `RunSession` is the only `IRunSession`; `PlayerMotor` + `PlayerCombat` decide movement, targeting, damage and the Charge; `EnemySystem` + `EnemyRegistry` + `ChaserBehaviour` own the enemies; `Weapon` swings on a clock and asks the body for a cone, which answers through `ReportConeHits`; `ViewPool` keeps every body pooled for the length of the run, and **every one of them is scaled to the run's depth on the way in and wiped clean on the way back out**; `NavPathSense` routes them round the pillars; `HudPresenter`, `ReticleView` and `EnemyHitFeedback` render it; `HapticsListener` buzzes it; `PlayerAnimatorView` gives the player a Knight that idles, runs, swings, dodges, flinches and dies — the enemies are still capsules. Composition is `BootScope` → `RunScope`/`MenuScope`, no statics anywhere. **The difficulty spine is wired as of M2-05**: a run composes its opening stage into a `WavePlan` and `SpawnDirector` turns it into telegraphed, spaced, safely placed bodies, scaled to depth on the way in. `RespawnPolicy` is still in the codebase but unused — `keepAlive` is 0, so the director is the only spawner — and **M2-10 is the one that deletes it**. **As of M2-12b every event the director publishes has a subscriber**, which had not been true since M2-05: a wave's arrival is drawn rather than only counted in `DebugOverlay`. **As of M2-06 a stage-2 wave is no longer all Husks** — `Descent.asset` rosters the Spitter at 2 and the Bloater at 4, and the three archetypes are told apart by tint and body scale on the one shared prefab. **The Spitter fights as of M2-07b**, which flipped its asset off `Static`: it walks to fourteen metres, plants, swells for 0.7 s and throws a bolt that arrives 1.17 s later — so a stage-2 wave now contains something that punishes standing still. **The Bloater fights as of M2-08**, which flipped the last one: it waddles in, plants at two metres, swells for 0.8 s and kills itself, and the corpse takes a third of the player's bar with it inside three metres. **No shipped archetype is authored `Static` any more** — all three have a mind, which is the first time that has been true. **Detail for any of it is in the [M0](archive/PROGRESS-M0.md) and [M1](archive/PROGRESS-M1.md) archives**, task by task. **As of M2-07a a run can carry shots in the air**: `ProjectileSystem` is composed per run, ticked between the enemy behaviours and the death check, and lands a shot by core’s own arithmetic — no fact is asked of the body, which is [ledger row 7](ROADMAP.md#carry-forward-into-m2) settled for all three archetypes. **It stopped being empty at M2-07b**, so `DebugOverlay`’s `shots` field now counts something during a stage-2 wave. **A bolt is drawn as of M2-09**, which is what makes the Spitter's *fairness* judgeable rather than only its rhythm: `ProjectileViews` rents a body on `ProjectileFired` and returns it on `ProjectileImpacted`, `RunTicker` steps it with the snapshot's `Dt`, and the flight arcs on Y over core's own straight XZ line. It is pooled to core's projectile capacity, so a run's only `Instantiate` calls happen while the scene loads, and `DebugOverlay` shows `bolts rented/pooled` beside `shots` — two counts of the same thing on opposite sides of the boundary. **`EnemyExploded` was the last of the three left unseen and stopped being so at M2-12b:** the blast draws its real radius, so the swell, the hit flash and the haptic are joined by the circle the player was asked to judge — and stepping outside the drawn ring is the same act as stepping outside the one that hurt. **As of M2-10 a run passes through stages rather than sitting in one.** `StageFlow` seals an arena for two seconds, runs its waves, waits out a beat when the last body drops, opens a door, and waits at it for as long as the player likes; walking in fades the screen and puts them one depth further down with the budget, the curves and the scaling that go with it. **`RespawnPolicy` is gone** — the director is the only spawner in the game and there is no longer a field that could make it not be. **The `_startingStage` workaround is over**: every manual step since M2-07b needed `Descent.asset` temporarily set to 2 and left uncommitted, and a run now reaches stage 2 and stage 4 by playing. **As of M2-11a the world changes at a boundary.** A run draws its rooms from `Descent.asset`'s roster — `Arena_Pillars` and `Arena_Tiered` — and which one a depth is fought in is derived from the seed and the stage, never drawn, so the same seed visits the same rooms in the same order and no room appears twice running. `ArenaPool` raises one on `StageArrived`, puts the player at its own start, seals it, and builds the next one **inactive** during the clear beat; the 0.3 s fade now covers a swap that has something to swap. The door, the spawn points and the cover all moved onto the arena prefab, so `Run.unity` no longer dresses a room at all. **As of M2-11b the cover does something.** A Spitter that cannot see the player through a pillar plants and waits rather than winding up, so an arena's three-to-six pillars are finally geometry the player can *use* — and the last thing missing from GD §12.4's fairness contract is the one indicator that says where an unseen shot came from, which is M2-12a's. `RunTicker`'s frame order also has a fixture under it for the first time. **As of M2-12a that indicator exists** — an off-screen Spitter is pointed at from the border and a tapped-but-untaken enemy is marked, so GD §12.4's on-screen rule and CC §3.5 both have something on screen keeping them. **As of M2-12b the floor is legible too:** a body arrives inside a ring that filled first and a Bloater's blast leaves a circle the size of the one that hurt, which is the last of the director's events to become visible and the first time the fight's *fairness* is drawn rather than argued for. **As of M2-13a a run can be written down, though nothing writes one yet.** `RunSnapshot` and `PlayerProfile` are two versioned value types behind one async `ISaveStore`, and `IRandom` can say where all five streams stand and be put back there — so the format that a resume needs exists at v1, before anything has shipped that would have to be migrated to reach it. **Nothing calls it**: no file, no path, no JSON, no registration in a container. That is M2-13b's, M2-14a's and M2-14b's, in that order. |
| **Owner retune, after M2-03** | **Five numbers, playtested and changed by the owner in the assets, not by a task.** Oathbound move speed **5.4 → 3**, Charge **10 m / 0.22 s / 5 m knockback → 8 m / 0.5 s / 4 m**, Husk move speed **3.5 → 2**. The whole game got slower and **the speed ratio barely moved** — 5.4/3.5 was 1.543×, 3/2 is 1.5× — so GD §6.1's *"every class must feel faster than almost every enemy"* survives, and escaping a Husk telegraph still works because the Husk plants itself during its windup (1.2 m gained against a 0.6 m cancel band). **Two derived numbers moved a long way and neither is authored anywhere:** the dash is 45 m/s → **16 m/s**, 8.4× move speed → 5.3×, and its invulnerable window is 0.27 s → **0.55 s**, now longer than a Husk's whole 0.4 s windup. CC §2.5, §5 and §7 were updated with the assets and the two asset-pinning test rows moved with them. **The class speed band was deliberately not touched** — see Known issues. |
| **Verified** | **At M2-13a: 960 EditMode passed, 1 failed, 0 skipped, 13.3 s** — M2-12b's 931 plus **30 new rows, every one of them green**: seventeen in `SaveDtoTests` (every field read back, the version and stage floors, the three float doors including the NaN-and-infinity spelling, `default`'s version 0, `default(ContentId)` deliberately allowed, `RunTime` and `WrittenAt` independent, the profile's default and its two-property shape pinned against Shards, `RandomState`'s five words in index order, and four reflection rows — every `ISaveStore` member async, no by-ref parameter on any of them, both DTOs value types so the `?` is `Nullable<T>`, and `IRandomStream` still four members with no `State`); six in `InMemorySaveStoreTests`; six capture/restore rows in `SeededRandomTests` including both allocation rows; one round trip in `FixedRandomTests`. Zero compile errors, **zero new analyzer warnings**, `git diff ProjectSettings/` **empty**, and **no asset, prefab or scene was touched at all** — nothing here is visible, registered or rendered. **The one failure is neither new nor this task's**: `ModeDefinitionTests.Descent_MatchesDesign` on `_startingStage`, which is `dev`'s own — see Known issues. The two `ArenaView.OnValidate` Console warnings are the pre-existing import-order pair; every other warning in the run is a fixture exercising a warning path on purpose. Previous baseline: **931 EditMode green, 11.3 s** (2026-09-12, at M2-12b). Earlier tallies are in each task's log entry. |
| **Reference device** | **None, and the emulator is not a fallback.** BlueStacks 5 (Android 9 / SDK 28, x86_64) **installs the APK and crashes on launch** in its own Vulkan driver via `libhoudini.so` — an emulator-bridge fault on a development-build Vulkan path, not game code. The APK is ARM64-only and always will be: Unity 6.3 cannot build x86-64 for Android. **Nothing outside the Editor has ever run.** [M0-20a](tasks/M0-20a-apk-runs-on-bluestacks.md) owns it; a phone removes the question. Unity Device Simulator still serves for layout. |
| **Deferred — device-only** | Multi-touch (stick + button); landscape flip on rotation; touch latency; real frame rate; thermal; kill-from-recents and relaunch; whether the M0/M1 **feel** verdicts survive leaving the Editor. **M1-20's haptics are the largest single block** — the mapping, the off switch, and above all whether the 100 ms coalescing window reads as *late*. **M2-10 adds two pacing knobs to the list:** whether 2 s of arrival and 1.5 s of clear read as *beats* or as *waiting* inside GD §7.3's 40–75 s stage. `StageFlow.ClearTime` is the one number in that class no design document gives, and this is the session that sets it. **M2-11a adds the swap:** whether 0.3 s of fade is long enough to cover a room being torn down and another raised without reading as a stutter, and whether the change of room lands as *progress* rather than as a load. **M2-11b adds one of its own, and it is a legibility question rather than a timing one:** whether a pillar *reads* as cover at a phone's size — whether a player works out, without being told, that standing there is why they stopped being shot at. A Spitter that holds its fire looks identical to a Spitter that has not noticed you, and the difference is the whole mechanic. **M2-12b adds the overdraw question the whole ring design is bounded by:** whether eleven overlapping additive quads still read as *separate countdowns* rather than as one bright smear, and what that fill rate does to the frame time (GD §11.3) — this is the step that decides whether a concurrent-ring cap is needed. **And one legibility question it created on purpose:** the fill is the quad's own scale, so a spawn ring has **no rim at full radius** and shows where the body will land only as the disc arrives. If that reads as *too late* on a phone, the fix is a second quad and twice the overdraw, which is a trade only a device can price. Nothing in the Editor can answer any of it: the container resolves `NullVibrator` there by construction. The first session with a phone runs all of them. |
| **Known issues** | **`dev` fails an EditMode row as well as the PlayMode one, and nothing had recorded it.** `ModeDefinitionTests.Descent_MatchesDesign` fails on `spec.StartingStage` being **5** where GD §8.2 says 1, because `Descent.asset` carries `_startingStage: 5` **on `dev`** — changed 1 → 5 in `ea0627f` (M2-12b). That is the playtest workaround this block had recorded as *over* at M2-10; it was reverted in the working tree every session and this time it went in with the commit, which is exactly the Unity re-serialisation shape [Traps §5](../Traps.md) warns about — the Editor holds the rewritten copy and the next write repeats it. **M2-13a did not touch it and did not fix it**: it is one line in a data asset, outside that task's Files table, and which stage a playtest starts at is the owner's knob. One line to revert, and the suite is green. **The physics-sync decision is open and it is the owner's, and as of M2-12b it is measured.** `m_AutoSyncTransforms` is **0 project-wide**, so `RunTicker` moves the bodies and then asks `ConeOverlapQuery` about an arena the physics scene has not been told about. AR §18.1 says the order exists so a sweep resolves against *this* frame's arena; today that is true of the call order and **not of the code**, so a swing can miss an enemy that moved this frame. The evidence, all with the Run scene open: **`dev` fails one `FrameOrderTests` row** (`Ticker_RunsTheStepsInOrder`, reproduced twice, once in isolation) — so the merged baseline is **not** green; **M2-12b's branch fails two** (that row plus `Ticker_ReportsFactsAfterBodiesMoved`), only ever on the `ConeReport` assertion and never on an ordering one; and **one line, `Physics.SyncTransforms()` between the bodies step and the fact phase, makes all eleven pass.** That line was applied temporarily to measure and then reverted; nothing in the tree contains it. The ring step lands inside exactly the window the staleness lives in, which is why the count moved by one — it touches no collider and asks physics nothing. **Three other fixtures already paper over this with an explicit `SyncTransforms`, one of them with a comment calling it "a project setting a fixture should not depend on"; `FrameOrderTests` cannot, because the move and the query are inside one `Tick()`.** Second open item, also the owner's and much smaller: **whether GD §16.4's palette earns a file of its own.** M2-12b reads `#FF4A1F` from `ThreatArrows.Danger` rather than copying it, which keeps the number in one place but makes `Views` and `Presentation` mutually dependent — legal inside one assembly, untidy. One shared `Palette` file fixes it. **One doc contradiction, open on purpose.** The owner's retune puts the Oathbound at 3 m/s while **GD §6.1's *"speed range across classes 5.4–6.2 m/s"* and Characters.md §3's `140 / 5.4` row still carry the old band** — so the starter class sits below its own stated floor. Not changed with the assets, because moving the band is a statement about the Gravecaller (5.6) and the Emberwright (6.2), neither of which is built, and whether the whole band scales down or the Oathbound simply becomes the slow class is the owner's call. [Parking lot](ROADMAP.md#parking-lot) holds it; **M5-02** is the first task that cannot avoid it. The other real open issue is the APK crash, which has its own task. Disk sits near full — an IL2CPP Android build wants several GB of headroom. **Before every commit, check `git diff ProjectSettings/`**: Unity backfills defaults and re-serialises settings files behind you, and the Editor holds the rewritten copy in memory until restarted (see [Traps.md §5](../Traps.md)). Three sessions running it has not recurred. |
| **Where the rules live** | The watch list is gone, and that is deliberate — it had reached 49 KB on **one line** and was read in full every session. It was split by kind: **[Traps.md](../Traps.md)** for things in the toolchain that lie to you (the "an API that echoes your value back has not agreed to honour it" family, the shell probes, the unfocused Editor, the MCP, VContainer, test measurement), and **[Architecture.md §18](../Architecture.md#18-invariants)** for rules the code depends on — orderings, boundary rules, why a field is `internal`, and the soft spots no task owns yet. Spent milestone incidents stayed in the archives. A future obligation with an owning task is a [ledger](ROADMAP.md#carry-forward-into-m2) row; without one, a parking-lot line. **Read Traps.md before debugging a probe; read §18 before changing an ordering.** **Six ledger rows are open as of M2-11b** — rows 8 and 13 left the table with it, rows 2, 3, 4, 5, 7 and 9 earlier. Numbers are never reused, so an old entry citing "row 5" still points at the right thing. |

---

## How to write an entry

Append one entry per merged task, newest at the bottom. **Twelve lines or fewer.** The spec's *As built* footer owns the full account of what deviated and why; this entry is the index — what happened, and where each lesson was filed. (The archived logs average 7 KB an entry against a template that said "one line"; the cap is what keeps this file readable.)

```markdown
### YYYY-MM-DD · M0-03 · Domain events
**Built:** one or two lines — what exists now that didn't.
**Deviations:** none | *n*, in the spec's As built. Name here only one that changes a decision.
**Learned:** one line each, ending with where it was filed — Traps §n · AR §18.x · ledger row n · spent (stays here).
**Follow-ups:** tasks created, ledger rows added, or "none".
```

The PR number lives in the merge commit — do not write it here. Current State is written **as of the PR's merge**: the task the PR closes is *Last merged*. If a deviation changes a decision in `Architecture.md`, it needs a superseding ADR — say so here and link it.

**A durable lesson does not go in Current State.** A toolchain trap goes in [Traps.md](../Traps.md); a rule the code now depends on goes in [Architecture.md §18](../Architecture.md#18-invariants); a thing a named task must handle goes in the [carry-forward ledger](ROADMAP.md#carry-forward-into-m2), and one with no owner yet in the ROADMAP parking lot, one line. The entry says *what happened*, and links.

**At the end of a milestone**, move its entries to `archive/PROGRESS-M<n>.md`, promote anything durable out of them first, and leave this Log holding only the milestone in progress.

---

## Log

_M0 and M1 are archived: [M0](archive/PROGRESS-M0.md) (20 entries) · [M1](archive/PROGRESS-M1.md) (21 entries)._

### 2026-09-10 · M2-00a · Plan hygiene: archive, split the watch list, carry-forward ledger

**Built:** the planning docs are readable again, and every durable lesson has a home that is not a table cell. **`PROGRESS.md` went from 381 KB to 6.5 KB** — it could not previously be opened whole by any tool, including the Read tool's 256 KB cap, so reading Current State meant shelling out to `awk` and `fold`. The M0 and M1 logs moved verbatim to `archive/PROGRESS-M0.md` (20 entries) and `archive/PROGRESS-M1.md` (21 entries), links rewritten one level up and each one verified to resolve; a checksum of the rejoined archives against `HEAD` differs from the original by exactly two blank lines and nothing else.

**The watch list is gone, and that is the point.** It had reached **49,395 bytes on a single line** — ~12 k tokens loaded every session inside a Current State that itself cost ~23 k — and it mixed three incompatible kinds of thing. Split by kind rather than by milestone, because a milestone cut would have archived live rules: **[Traps.md](../Traps.md)** takes the toolchain lies (the "an API that accepts a value and echoes it back has not agreed to honour it" family, now with all six instances in one table; the seven-deep shell-probe family; the unfocused Editor; the MCP; VContainer; allocation measurement), **[Architecture.md §18](../Architecture.md#18-invariants)** takes the rules the code depends on — orderings, boundary rules, why a field is `internal` — plus a **soft-spots table naming the task each one bites at**, and spent incidents stayed with their milestone in the archive. Current State is now ~1 k tokens, and its longest line is 948 characters rather than 49,394.

**Deviations from spec:** no spec — this was directed conversationally by the owner after the M0+M1 audit, and the ROADMAP row was written as part of it. **Eight files, which is past the five-file rule**; it is a documentation restructure with no code in it, and splitting it would have left the docs cross-referencing files that did not exist yet. Called out rather than hidden.

**Learned:** **a table cell is not a filing system.** The watch list grew one entry per task for 41 tasks without anyone deciding it should become the project's institutional memory, and the failure mode was silent — nothing warns you that a doc has become unreadable, it just gets more expensive every session until a tool refuses it. The fix that mattered was not deleting anything; it was noticing the file held three kinds of thing and only one of them was milestone-scoped. **The new rule is in "How to write an entry": a durable lesson goes to Traps.md or §18, a future obligation goes to the ledger, and the log entry says what happened and links.** At the end of a milestone, promote first, then archive.

**Follow-ups:** [carry-forward ledger](ROADMAP.md#carry-forward-into-m2) — eleven rows, each naming the M2 task that must absorb it, ranked by cost-of-later. Row 1 (random streams expose no state, so a resumed run restarts every stream at draw 0 — and once M3-04's offers ride that stream, killing the app is a free reroll) is the one with no prior home in any doc or ADR and the only free moment to fix it is M2-13's format v1. `CoreCombat.md` §8 renamed M0 → M1, which is what it always described. **M2-00b…e** write the fifteen specs.

### 2026-09-10 · M2-00f · Doc-system audit: template, entry cap, dedupes, staleness

**Built:** the doc system reviewed from the implementer's seat, verdict *small fixes*, applied in one pass. The task [template](tasks/_TEMPLATE.md) now carries what every M1 session had to invent — spec precedence, implied guard rows, a ripple row, an author's self-check — before M2-00b…e write fifteen specs from it. Entries here are capped at twelve lines with the As-built footer owning deviations; AR §18.5 keeps only the soft spots no task owns and points at the ledger for the rest; Architecture.md says which of its sketches to trust; CLAUDE.md stops contradicting Current State on the device.
**Deviations:** no spec — directed conversationally after the M2-00a review, like M2-00a. Eight files, past the five-file rule, all docs; called out rather than hidden.
**Learned:** a log format with no length rule is a watch list with extra steps — the M0/M1 entries averaged 7 KB against a template that said "one line" → the cap in *How to write an entry*. A rule nothing checks does not fire — the ~75 % detailing trigger missed twice → `M<n>-00x` spec tasks (ROADMAP). Two lists that mean the same thing drift — §18.5 and the ledger shared seven rows → §18.5 points at the ledger.
**Follow-ups:** ledger row 12 (the out-of-range focus tap, owner M2-12, moved from the parking lot). `m1` is still the owner's to tag. Next is M2-00b.

### 2026-09-10 · M2-00b · Specs for M2-01…M2-05, the spawning spine

**Built:** five specs written from the [template](tasks/_TEMPLATE.md), each passing its self-check, and the ROADMAP's M2 title table promoted to sized, linked, dependency-ordered rows for those five. The spine they describe: a wall-clock port that stays out of the simulation ([M2-01](tasks/M2-01-clock.md)); Descent as an asset plus the `RunConfig` reshape that resume needs ([M2-02](tasks/M2-02-mode-spec.md)); GD §12's five curves and depth scaling a recycled enemy forgets ([M2-03](tasks/M2-03-threat-budget.md)); budget → waves under a cap chosen against measured cost ([M2-04](tasks/M2-04-wave-composer.md)); and the director that paces, telegraphs and places them ([M2-05](tasks/M2-05-spawn-director.md)).
Ledger rows 2, 3, 4, 5, 6 and 9 are each answered in Behaviour by the spec that owns them, with the *how* rather than a citation; row 1 is deliberately not decided here — M2-02 rule 5 names the `IRandom.Reseed` question and leaves it to M2-13, which owns it.
**Deviations:** none from the brief. Seven files rather than five — the five specs plus the two plan files the protocol requires the same change to update. Two design questions were put to the owner rather than guessed, and both came back as specced: the seed-agreement guard in `RunSession.Start` over an `IRandom.Reseed` (M2-02 rule 5), and enemy `MoveSpeed`/`ContactDamage` becoming `Stat`s (M2-03 rule 7). Both specs now record the ruling and the rejected alternative.
**Learned:** a spec that names a number has to do the arithmetic — checking GD §12.1's table against its own formula found the stage-40 row wrong by 105, and checking M2-04's cap of 28 against M1-19's path budget found the pathfinder sustains 24 at 60 fps and 12 at 30, which is below GD's *low* tier. Both are what "a spec that does not name its ledger rows is not finished" is actually for → parking lot (the GD row), M2-05 rule 17 (the fix).
**Follow-ups:** two parking-lot lines — the spatial hash that a device cap above 40 would need, and GD §12.1's stale table row. Next is **M2-00c**, specs for M2-06…M2-09, which also has to settle ledger row 7 (fact versus direct call) before two enemies answer it two ways.

### 2026-09-11 · M2-01 · `IClock` + `UnityClock`

**Built:** core can ask what time it is *in the world* — `IClock.UtcNow`, one member — with `UnityClock` behind it in `BootScope` as a singleton, and `FixedClock` in the fakes so a migration fixture can say *"written three days ago"* without sleeping. Nothing consumes it yet; the first reader is M2-13a's save timestamp. **462 EditMode green, 0 failed, 8.6 s.**
**Deviations:** 3, in the [spec's As built](tasks/M2-01-clock.md). One changes a decision: rule 7's *"beside `IRandom`"* is wrong — `IRandom` is `Scoped` in `RunInstaller`, because a seed *is* a run. The clock is registered where the Files table says, and the comment now argues from the vibrator and names `IRandom` as the contrast.
**Learned:** a spec can name ten tests and only enough files for seven — rows 4, 5 and 10 went into the three existing fixtures that already own their kind of assertion, not a sixth file → spent (stays here); the `Type[]` seam in `RunSession_TakesNoClock` is how M2-14a widens it to the whole `Soulvail.Core.Run` namespace by editing one literal → the row's own spec. Both MCP failures on the way (a nested `ICallbacks` hoisted out, `CompilationPipeline` shadowed by the injected namespace) were **already filed** — Traps §4, unread first → spent.
**Follow-ups:** AR §6's `IClock` row said `Now` (core time, seconds) and had been wrong since M0-10; it now says `UtcNow` and points at §18.2. No new ledger rows, no parking-lot lines. Next is **M2-02**.

### 2026-09-11 · M2-00d · Specs for M2-10…M2-12, stage flow, arenas and telegraphs

**Built:** five specs where the ROADMAP had three titles, and the four ledger rows they inherited are each answered with a *how*. A stage that arrives, seals, clears and lets you out through a door, taking `RespawnPolicy` with it ([M2-10](tasks/M2-10-stage-flow.md)); the arena as a prefab contract with a pool, a barrier and pillars on a layer ([M2-11a](tasks/M2-11a-arena-contract-and-pool.md)); the sense that makes those pillars block a Spitter, plus the frame-order test the new step makes worth writing ([M2-11b](tasks/M2-11b-line-of-sight-sense.md)); screen-edge arrows and a focus that finally reads as held ([M2-12a](tasks/M2-12a-threat-arrows-and-held-focus.md)); and the ring that says something is about to happen here ([M2-12b](tasks/M2-12b-telegraph-rings.md)).
**Two splits, both before the tasks start.** M2-11 was seven files and two reviews — a room, and something core can ask about a room — and the sense has to land after the pillars exist. M2-12 split by machinery and **not** by ledger row: rows 12 and 14 stay together in 12a, because the ledger's whole complaint is that answering them apart yields two indicators that disagree.
**Deviations:** none from the brief. Seven files rather than five — the five specs plus the two plan files the protocol requires the same change to update, which is M2-00b's arithmetic and called out for the same reason. Two design questions went to the owner: the stage transition (answered with a door, a closed one during waves, and *do not render the next stage until the player gets there*) and row 12's shape (the fourth reticle state). Both specs record the ruling and the rejected alternative.
**Learned:** *answering two rows together is a decision about grammar, not about a widget* — rows 12 and 14 resolve into one edge-arrow renderer and GD §16.4's palette carrying the difference, red-orange for danger and cyan for the player's own choice, which is a rule that already existed and had never been spent. Filed in M2-12a rule 9. And a second: **the owner's rendering constraint turned into a payload** — `StageCleared` carrying the *next* arena's id is what lets the next room be built inactive during the clear beat and raised behind an opaque screen, so "never render a stage the player has not reached" is true by construction rather than by camera placement (M2-10 rule 5, M2-11a rule 5).
**Follow-ups:** **two owner-approval items are specced rather than assumed** — M2-10 rule 16 asks to delete `RespawnPolicy` and its tests, and M2-11a rule 9 adds a `Cover` layer to `ProjectSettings/TagManager.asset`; both are *Ask before* items in CLAUDE.md and both are flagged in their Files tables. Ledger rows 8, 12, 13 and 14 now name the split owners and record the rulings; **row 13 leaves the table on M2-11b's *As built*, not M2-11a's**. One parking-lot line: arenas 3 through 12. Next is **M2-00e**, specs for M2-13…M2-15, which owns ledger row 1 — the random-stream state a resumed run cannot restore, and the last free moment to fix it is format v1.

### 2026-09-10 · M2-00c · Specs for M2-06…M2-09, enemies and projectiles

**Built:** five specs and the ROADMAP rows for them. [M2-06](tasks/M2-06-enemy-authoring.md) authors the Spitter and the Bloater as two optional blocks on `EnemySpec` and buys legibility with a per-archetype tint and scale on the one shared body; [M2-07a](tasks/M2-07a-projectile-system.md) gives core shots that are already in the air; [M2-07b](tasks/M2-07b-spitter-ai.md) adds `IEnemyBehaviour` and the enemy that throws them; [M2-08](tasks/M2-08-bloater-ai.md) makes the blast a property of the corpse rather than of the fuse; [M2-09](tasks/M2-09-projectile-views-and-pooling.md) draws the bolt and finally puts `EnemyView.OnDespawn` under a test.
**Ledger row 7 is ruled** ([M2-07a](tasks/M2-07a-projectile-system.md) rule 1): **core decides every enemy outcome and calls `PlayerCombat.ApplyDamage` directly; Unity owes a *fact* only when the answer depends on colliders core cannot see, and a *sense* when the geometric question is a standing one.** So M1-18's contact call is ratified, a projectile impact is decided in core, and **`IRunSession` gains no member in M2** — its stale `ReportContact` / `ReportProjectileHit` promises and AR §5's matching row are deleted by M2-07a. The rejected fact route loses on three counts: it lands damage a frame after the tick that decided it, it makes enemy damage unreproducible from a seed (which M2-13 and M2-14 exist to preserve), and it would make an enemy need a body before it could hurt anybody. Row 8 is split: M2-09 takes the pooled-view half, M2-11 keeps the frame order.
**Deviations:** **M2-07 is split into 07a/07b** — together they are seven files, and the protocol splits before starting, never after. That makes seven files in this PR (five specs plus the two plan files), past the five-file rule, docs only, called out rather than hidden — M2-00b's shape. Two design questions were put to the owner and both came back as recommended: the shared tinted body over importing the KayKit skeletons (M2-06 rule 9), and a blast that damages the player only (M2-08 rule 4). The second is what keeps the whole explosion a single leaf call with no re-entrancy into a registry `EnemySystem.Tick` may be walking.
**Learned:** **a design guardrail is arithmetic, and nothing was checking it.** GD §12.4 caps a non-boss hit at 35 % of max HP *at any depth*; the Oathbound has 140 and M2-03's `d(n)` caps at 3.0×, so **no authored `contactDamage` may exceed 16.3** — a ceiling nothing in the project states, and the failure would arrive forty stages into a run. Spitter 12 and Bloater 15 are chosen against it → M2-06 rule 8. The same pass found the Bloater's GD-authored 24 HP dies in 2 Censer hits, one below §12.4's TTK band → flagged for the owner, like M2-00b's stale budget row. Also: two `EnemySystem` comments and one `CombatBlackboard` field named this task by name and were right about what it would need — a comment that names its owning task is worth writing.
**Follow-ups:** **ledger rows 13 and 14** — cover does not block projectiles (owner M2-11, and the sense it needs fills a field carried unused since M0-05), and a Spitter can damage you from off-screen against GD §12.4's on-screen rule (owner M2-12, to be decided with row 12 rather than after it). **M2-00d must pick both up.** Two parking-lot lines updated or added: the KayKit roster's promotion condition, and an `EnemyRegistry` free-list preference that would make M2-07b's rental churn rare. Next is **M2-00d**.

### 2026-09-10 · M2-art · KayKit import, ThirdParty layout, and the player gets a body

**Built:** the player is a Knight instead of a capsule, animated from combat events. Three CC0 KayKit packs land in a new `Assets/ThirdParty/KayKit/` (Adventurers, Animations, Skeletons — 173 clips); `AC_Player` gives locomotion a blend tree on ground speed plus Attack, Charge, Hit and a terminal Death; `PlayerAnimatorView` drives it from `PlayerAttacked`, `PlayerDamaged`, `PlayerDied` and `ChargeStarted`, registered null-guarded on `RunScope` like the reticle and the glow. A `CharacterShowcase` scene tours every clip on all eight bodies.
**Deviations:** no spec — directed conversationally, like M2-00a and M2-00f. Well past the five-file rule at 237 staged paths, though all but six are vendor art or generated assets; called out rather than hidden. **`PlayerAnimatorView` shipped without tests**, which is the real miss → parking lot.
**Learned:** **Generic beat Humanoid once the facts were in.** The plan was Humanoid + Mixamo because the first pack had no attack clips; the animation pack turned out to ship the identical 23-bone `Rig_Medium` every character *and* every skeleton uses, so clips bind bone-for-bone with no retarget distortion and no per-frame retargeting cost at M2-04's cap of 28. Reversing a decision the owner had already approved was cheaper than honouring it — spent.
**Versions in folder names are a trap that already sprung:** a re-download landed as `KayKit_Adventurers_2 1.0_FREE`, a whole duplicate pack with fresh GUIDs. Versions now live in `ThirdParty/KayKit/VERSIONS.md` and updates overwrite in place → spent, and the layout is the fix.
**I did not read Traps.md before debugging a probe that said "no", which is the one thing CLAUDE.md says to do first.** Four attempts to run the suite through the MCP failed on `User interactions are not supported`; §4 already recorded the working shape (one class implementing `IRunCommand` *and* `ICallbacks`, registering `this`). Handed over "unverified" once before going back → Traps §4, refined with what the plural `DeleteAssets` and `Refresh` actually do.
**Filed:** AR §18.2 gains two invariants — animation never gates a damage frame and no clip carries an Animation Event; `applyRootMotion` off and root node empty on every rig, which is live rather than formal because 9 of 173 clips do carry root translation. Traps §5 gains three edit-mode animation lies (a sampled pose does not survive to the next command; `Animator.Play` only evaluates while `speed` is non-zero; imported clips are non-looping and freeze a blend tree).
**Verified:** **452 EditMode green, 0 failed, 9.3 s**; zero errors, zero analyzer warnings, one expected Console warning. All 914 controller states resolve after the move, and 77 LFS pointers staged with no raw blob over 1 MB.
**Follow-ups:** ROADMAP row `M2-art` (the plan had no home for character art anywhere — M7-05/06 are biome art only) and three parking-lot lines: the missing tests, the empty `handslot` sockets, and the skeleton→enemy mapping M2-06 should read before it specs Husk/Spitter/Bloater. Next is **M2-00c**.

### 2026-09-11 · M2-00e · Specs for M2-13…M2-15, persistence, resume and acceptance

**Built:** the last five specs of M2, and with them the milestone is detailed before its `-01` starts — the rule kept on time, which is what M2-00a and M2-00f existed to make possible. Persistence as a port and two `readonly struct` DTOs ([M2-13a](tasks/M2-13a-save-port-and-dtos.md)); a JSON file written atomically that refuses a version it cannot read ([M2-13b](tasks/M2-13b-local-json-save-store.md)); a run on disk from its first frame and rewritten at every boundary ([M2-14a](tasks/M2-14a-snapshot-at-the-boundary.md)); a `Continue` that lands in the same arena with the same waves and the health the player had ([M2-14b](tasks/M2-14b-resume-and-continue.md)); and an acceptance task that closes the ledger row by row ([M2-15](tasks/M2-15-acceptance-and-tag.md)).
**Two splits, decided before the tasks start.** M2-13 was eight files: the port and the DTOs are a review about *what a run is*, the JSON and the migration harness about *how it is spelled on disk*, and the second cannot be judged until the first is settled. M2-14 splits at the write/read seam — everything through 14a is verifiable by looking at a file, while 14b is the menu, the container and the `RunConfig` sweep.
**Deviations:** none from the brief. Seven files — the five specs plus the two plan files the protocol requires the same change to update, which is M2-00b's and M2-00d's arithmetic. Two questions went to the owner rather than being guessed, and both came back as recommended: ledger row 1's shape, and what a resume is allowed to give back (a stage restarts whole; the rewind that implies is the ordinary checkpoint one, named rather than engineered around before a phone exists).
**Learned:** **writing the spec is what found the hole.** A boundary-only write leaves the *previous* run's file on disk for the whole of a fresh run's first stage, so a phone call during stage 1 resumes a run nobody is playing; the fix is an opening write in `RunSession.Start`, cheaper than a menu that deletes the file and durable from the first frame instead of the first door → M2-14a rules 1–2. **An invariant that names one class is one refactor from decorative:** `RunRecorder` must hold the `IClock` that `Core.Run` is forbidden, so M2-01's `RunSession_TakesNoClock` is widened to the whole namespace rather than worked around → M2-14a rule 12. **Nullable reference types are off project-wide**, so AR §10.3's `Task<RunSnapshot?>` compiles only if the DTOs are value types — an ambiguity in the architecture read as a constraint → M2-13a rule 2. And `in` is banned on the port's parameters, because an `async` method cannot take one and ADR-0007 says an async implementation is coming → M2-13a rule 3.
**Follow-ups:** ledger rows 1, 6, 10 and 11 now name the split tasks that own them, each with the *how*; row 1 leaves on **M2-14b**'s *As built*, not M2-13a's. M2-15 owns closing the table and opening M3's. One parking-lot line, and it is a *class* rather than an instance: **already-merged specs still point at pre-split task ids** — five of them now, across two splits — filed once instead of patched four times in a PR whose Files table does not cover them. Next is **M2-01**, and implementation begins.

### 2026-09-11 · M2-02 · `ModeSpec` + `ModeDefinition`, Descent as an asset, and the `RunConfig` reshape

**Built:** a mode is a data object. `ModeSpec` + `RosterEntry` in `Core/Content/`, `ModeDefinition` + `Descent.asset` (`mode.descent`, endless, from stage 1, Husk at 1), and the catalog's third kind with `Mode` / `TryGetMode` / `Modes`. **`RunConfig` is five fields, swept across nineteen call sites in one PR** — mode, class, seed, stage, plan — and the seed now arrives *in*: `RunSession.Start` throws when it disagrees with the injected `IRandom`, so the single truth is checked rather than inherited. `RunState` gains `ModeId` (get-only — a run goes deeper, it never changes mode) and `StageIndex` (`internal set`, for M2-10).
**Deviations:** 7, in the spec's As built. The one that changes a decision: **`RunTicker` gained an `IRandom`**, because `Start` must state the seed and it is the only object that knows it on both paths — `PendingRun` carries the menu's, and a direct Play's is invented inside `RunInstaller.CreateRandom` and reaches nothing else.
**Learned:** **a guard is only worth what its call site cannot already guarantee.** Rule 5's seed check is a tautology on both live paths today and will not be on the one that matters — the resumed run stating a seed from a save file — so the test that proves it is the one with a mismatched pair, not the one that starts a run → M2-14b inherits it. **A spec that says "and the roster too" is naming the half with no other line of defence:** an unauthored id in a plan fails on the first frame either way, but one in a roster would have waited for M2-05's director and looked like a director bug → spent (row 3 closed). **Two of Descent's five fields ship the value their C# initialiser already holds**, so the asset test could not tell a bound key from a misspelled one; comparing the file text either side of a reserialise proves the binding *and* leaves no diff → Traps §7 already covers it.
**Follow-ups:** **ledger row 3 closed and struck**; row 6 reduced to its sixth field (`RunSnapshot Restore`, M2-14b). AR §5's `Run` row corrected — `ModeSpec` moved to `Content`, with the reason under the table. **`Descent.asset` ships with the Husk alone**: M2-06 adds Spitter 2 and Bloater 4 in the same change that authors them, and `AllModeDefinitions_RosterIdsAreAuthored` fails if it does not. Next is **M2-03**.

### 2026-09-11 · M2-03 · `ScalingSpec` + `ThreatBudget`, and depth scaling a recycled enemy forgets

**Built:** GD §12's five curves exist as authored data with one implementation each — `BudgetCurve`, `WaveCurve`, `ConcurrencyCurve`, `StatCurve` and `ScalingSpec` grouped in `Core/Content/`, `ThreatBudget` in the new `Core/Director/`, `DepthScaling` in `Core/Ai/` — and an enemy spawned at depth *n* wears exactly that depth's scaling. `EnemyAgent` gained `MoveSpeed` and `ContactDamage` as `Stat`s (the M2-00b ruling), `ChaserBehaviour` reads both instead of the spec, `EnemySystem` holds the run's `Depth` and scales inside `Spawn` so there is nowhere to forget it, and `Descent.asset` carries twenty-one authored numbers behind one Inspector foldout.
**Deviations:** 6, in the spec's As built. The one that changes a decision: **`EnemySystem` takes its `DepthScaling` as a constructor argument**, which makes rule 12's own claim structurally true rather than conventional — at the price of a fifth parameter across fourteen call sites, two in fixtures the ripple row did not name. A sixth file (`Tests/Core/Support/Scalings.cs`) exists because nine fixtures build a `ModeSpec` and eight of them are not about its difficulty model.
**Learned:** **a struct's zeroed form is worse than null when every field means a multiplier** — a `default(StatCurve)` caps at 0 and gives every enemy in the game no hit points, in silence, so each curve carries an `IsAuthored` the spec checks → AR §18.3. **The formula and the design doc disagreed, and the code had to say which wins out loud:** `B(40)` is 1,876.9 against GD §12.1's table row of 1,772, pinned in two tests and in a §18.3 line that says not to "fix" the code to match the table → AR §18.3, parking lot (the doc edit is the owner's). **An allocation row measuring an unmodified stat measures the wrong thing** — `Stat`'s cache keeps its laziness only while nothing is subscribed, so the crowd is now at depth 40 and the per-frame reads go through a real stack → spent. **Wiping by source token is a list that gets one entry short** → ledger row 2's answer, AR §18.1.
**Follow-ups:** **ledger row 2 closed and struck** — `Stat.RemoveAll()` with no argument, called by `EnemyAgent.Initialise` before it re-bases, and `Recycle_ForgetsAForeignModifier` is what makes the distinction real. Three orderings and three number rules added to AR §18; AR §11.1's `Stat` sketch gained the overload. `ModeSpec.Scaling` is what M2-02 deliberately left out. Next is **M2-04**, which chooses the device cap against measured cost and answers ledger rows 4 and 5.

### 2026-09-11 · M2-04 · `WaveComposer`: a stage's budget becomes waves, under a priced cap

**Built:** a stage is a fixed list of waves now. `WaveComposer` splits GD §12.1's budget across GD §12.2's waves rising triangularly, buys archetypes from the mode's roster with draws off the `Spawn` stream, and when the concurrency cap binds it stops adding bodies and starts **upgrading** them — GD §11.2's device independence, spelled as arithmetic. `WavePlan` is allocated once per run and refilled per stage, `EnemySpec` carries GD §8.1's `ThreatCost` (Husk 4) now that something reads it, and `BootInstaller.DeviceEnemyCap = 28` is where the cap and the arithmetic that priced it live together. Nothing spawns from a plan yet — that is M2-05.
**Deviations:** 6, in the spec's As built. The one that changes a decision: **a stage's spend is tracked as `int` and each wave's allowance is the cumulative share minus it, not a running `float` remainder** — rule 3 reads as a running carry, and a running carry composes *nine* Husks at stage 1 against GD §12.1's ten, because `B·1/3` less 12 plus `B·2/3` is 27.999999 and the tenth Husk costs 4 of the 4 that are not quite there.
**Learned:** **a spec's headline number is the test that catches its own arithmetic** — `Compose_StageOne_TenHusks` is the only row here asserting a published figure, and it is the one that failed the obvious implementation → AR §18.3. **`stackalloc` is not available for a managed struct**, and `ModeSpec.RosterFor`'s own doc comment promised a `stackalloc` caller that cannot exist: `RosterEntry` holds a `ContentId`, which holds a `string` → spent, fixed in the comment. **A grep for `new TypeName` still misses target-typed `new(...)`** — eight sites by grep, fifteen by compiler, seven of them invisible; loud only because each passed `isElite:` out of position → Traps §2 (already there, re-confirmed). **`TestRunnerApi.Execute` is refused from an MCP command again**, and Traps §4 said it was not: every dodge inside a command fails pre-execution, so the call has to live in an assembly that already references the runner → Traps §4, rewritten.
**Follow-ups:** **ledger row 4 closed and struck** — the O(n²) ally count stays, 756 comparisons a frame at 28, and a cap above 40 needs a spatial hash first (parking lot). **Ledger row 5 is half answered**: the path-refresh ceiling is measured and written down (24 sustained at 60 fps, 12 at 30, against a cap of 28) and deliberately *not* worked around by lowering the cap, which would have hidden the debt in a constant — **M2-05 owns the fix.** One invariant added to AR §18.3. Next is **M2-05**, the director that paces, telegraphs and places them.

### 2026-09-11 · M2-05 · `SpawnDirector`: waves that overlap, spawns that telegraph, paths that keep up

**Built:** a composed stage now happens. `SpawnDirector` (`Core/Director/`) paces GD §12.2's waves — one body per 0.35 s, round-robin across a wave's archetypes, the next wave starting when a quarter or less of this one is still standing — announces every body 0.8 s before it exists (`SpawnEvents.cs`: `WaveStarted`, `WaveCleared`, `SpawnTelegraphed`), and puts it somewhere at least 6 m from the player and 2 m from every ring already up. The whole difficulty spine is wired at last: `RunSession.Start` composes `config.StageIndex` into a run-lifetime `WavePlan` and `Tick` gains a director step after the death check, so `ThreatBudget`, `DepthScaling`, `WaveComposer` and `WavePlan` all reach the arena. `PathRefreshBudget` (`Game/Adapters/`) replaces `NavPathSense.MaxRefreshesPerFrame = 4` with `ceil(n · refreshHz · dt)`, and `StalePathCount` is the counter that says when it cannot keep up. `RunScope`'s `keepAlive` is 0: the director is the only spawner in a run.
**Deviations:** 7, in the spec's As built. The one that changes a decision: **`RunSession` gained a sixth constructor parameter, `deviceEnemyCap`**, rippling 24 positional call sites across six fixtures the ripple row did not predict — ruled by the owner against a settable property, because a cap a scope forgets to set composes a whole run at the wrong difficulty in silence.
**Learned:** **the spec's instruction to re-run PlayMode is what found the only real bug** — `NavPathSense`'s new third parameter broke the run's whole composition, because VContainer never falls back to a C# default, and 625 EditMode rows were green while it did → the incident is named in `RunScope`'s own comment, which had already warned about it once. **A telegraph is a promise**, so the cap counts the pending and refuses new rings rather than dropping issued ones → AR §18.4. **Choosing a place makes exactly one draw however it ends** — now the second spawner to owe it → AR §18.3. **Composition must happen before `RunStarted`**, or a mis-authored mode strands the announcement, which is ledger row 3's failure re-made → AR §18.1.
**Follow-ups:** **ledger rows 5 and 9 closed and struck** — the path budget scales and *reports*, and a claimed spawn point cannot be reused while its ring is up. Nine ledger rows left. Next is **M2-06**, the Spitter and the Bloater as authored data.

### 2026-09-11 · M2-06 · The Spitter and the Bloater as authored data, and three archetypes you can tell apart

**Built:** three archetypes exist as content. `ProjectileSpec` + `ExplosionSpec` (`Core/Content/`) are the two optional blocks an enemy now has, both **carrying no damage** — a shot and a blast deal the agent's `ContactDamage`, so GD §12.3's depth curve reaches them for free. `EnemySpec` gains them plus `AggroRange`, moved off `ChaserBehaviour`'s `const` because two behaviours need it, and **a kind requires its block while a block does not require its kind** — which is exactly what `Spitter.asset` is until M2-07b: a filled-in projectile block on an archetype authored `Static`. `Spitter.asset` (28 HP / 7 threat / priority 3 / 14 m standoff) and `Bloater.asset` (24 / 8 / 2 / 3 m blast) ship with GD §8.1's numbers, and `Descent.asset` gains their stage-2 and stage-4 roster rows, which is what stops every wave M2-05 composes from being Husks. `EnemyLook` + `EnemyLookBook` (`Game/Authoring/`) tell them apart with a tint and a scale on the **one shared body** (GD §11.3) — `BootInstaller` builds the book, `EnemyViews` applies it on every rental, `EnemyHitFeedback.SetArchetypeLook` re-bases the two fields the flash, the swell and the dissolve are all written relative to.
**Deviations:** 5, in the spec's As built. The one that changes a decision: **`BootScope.prefab` had to be edited**, which the Files table did not list — a definition that is not in the boot list never reaches the catalog, and `Descent.asset`'s new roster rows would then name archetypes `RunSession.Start` refuses to resolve, so the two assets and the boot list are one change or the game does not start.
**Learned:** **a look applied on every rental is what makes the pooled-reset rule hold without a third field to clear** — `ResetVisuals` restores the look the body was *rented* with, and the next rental overwrites it, so a tint cannot be inherited → AR §18.4. **`Is.EqualTo` on a `Color` is exact where Unity's `==` is approximate**, and a colour round-tripped through a `MaterialPropertyBlock` came back printing identically and comparing unequal → spent (the fixture compares channels within a tolerance). **A `[TestFixture]` field initialiser is the only capture that survives EditMode**, where `Awake` never runs — which is what lets `_prefabScale` default to one and the scale multiply stay idempotent → spent.
**Follow-ups:** **Flagged for the owner, not fixed:** at the Censer's 13 damage a 24 HP Bloater dies in **2 hits**, one below GD §12.4's TTK band of 3–5 — either the 24 or the band's lower bound wants a one-line ruling, and M2-15 is where acceptance fails on it otherwise. No ledger row closed; nine left. Next is **M2-07a**, projectiles as a point and a moment rather than a body.

### 2026-09-11 · M2-07a · `ProjectileSystem`: shots already in the air, and who decides they landed

**Built:** a run can carry shots that are in the air, and **the fact-versus-direct-call question is answered in writing before two archetypes answer it two ways** (ledger row 7, closed). `ProjectileSystem` + `Projectile` (`Core/Combat/`) hold a shot as **a point and a moment, not a body**: `Fire` computes the impact point and the arrival time once from the XZ distance and the speed — the Spitter's 14 m at 12 m/s is 1.17 s, which is GD §8.1's "punishes standing still" as arithmetic — and nothing about the shot changes afterwards, so there is no per-tick position in core and no per-tick intent. `ProjectileFired` carries the whole flight so M2-09's view needs nothing else; `ProjectileImpacted` is published **whether or not it hit**, because the view has to stop existing either way. A landing calls `PlayerCombat.ApplyDamage` **directly** and does not consult i-frames — a shot that arrives during a dodge is still a shot that arrived. `ProjectileSystem` is now the sole writer of `CombatBlackboard.IncomingProjectiles`, the field M1-08 left saying "zero until M2-07". `RunSession` composes one per run, clears it at `End`, and ticks it **after the enemy behaviours and before the death check**; `RunState.Projectiles` is `internal` with an `InFlightProjectiles` scalar, and `DebugOverlay` shows it. **Nothing fires one yet:** the count reads 0 for the whole task, which is what M2-07b's first manual step checks against.
**Deviations:** 6, in the spec's As built. The one that changes a decision: **rule 8's "array walked backwards on removal" cannot also keep rule 6's oldest-first order**, so the store keeps the rule's stated *reason* instead — all bookkeeping before any callout, over a second preallocated array — and arrival is explicitly not fire order, since a shot aimed closer can be fired later and land first.
**Learned:** **a port's remarks are where a decision rots first** — `IRunSession` promised two facts that had been wrong since M1-18 merged, and nothing could have caught it because a comment compiles → AR §18.2 (the rule, with the three counts the fact route was rejected on). **`PlayerCombat` holds no position**, so anything resolving geometry against the player is handed one; the spec's `Tick(now, player)` signature could not answer its own rule 6 → spent. **A spec's Tests table can ask for rows the spec's own design forbids** — three `Session_*` rows assume a shot in a live run, which `internal` state and "nothing fires until M2-07b" together make unwritable → spent.
**Follow-ups:** **Ledger row 7 closed; eight left.** **Handed to M2-07b:** the end-to-end ordering row — a shot that kills mid-run, `PlayerDied` then `RunEnded`, no intent written — and the first PlayMode run since M2-05. Rows 13 (cover does not block a shot) and 14 (damage from off-screen) are row 7's named price and keep their owners, M2-11b and M2-12a. Next is **M2-07b**, the Spitter that keeps its distance.

### 2026-09-11 · M2-07b · `IEnemyBehaviour`, and the Spitter that keeps its distance

**Built:** the arena has a second kind of mind in it, and the seam that lets a third exist. `IEnemyBehaviour` + `EnemyTickContext` (`Core/Ai/`) are two methods and one struct: the context is built **once per tick by `RunSession`**, never per agent, so widening what a behaviour may reach — M2-08 is next — is one line in one file instead of an edit to every implementer, every dispatch and every test that calls one. `SpitterBehaviour` walks GD §8.1's loop: Idle → Approach → Aim → Release → Recover. **Approach is a band, not a point** — beyond 14 m it walks in on the NavMesh path, inside 9.8 m it backs out on the straight line, between them it plants — and 9.8 is chosen against the Censer rather than picked, being just outside the Oathbound's 8 m reach and just inside its 12 m acquire. **Aim never cancels**, the deliberate opposite of the Chaser's windup: the dodge window is the flight, not the telegraph. `Release` throws at where the player is standing at that instant, for the agent's `ContactDamage` stat, at the archetype's speed and radius. `EnemyAgent.Behaviour` is the interface now, built by kind and kept across a recycle when the kind matches; `EnemySystem.Tick` takes the context and gives `Chaser` and `Spitter` one shared line. **`Spitter.asset` is off `Static`** — the PR that can run one is the PR that flips it.
**Deviations:** 9, in the spec's As built. The one that changes a decision: **`Initialise` builds by kind with `as`, so a Chaser recycled as a Static now loses its behaviour object** — M1-05's remarks promised it would keep it, and that paragraph was rewritten rather than left to lie.
**Learned:** **a guard belongs where the thing is built, not where it is used** — the context is constructed once a frame for the whole arena, so it can afford the six checks that `Tick` cannot → AR §18.2. **Negating a path direction does not point away from the player**, it points away from the next waypoint, which walks a retreating enemy into the pillar it was just routed around → AR §18.4. **A Tests table can name a row its own assembly cannot hold** — the asset row needs the `AssetDatabase` and `Soulvail.Tests.Core` has no engine reference, so M2-06's `EnemyLookTests` row flipped instead → spent.
**Follow-ups:** **the two things M2-07a handed over are both landed** — the end-to-end row (a bolt that kills mid-run: `PlayerDied` then `RunEnded`, no intent written) and the first PlayMode run since M2-05, 3 green. Ledger row 7 is *applied*, not closed again; eight rows left, and M2-08 is the last archetype that has to obey it. Next is **M2-08**, the Bloater's fuse.

### 2026-09-11 · M2-08 · `BloaterBehaviour`: a fuse you have to walk away from, and a blast the corpse owns

**Built:** the third archetype, and the first enemy that kills itself. `BloaterBehaviour` (`Core/Ai/`) is three states — Idle → Waddle → Fuse — and the two missing ones are the point: there is no damage state and no recovery, because **the blast is not in the behaviour at all**. "Explodes on death or contact" is implemented as *contact kills it, and death explodes it*: the fuse ends by calling `EnemySystem.ApplyDamage` on its **own id for its own remaining hit points**, and `EnemySystem.Explode` resolves the blast because the archetype's spec carries an `ExplosionSpec`. **The trigger is the spec, never the kind**, so a Bloater shot at range, cut down mid-fuse or killed by a Charge goes off by the identical path — no `switch`, no flag, and M7-02's Volatile affix is a spec block rather than a new mechanism. `EnemyExploded` (`Core/Events/`) carries the centre, the radius and `HitPlayer`, and is published **even when it catches nobody**, because a view must draw the flash either way. **The blast reaches the player and nothing else** — the owner's M2-00c ruling: a chain would re-enter `ApplyDamage` mid-call against a registry `Tick` may be walking, which wants a work queue and is bigger than the archetype. `ApplyDamage` gained a required `PlayerCombat`, which is the honest signature now that hurting an enemy can hurt the player; both production callers are inside `PlayerCombat` and pass `this`. `EnemyTickContext` gained the census in the one line M2-07b promised. **`Bloater.asset` is off `Static`.**
**Deviations:** 7, in the spec's As built. The one that changes a number: **rule 9's escape arithmetic is written against the Oathbound's 5.4 m/s and the owner's retune left it at 3** — `Fuse_IsEscapable` pins both, and at 3 m/s the 2 m trigger clears the 3 m radius by 1.4 m, which is what the rule's "a metre to spare" actually describes.
**Learned:** **the day a warning names arrives, and updating it beats deleting it** — `EnemySystem.Tick` warned that a self-retiring behaviour would owe a backwards walk; it does not, because `ApplyDamage` leaves the corpse *registered*, but the next behaviour to call `Despawn` directly still does → AR §18.1. **A fixture that hand-writes perception can pass while proving nothing** when the thing under test reads a *different* copy of the same fact — the blast resolves against `EnemySystem`'s ingested player position, not the blackboard → Traps §7. **A spec's ripple row is a guess and the compiler is the authority**: it named three fixtures and the right answer was four different ones → spent.
**Follow-ups:** **Flagged for the owner, unfixed:** GD §6.1's 5.4–6.2 m/s class band now contradicts the shipped Oathbound in a third place, this task's rule 9 being the newest — it is already the Known-issues item M5-02 owns. Ledger row 7 is applied for the last time; **eight rows left and no archetype still owes it**. Next is **M2-09**, which finally draws a bolt.

### 2026-09-11 · M2-09 · Projectile views, and the first test that proves a pooled body forgets its last life

**Built:** the game draws a bolt. `ProjectileViews` + `ProjectileView` (`Game/Views/`) rent a body on `ProjectileFired` and return it on `ProjectileImpacted` — `EnemyViews`' shape one layer simpler, because nothing here reports back: core never held a position for a shot to disagree with. The ground track is the same straight `lerp` core measured the flight time along and the arc is `4h·t(1−t)` on Y alone, so the hump that makes a shot read as *incoming* cannot change an outcome core's XZ hit test decides. **The body owns no `Update`** — `RunTicker` steps it with `snapshot.Dt` beside the two bodies it already moved (AR §18.1's last ordering row, one view family along). Pooled to core's projectile capacity and registered in `RunScope` beside `EnemyViews`; `DebugOverlay` gained `bolts rented/pooled`.
**Row 8's pooled-view half is closed.** `Tests/PlayMode/PoolingLifecycleTests.cs` takes an `EnemyView` through hit → wind-up → death → four frames of dissolve → return → re-rent and asserts scale, material, tint, alpha, collider, id and velocity against a body the pool has never handed out — plus a shove caught mid-slide, a bolt that flew its whole flight, and twelve lives through a pool of four. The first PlayMode rows any task has added since M0-17.
**Deviations:** 8, in the spec's As built. The one that changes what a row asserts: **`Ticker_StepsWithSnapshotDt` is asserted at the view**, because nothing in the project constructs a `RunTicker` and rule 12 keeps its coverage with M2-11b — the step integrates the argument it is handed, and `ProjectileView` is asserted by reflection to declare no `Update`, `LateUpdate` or `FixedUpdate`, which is the property that makes the ticker the only thing able to advance a bolt.
**Learned:** **a Files table can name a file that cannot hold the thing** — `RunInstaller` was specced as the registration site for an object two of whose arguments are scene references, and the spec's own Public API block is what proved it wrong; the installer is deliberately the scene-free half → spent. **A pool rented one body at a time hands back the same instance every time**, so "twelve lives, four instances" passes with a prewarm of one unless the set is held concurrently — the row became three rounds of four → spent. **An inactive template is what makes a PlayMode pooling fixture possible**: VContainer deactivates a prefab before instantiating and injects the copy while it is off, so an inactive template still yields an injected instance and never runs the `Start` that throws when nothing injected it → Traps §6.
**Follow-ups:** ledger row 8 halved — the frame-order half stays with **M2-11b**, and it is now the only thing in the project with no fixture at all. `EnemyExploded` is the last of the three unseen events; its ring is M2-12's. Next is **M2-10**, which is also what ends the `_startingStage`-in-the-Inspector workaround the manual steps have needed since M2-07b.

### 2026-09-11 · M2-10 · `StageFlow`: a stage that arrives, seals, clears, and lets you leave through a door

**Built:** a stage stopped being a number a run was composed at. `StageFlow` (`Core/Stage/`) is five phases on core's own clock — **Arrival** (2 s, sealed and announced, nothing spawns) → **Waves** (the director runs until `IsStageComplete`) → **Clear** (1.5 s, the door opens, the next arena is named) → **Gate** (no timeout, 1.5 m on XZ) → **Transition** (0.3 s) → the next Arrival — and `StageEvents` (`Core/Events/`) carries the three things outside core may need: `StageArrived`, `StageCleared`, `StageTransitionStarted`. **GD §7.1's two seconds are now a pause with a cause**, which is the one M2-05 rule 1 refused to insert because nothing then sealed or announced it. `IsStageComplete` finally has its reader: M2-05 rule 6 left it deliberately unpublished so "the stage is over" would have exactly one publisher, and this is it. `WorldSnapshot` grew `GatePosition`/`HasGate`, `SnapshotBuilder` reports them from a `Transform` every frame, `Run.unity` gained a `Gate` slab and `Hud.prefab` a `Fade` cover, and `DebugOverlay` reads `depth n <phase>` and `gate 4.2` — or `gate: —`, which is rule 15 being visible rather than mysterious.
**`RespawnPolicy` is deleted, not deprecated** — with `EnemySystem.ApplyRespawn`, `SpawnPlan.Respawn`, `RunScope`'s three serialized fields and `RespawnPolicyTests`. M2-05 set `keepAlive` to 0 and left the type standing for one PR; that PR is over, and a second spawner keeping twelve bodies breathing regardless of the wave plan was a bug waiting for someone to set the field back to 12. **The owner approved the deletion in the task brief**, which is what the spec asked for. `Session_NothingRespawns` is the one behavioural assertion that it is gone.
**Fixed in branch, found by the owner's playtest:** walking out of stage 4 threw `IndexOutOfRangeException` from `SpawnDirector.SweepTheDead`. `Advance` recomposed the run's one `WavePlan` while the director still held it, and the director sizes its per-wave arrays from the plan's dimensions at `Begin` — W(n) goes from two waves to three at stage 5, so the first frame of the new arrival walked off the end. One line: `_director.Clear()` before the recompose.
**Deviations:** 10, in the spec's As built. The one that changes a decision: **`Begin` adopts the opening stage's composition rather than making it, and drops the spec's `spawn` parameter** — composing there would either publish `StageArrived` into a run that has not been announced, or let an ineligible mode throw with the run announced and half an arena standing, which is **ledger row 3**, the exact bug M2-02's ordering was built to prevent. `Tick` still takes the stream, because a *boundary* composes.
**Learned:** **a fixture built for readability can be blind by construction** — every row here composed from a *flat* mode, one wave and a fixed concurrency, which is what makes a pacing assertion readable and is also why nothing ever saw a plan change shape across a boundary; two rows now use GD §12's real curves and nothing else does → AR §18.1 (*nobody may recompose a plan a director is still holding*). **a spec can ask for a spy that the code's own sealing forbids** — `EnemySystem` is `sealed` and the clears publish nothing, so rule 10's ordering is asserted by its consequence (the first body of the new arena is priced at the new depth) rather than by observation → spec's *As built*. **A phase boundary asserted on the exact constant fails by float accumulation**: a clock summed a step at a time reads 1.499999 where 1.500000 was meant, twenty stages in → spent, and written into the fixture's own helper. **An enemy in a core fixture never moves** — a move is an intent, and only a body reporting back through the snapshot changes a position, so a row that wants a Husk to reach the player walks the *player* → spent.
**Follow-ups:** **the `_startingStage`-in-the-Inspector workaround is over** — every manual step since M2-07b needed `Descent.asset` temporarily set to 2 and uncommitted; a run now reaches stage 2 and stage 4 by playing. `ClearTime` = 1.5 s is flagged as the one invented number and is the device session's to tune (GD §7.3's 40–75 s stage). Eight ledger rows still open; none moved. Next is **M2-11a**, the arena contract and pool — which is also what takes the gate off `RunScope` and puts it on an arena prefab.

### 2026-09-12 · M2-11a · The arena as a contract: a pool of them, a barrier, and a door

**Built:** a stage happens somewhere specific. `ArenaView` (`Game/Arena/`) is a prefab full of named places — a player start, a door, spawn points, a barrier, cover pillars on a new `Cover` layer and a pre-baked `NavMeshSurface` — and it decides nothing, ticks nothing and validates itself against GD §7.2 in the Inspector. `ArenaPool` beside it is `EnemyViews`' shape at the scale of a whole room: `StageArrived` raises one, places the player in it and seals it; `StageCleared` opens its door and builds the **next** one inactive. **Every arena stands at the same world origin and only one is active**, which is what makes "the next stage is not visible" true by construction rather than by where the camera is pointed — and the parked ones live under a deactivated root, so a prepared arena never runs an `Awake`, never enables a renderer and never adds a second set of navigation data on top of the room being fought in. **`ModeSpec.ArenaFor(stage, seed)` is arithmetic, not a draw**: a run resumed at stage 7 lands in the arena stage 7 always had with no saved state, and arena identity does not depend on how many spawn positions the previous six stages happened to reject.
**`GreyBox.prefab` became `Arena_Pillars.prefab`** — it was already GD §7.2 read literally — and `Arena_Tiered.prefab` is the second, a dais and four ramps with its own bake, so that the swap, the no-repeat rule and the per-arena NavMesh are exercised by something rather than asserted. **Spawn points left `SpawnPlan`**: a run has one room per stage now, so where a body may go is a fact about whichever arena is standing — it rides in on `WorldSnapshot.SpawnPoints` and reaches `SpawnDirector.Begin` when a stage leaves arrival. `Run.unity`'s dressed-in grey box and gate slab are **deleted**; the scene carries an empty `Arenas` root and the pool raises the rest.
**Deviations:** 9, in the spec's As built. Two change a decision: **`ArenaFor` replays the chain from stage 1** rather than hashing once, because the no-repeat rule cannot be evaluated in O(1) — comparing against the previous stage's *raw* index is the O(1) version and it repeats; and **rule 4's core-side arena check was not written**, because arenas are not catalog content and core has nothing to resolve an id against. The throw lives in `ArenaPool`, which for the opening stage is the run's first frame anyway.
**Learned:** **a body instantiated under a deactivated parent never runs `Awake`**, which is the whole of "prepare is not raise" — instantiate-then-deactivate would add and remove a `NavMeshSurface`'s data in the same frame, over the arena the player is still in → AR §18.2. **A hash cannot answer a question whose input is its own previous answer** → the spec's *As built*, and the method's remarks carry the cost. **`OnValidate` fires the frame a component is added**, so a rule that errors there makes adding the component to a new prefab a failure and every fixture noisy → spent (it warns, and is silent while nothing is dressed).
**Follow-ups:** **ledger row 13's layer half is done and the row stays** — the pillars are on `Cover`, and M2-11b's *As built* is what retires it. Eight rows open; none moved. `StageArrived.ArenaId` and `StageCleared.NextArenaId` have a subscriber at last. Next is **M2-11b**, the line-of-sight sense — and the frame-order half of row 8 with it.

### 2026-09-12 · M2-11b · `LineOfSightSense`: a pillar you can hide behind, and the frame order under test

**Built:** cover does something at last. `LineOfSightSense` (`Game/Adapters/`) is `NavPathSense`'s shape applied to a different question — a cached answer per enemy, refreshed round-robin at 10 Hz inside the `PathRefreshBudget` M2-05 already built, **5 raycasts a frame at 28 enemies and 60 fps, 10 at 30** — and `SnapshotBuilder` fills the `EnemySense.HasLineOfSight` that has been carried unanswered since M0-05. A Spitter with a pillar between it and the player **plants, faces, and does not begin the wind-up**; one already aiming still fires, because M2-07b's *an aim never cancels* is untouched. So the pillar is a shield you have to be behind *before* the telegraph, which is a positioning decision rather than a reflex, and AR §18.4's *a telegraph is a promise* survives intact. **Core still holds no walls** — ledger row 7's ruling paid for, as a sense and not a fact. `Tests/PlayMode/FrameOrderTests.cs` builds a real `RunTicker`, which nothing in the project had ever done, and pins AR §18.1's frame order by observation.
**Deviations:** 12, in the spec's As built. Three change a decision: **the stage boundary does not call `Clear`** — `SnapshotBuilder` compares `ArenaPool.Active` against last frame's and clears on a swap, which is the geometry rather than the announcement and needs no new subscription; **`EnemyViews` losing the hard `false` made the builder the only writer of the field**, so it now writes it on every path including the null-sense one, where the answer is `true`; and **an empty cover mask throws**, in the sense and again in `RunScope`, because an empty mask is not a degraded run — it is the game M2-11a shipped, silently.
**Learned:** **the ray is the project's one 3D perception, and the exception had to be written down** — occlusion is not a separation, so AR §18.4's *all perception is XZ* gains a named carve-out rather than leaving the next reader to rediscover it → AR §18.4. **A permissive default is chosen by its failure mode, not by which answer is "safer"**: unmeasured means *can see*, because the other way round switches a whole archetype off in silence while this one degrades to behaviour that has already been playtested → AR §18.4. **A cadence row's clock starts when the answer was taken, not when the run began** — the one red row on the first suite pass, and the fixture's fault → spent (the row says so). **Only two of a frame's seven steps call anything a fake can see**, so an order is pinned by consequences rather than by a recorded sequence → the spec's As built, deviations 9 and 10.
**Follow-ups:** **ledger rows 13 and 8 both leave the table.** Row 13 is answered end to end; row 8 is closed on the six steps that carry its named failures, with `CommandPhase` recorded in AR §18.1's row as the one step no fixture in this assembly can reach — it needs the Input System, and the consequence of moving it is a frame of latency rather than a wrong outcome. **Six rows open.** `Run.unity` was edited outside the Files table — one serialized mask, a three-line diff. Next is **M2-12a**, the threat arrows and the held focus, which answers rows 12 and 14 together.

### 2026-09-12 · M2-12a · Screen-edge threat arrows, and a focus that reads as held

**Built:** two things the player cannot see stopped being invisible in one visual language. `TargetChanged` carries a fourth field, `HeldFocusId` — derived at `PlayerCombat`'s one publish site from two ids `Targeter` already had, so **core decided nothing new** — and `ReticleView` grew a second, fainter, non-pulsing marker that parks on the enemy the player *tapped* when the gun has taken something else. `ThreatArrows` (`Game/Presentation/`) is a listener and a projector: it holds its own census, keeps a fixed set of instances rather than a `ViewPool`, owns a real `LateUpdate` as an `ILateTickable`, and puts a **red-orange** arrow on the screen border for every living enemy off-camera inside 16 m, a **cyan** one on a held focus at any range, and one on every survivor once GD §7.3's eight seconds pass with three or fewer left. Arrows slide along the border out of GD §12.4's two thumb corners, fade and shrink with distance, and never carry text.
**Deviations:** **nine, three that change a decision** — the arrows are registered in `RunScope` and are **required** rather than optional, because GD §12.4's on-screen rule is an invariant and a run that can be shot from off screen with no indicator is unfair in a way nothing reports; the constructor takes a `PlayerView` and a `Func<float>` the spec's signature omits, because the range is measured from the character (the camera stands 8.7 m back) and eight seconds is not a thing a test can wait for; and it keeps its own census, `EnemyDied` included, because `EnemyViews` can say *where* but not *who is alive*. The rest are in the [spec's As built](tasks/M2-12a-threat-arrows-and-held-focus.md). **The predicted ripple does not exist** — `new TargetChanged(...)` had exactly one call site in the project and none in any test.
**Not merged.** EditMode is **911 green, 0 failed (11.6 s)**, zero compile errors, zero new analyzer warnings, `git diff ProjectSettings/` empty, and all three asset diffs are pure insertions. **PlayMode is 9/11 or 10/11**: `FrameOrderTests.Ticker_RunsTheStepsInOrder` and `Ticker_ReportsFactsAfterBodiesMoved` fail on their final assertion only — the `ConeReport` one — while every ordering assertion in both passes. `dev` is green twice; this branch is red consistently; **no code from this task runs inside that fixture**.
**Learned:** **`m_AutoSyncTransforms` is 0, so `RunTicker` asks physics a question about an arena it has not told physics about.** The frame order is right and the physics scene is a frame behind it, which means AR §18.1's *"a sweep resolves against this frame's arena"* is held by the call order and not by the code — a swing can miss an enemy that moved this frame. Three other fixtures paper over it with `Physics.SyncTransforms()`; `FrameOrderTests` cannot, because the move and the query are inside one `Tick()`. **The fix is one line in `RunTicker` and it is a correctness fix, not a test accommodation** — left for the owner, because `RunTicker` is outside this task's Files table → the spec's As built, last section. **A spec's ripple row is a prediction, not an inventory**: this one named test rows that have never existed, and the grep that would have caught it was never run → spent (deviation 5).
**Follow-ups:** **rows 12 and 14 are answered but stay in the ledger until this merges.** One decision is waiting: whether `RunTicker` gains the sync. **Merged as PR #66 with that decision still open** — it outlived the task, so it moved to Current State → Known issues, and M2-12b measured it.

### 2026-09-12 · M2-12b · The ring that says *something is about to happen here*

**Built:** the two events that have been describing a circle on the floor to nobody since M2-05 and M2-08 finally draw one. `TelegraphRingView` is a single additive quad whose **scale is its radius**, and the two rings are one body told apart by how it is bound: a spawn ring fills — a disc growing to the rim over the director's own 0.8 s, so the ring left is the time left — and a blast ring is drawn at the archetype's real `ExplosionSpec.Radius` on its first frame and fades. `TelegraphRings` is `ProjectileViews`' shape with the index removed, because **M2-05 rule 7 shows up here as two absent methods**: a telegraph is a promise, so there is no cancel path and no id to cancel by, and a ring leaves service by running out. Pooled, prewarmed to 8, stepped by `RunTicker` on `snapshot.Dt` beside the bolts, and counted in `DebugOverlay` as `rings rented/pooled`. **`EnemyExploded` was the last of the director's three events with no subscriber; all three are now drawn.**
**Deviations:** **eleven, three that change a decision** — the census is registered in `RunScope` and is **required**, with a second guard that the prefab's quad is assigned, because a ring with no quad is timed and sized and returned correctly and is never visible; a spawn ring's radius is **derived**, `MinSpawnSeparation / 2` = 1 m, which is the only circle core tests for a spawn and is why two spawn rings can never overlap; and the fill is the quad's **scale** rather than a shader property, which keeps rule 10's one-quad budget literal at the price of there being no rim at full radius until the disc arrives. Details in the [spec's As built](tasks/M2-12b-telegraph-rings.md).
**Verified:** **931 EditMode green, 0 failed, 0 skipped, 11.3 s** — 911 plus 20 `TelegraphRingsTests`. Zero compile errors, zero new analyzer warnings, `git diff ProjectSettings/` empty, `Run.unity` **+34/−0** with no re-serialisation collateral. **PlayMode 9/11**, the two known `ConeReport` rows — and the measurement below is the point of this line.
**Learned:** **`new MaterialPropertyBlock()` in a field initialiser throws, and it throws at *import*** — `"CreateImpl is not allowed to be called from a MonoBehaviour constructor (or instance field initializer)"`, four times, from inside `AddComponent` and prefab serialisation, in code that compiles clean and that no test would ever run. `ReticleView` builds its own in `Awake` for that reason; `Awake` never runs in EditMode, so a pooled body bound from a fixture would paint through a null, and the answer is neither place but the first paint → **Traps candidate, filed in the spec's As built deviation 8.** **`dev` is not green either:** it fails one `FrameOrderTests` row on its own, reproduced twice, which Current State did not say — and the one-line `Physics.SyncTransforms()`, applied temporarily and reverted, takes this branch to **11/11**. The ring step lands inside the very window the staleness lives in, which is why the count moved by one; it touches no collider → Current State → Known issues.
**Follow-ups:** none created; six ledger rows still open. Two things left for the owner and neither is code here: **the sync line** (now measured, `grep SyncTransforms` over the branch returns nothing) and **whether GD §16.4's palette earns a file of its own** — reading `ThreatArrows.Danger` from `Views` keeps `#FF4A1F` in one place but makes `Views` and `Presentation` mutually dependent. Next is **M2-13a**, the save port and the DTOs.

### 2026-09-12 · M2-13a · The save port, the DTOs, and the stream position

**Built:** core can now describe a run completely enough to rebuild it. `RunSnapshot` and `PlayerProfile` are two versioned `readonly struct`s behind one async `ISaveStore`, and `IRandom` grew `Capture()` / `Restore(in RandomState)` — five stream positions in index order, which is the half of [ledger row 1](ROADMAP.md#carry-forward-into-m2) that makes a resume **sayable** at format v1. **Position is not identity:** the seed selects which of 2^63 sequences each stream walks and the state says how far along, so a restore onto a same-seeded generator is complete and `Restore_IsCompleteAcrossAFreshGenerator` is the row that shows it. Nothing calls any of it and not a byte is written to disk — M2-13b spells it, M2-14a captures, M2-14b restores. `InMemorySaveStore` joins `FixedRandom` and `FixedClock` on the fakes shelf, counting its writes and able to fault one.
**Deviations:** **eleven, three that change a decision** — six test rows had to live in `Tests/Game` because `Soulvail.Tests.Core` cannot reference `Soulvail.Game` and so cannot name `SeededRandom` at all; `FixedRandom`'s scripted position clamps a `ulong` into `int`, so a real generator's state word exhausts the script instead of overflowing; and `FailNextWrite` arms `ClearRun` too, because a clear is a write in every medium that has one. Details in the [spec's As built](tasks/M2-13a-save-port-and-dtos.md).
**Verified:** **960 passed, 1 failed, 0 skipped, 13.3 s** — 931 plus **30 new rows, every one green**. Zero compile errors, zero new analyzer warnings, `git diff ProjectSettings/` empty, no asset or scene touched.
**Learned:** **a spec can file a test row in an assembly that cannot see the type the row is about**, and only the compiler says so — the six capture/restore rows name `SeededRandom`, which is a `Soulvail.Game` adapter, so no `Tests.Core` file could ever have held them. The general form: **a row about an adapter belongs in `Tests.Game`, whatever the spec's Files table says** → filed in the spec's As built, deviation 1. **`dev` fails an EditMode row too, and it is a different one from the known PlayMode failure:** `Descent.asset` carries `_startingStage: 5`, committed in `ea0627f` (M2-12b) — the playtest workaround PROGRESS recorded as over → Current State → Known issues.
**Follow-ups:** none created. **Ledger row 1 stays open** — it leaves when M2-14b's *As built* says so; this task delivered the format that can carry it, 14a captures, 14b restores and checks the seed. Six ledger rows still open. Next is **M2-13b**, `LocalJsonSaveStore` and the migration harness.

_Next M2 entry below._

# Soulvail — Progress

**The log.** Where we are right now, and what each merged task actually did. Updated **in the same PR** as the implementation — never in a separate "update docs" commit.

The map is [ROADMAP.md](ROADMAP.md). The specs are in [tasks/](tasks/).

**Closed milestones are archived, not deleted:** [M0](archive/PROGRESS-M0.md) · [M1](archive/PROGRESS-M1.md) · [M2](archive/PROGRESS-M2.md). The log below covers the milestone in progress only. This file used to be 381 KB and could not be opened whole by any tool; keeping it to one milestone is what stops that happening again — **and M2 is the first milestone archived on time**, at its own acceptance rather than a milestone later.

---

## Current State

_Updated: 2026-09-13 (M3-00c)_

| | |
|---|---|
| **Milestone** | **M0 — Walking skeleton: complete, tagged `m0`. M1 — Combat feel: complete (21/21). M2 — Stage loop: complete (27/27), acceptance passed on Editor evidence.** `m1` and `m2` are both the owner's to tag. Now **M3 — Levelling and the tree** (3/31): three of its four spec groups are written, twenty-four specs stand where twelve titles did, and **no code yet**. The count moved 23 → 31 because M3-00c split all four of its titles; the tables are the authority and the Milestones row now agrees with them. |
| **Last merged task** | **M3-00c** — **specs for M3-09…M3-12, the screens and the first nodes.** **Twelve specs where the ROADMAP had four titles, because every one of the four split.** [M3-09a](tasks/M3-09a-pause-screen.md) the pause gate · [M3-09b](tasks/M3-09b-skills-screen.md) the Skills list, its trigger lines and the full-slots prompt · [M3-09c](tasks/M3-09c-profile-v2-and-first-active-hint.md) profile v2 and the one-time hint · [M3-09d](tasks/M3-09d-tree-view.md) the tree view · [M3-10a](tasks/M3-10a-skill-slot-buttons.md) S1–S4 · [M3-10b](tasks/M3-10b-progression-hud.md) the XP strip, the level, the auto-cast row and the Overflow toast · [M3-11a](tasks/M3-11a-bulwark-and-timed-effects.md) Bulwark and the expiry clock · [M3-11b](tasks/M3-11b-consecrate-and-zones.md) Consecrate and the zones · [M3-11c](tasks/M3-11c-skill-views.md) both views and **ledger row 5** · [M3-12a](tasks/M3-12a-addressable-stats.md) five addressable stats · [M3-12b](tasks/M3-12b-cooldown-and-knockback-primitives.md) two primitives · [M3-12c](tasks/M3-12c-oathbound-tree-v1.md) the twelve nodes and the TTK test. **Build order is ID order**, after M3-08b. **Five owner rulings:** Bulwark is timed shield points rather than a barrier; the tree view ships; a trigger line is `LocKey`s plus a formatted number; the twelve nodes are six stat lines, four rule changes and the two Actives; and the hint is worth a profile bump. **One ruling M3-00c made itself:** drag-to-reorder is not in V1 — M3-07b saves slot *positions*, so adding it later costs a port member and no bump. **Ledger rows 1, 2, 5, 6 and 9 all moved and none closed**; row 1 now has a table that closes it on paper at 3 / 4 / 5 hits. |
| **Task before that** | **M3-00b** — **specs for M3-06…M3-08, casting and choosing.** Five specs where the ROADMAP had three titles: [M3-06](tasks/M3-06-skill-runner.md) the `SkillRunner`, the 40 % floor and auto-cast; [M3-07a](tasks/M3-07a-auto-manual-and-slots.md) Auto/Manual, the four slots and the two commands; [M3-07b](tasks/M3-07b-save-format-v3.md) save format v3; [M3-08a](tasks/M3-08a-level-up-flow-and-pause.md) `IProgressionCommands`, the lazy offer, Overflow and the pause gate; [M3-08b](tasks/M3-08b-level-up-screen.md) the screen. **Build order is ID order, after M3-04.** Three owner rulings: **Auto/Manual survives a kill** (so M3-07 splits at the format seam, M3-01b rule 1's own terms); **pause gates the tick** rather than clocking it at zero, which is GD §11.4's *"idle the simulation"* read literally; **two pending picks are one screen that re-draws**. A fourth answered itself — AR §6 has put `CastSkill`/`SetAutoCast` on `IPlayerCommands` since M0-09. **Ledger row 9 opened** (node text is an unresolved `LocKey`); rows 6 and 8 gained a reader and a distinction. |
| **And before that** | **M3-00a** — **specs for M3-01…M3-05, the tree as data.** Seven specs where the ROADMAP had five titles, build order 01a → 01b → 05 → 02a → 02b → 03 → 04. Two owner rulings the group hangs on: **the tree is layered** (two nodes a tier × four + a keystone, 27 a class) and **CH §5.2 ships as authored**, its depth divergence pinned by a test. **Ledger row 7 closed with a hook**: a staged `ProjectSettings/` change now needs `ALLOW_PROJECT_SETTINGS=1`. |
| **Next task** | **M3-00d** — specs for M3-13…M3-15: health-bar treatment, content validation, and the milestone's acceptance. The last spec group; **M3-01a starts the code.** **What it inherits and must not re-argue.** **M3-13 is no longer a health-bar task alone.** It owns [ledger row 6](ROADMAP.md#carry-forward-into-m3), which M3-00c took from four readers to **eight across five files** — `OfferCard`, `TreeNodeView` (four kinds *plus* three node states), `AutoCastRow`, `ZoneView` and `BulwarkView`, every one of them serialized `Color` placeholders — so `Palette` is now a file with a known shape rather than a tidy-up. It also inherits a readout that did not exist when it was titled: `RunState.PlayerGrantedShield` (M3-11a rule 5), Bulwark's pool, which is GD §16.2's business and is deliberately *not* the Aegis ring. And the one hard colour rule already in force: nothing but danger may use `#FF4A1F` (M3-11c rule 10). **M3-14** has its checklist written for it in three places — M3-02b rule 6 (*every tree node is in the skill list, every Keystone is a last-tier singleton, every parent is in the same branch, every key resolves*), M3-02a rule 11 (*every shipped character has a tree*, which is what retires M3-03 rule 10's null-tree branch from a real build), and M3-12c, which gives it the first twenty-six assets to validate. **M3-15 carries four ledger rows and must rule on all of them.** **Row 1**: M3-12c rule 8's table says 3 / 4 / 5 hits at stages 1 / 15 / 30 and closes the row *on paper* — acceptance is where a played run agrees or does not, and the row's own warning is that a twelve-node tree leaves **29 Overflow levels** doing most of the work at stage 30. **Row 8**: there are now **two** pause holders, so play time and stopwatch time diverge by level-ups *and* menus (M3-09a rule 10) — M3-15 must say which number it quotes. **Row 9**: five text readers, twenty-four real keys, and one reader (M3-10b rule 8's 24 dp cells) that M6-10 **cannot** fix because there is nowhere to put text — that part is art. **Row 4**: the device debt is three milestones old and M3-10a adds the sharpest question yet — whether a thumb on the stick and a thumb on S3 are read at once. Also promoted to M3-15 by the parking lot: **CH §5.2's exponent**, which M3-01a rule 9 shipped as authored and said only a stopwatch could settle. |
| **What works** | **A run is a descent, and it survives the app closing.** Boot → Menu → `Descend` (or `Continue`) → a run that arrives in an arena, seals it, spends a threat budget on telegraphed waves, opens a door when they are dead, and puts the player one stage deeper with the budget, curves and scaling that go with it. **Core owns every outcome**; Unity reports facts and senses and renders events. Three archetypes fight differently — the Husk closes, the Spitter kites to 14 m and will not shoot through a pillar, the Bloater walks in and detonates — and all three are authored data on one shared body, told apart by tint and scale. Arenas change at a boundary, drawn from the mode's roster by seed and stage. Everything the player cannot see has an indicator: screen-edge arrows for off-frame threats, a dimmer marker for a focus held out of range, rings for spawns and blasts. A run writes itself to disk at its opening frame and at every boundary, and deletes itself on death. Composition is `BootScope` → `RunScope`/`MenuScope`, **no statics anywhere**. **Task-by-task detail is in the [M0](archive/PROGRESS-M0.md), [M1](archive/PROGRESS-M1.md) and [M2](archive/PROGRESS-M2.md) archives** — this row is the shape, not the history. |
| **What does not work yet** | **The player never gets stronger.** There is no XP, no level, no tree, no skills beyond the Charge — which is M3, and is why [ledger row 1](ROADMAP.md#carry-forward-into-m3) reads the way it does: a basic enemy takes 3 hits at stage 1 and 8 at stage 30, and nothing in the game closes that. There is **no boss** (M4), **no Sanctum** (M6), and **one class**. Stage 5 is not special and no code pretends it is. |
| **Owner retune, after M2-03** | **Five numbers, playtested and changed by the owner in the assets, not by a task.** Oathbound move speed **5.4 → 3**, Charge **10 m / 0.22 s / 5 m knockback → 8 m / 0.5 s / 4 m**, Husk move speed **3.5 → 2**. The whole game got slower and **the speed ratio barely moved** — 5.4/3.5 was 1.543×, 3/2 is 1.5× — so GD §6.1's *"every class must feel faster than almost every enemy"* survives, and escaping a Husk telegraph still works because the Husk plants itself during its windup (1.2 m gained against a 0.6 m cancel band). **Two derived numbers moved a long way and neither is authored anywhere:** the dash is 45 m/s → **16 m/s**, 8.4× move speed → 5.3×, and its invulnerable window is 0.27 s → **0.55 s**, now longer than a Husk's whole 0.4 s windup. CC §2.5, §5 and §7 were updated with the assets. **The class speed band was deliberately not touched** — see Known issues. |
| **Verified** | **M3-00c touched no C# either** — twelve specs and two plan files, fourteen in the PR — so the baseline is **still M2-15a's, now three spec groups old and unmoved: 1077 EditMode / 0 / 0, PlayMode 11/11.** **What was checked instead is the code the twelve specs claim to know, and it moved five of them.** `LocKey`'s member is **`.Key`, not `.Value`** — M3-08b rule 7 writes `spec.NameKey.Value` and will not compile; the four new screens use the real member and M3-08b resolves it in its *As built*. `RunState` has **no read for a cooldown in seconds**, only a fraction, so M3-09b adds one rather than letting a screen copy `CooldownRules` (M3-06 rule 1's whole point); **no read for availability**, so M3-09d adds one rather than re-deriving CH §5's gating in the presentation layer; and **no read addressed by slot**, only by runner index, so M3-10a adds two. `HapticsSettings.cs:85` really does author the whole `PlayerProfile` from one field, which is why M3-09c builds a store. `Health.Heal`'s own remarks really do already promise Consecrate that a heal will not reset the Aegis delay — the code named M3-11 before the task existed. `PlayerMotor` really has **no position** (only velocity and facing), which is why M3-11b puts one on the blackboard. `SaveWriter.cs:128` really clears the run on `PlayerDied` and not `RunEnded`, which is what makes M3-09a rule 8's *quitting does not delete your run* checkable. **And the arithmetic**, all of it recomputed: a twelve-node tree fills at level 13 (≈ stage 9), so stage 30's level 42 leaves 42 − 1 − 12 = **29** Overflow levels — not M3-08a rule 8's fourteen, which assumed the full twenty-seven — giving ×1.73 damage and **5 hits** on a 99 HP Husk, inside GD §12.4's band; 4 hits at stage 15, 3 at stage 1. Bulwark's 35 points is one Aegis-and-a-bit against CC §7's 30; Consecrate's twelve 3 HP pulses is 36 of 140, about a quarter bar; its 12 s cooldown floors at 4.8 s under CH §4.1's 40 %. |
| **Verified (M3-00b)** | **M3-00b touched no C# either** — five specs and two plan files — so the baseline is still M2-15a's, now two spec groups old and unmoved: **1077 EditMode / 0 / 0, PlayMode 11/11 three times running.** **What was checked instead is the code the specs claim to know:** `ProjectileSystem.cs:401` really is the only writer of `CombatBlackboard.IncomingProjectiles`, and it runs *after* combat — which is why M3-06 rule 7 puts the runner above the projectile step rather than below it; `ChargeSkill.cs:241` really does promise M3-06 the floor; `BootFlow` really does set `targetFrameRate` and not `timeScale`; AR §6 really has carried `CastSkill`/`SetAutoCast` since M0-09, which retired one of the four questions before it was asked. **And the arithmetic:** the 40 % floor at 2.5 s is 1.0 s and takes six −15 % multiplicative nodes (0.85⁶ = 0.377) or four additive ones; Overflow at stage 30 is **level 42 − 1 − 27 nodes = 14 levels = +28 % damage and +28 % max HP**, additively under one source, against ledger row 1's +52 %. **M3-00a touched no C#** — seven specs, two plan files, the hook, its README and one CLAUDE.md line. The hook was exercised rather than read: a temporary file staged under `ProjectSettings/` is refused with the file listed, passes with `ALLOW_PROJECT_SETTINGS=1`, and a clean index passes silently; the file was unstaged and deleted afterwards. **The spec arithmetic is checked against the formulas**: CH §5.2's `ToReach` at 2 / 8 / 13 / 22 / 30, cumulative budget through stages 5 / 10 / 20 / 30 / 35, and the levels those imply — all in [M3-01a](tasks/M3-01a-xp-and-levels.md) rule 9, and two of them become test rows. At M2-15a: six assemblies, zero compile errors, zero analyzer warnings; `git diff m1 HEAD -- ProjectSettings/` shows the `Cover` layer and one Unity-backfilled line, now a closed ledger row. |
| **Reference device** | **None, and the emulator is not a fallback.** BlueStacks 5 (Android 9 / SDK 28, x86_64) **installs the APK and crashes on launch** in its own Vulkan driver via `libhoudini.so` — an emulator-bridge fault on a development-build Vulkan path, not game code. The APK is ARM64-only and always will be: Unity 6.3 cannot build x86-64 for Android. **Nothing outside the Editor has ever run.** [M0-20a](tasks/M0-20a-apk-runs-on-bluestacks.md) owns it; a phone removes the question. Unity Device Simulator still serves for layout. |
| **Deferred — device-only** | **Now [M3 ledger row 4](ROADMAP.md#carry-forward-into-m3), because the debt spans three milestones and needs an owner rather than a list.** Multi-touch; landscape flip; touch latency; real frame rate; thermal; sustained fps at the concurrency cap; **the Profiler's no-per-frame-`GC.Alloc` check, carried unmet since M1-21**; whether the M0/M1/M2 feel verdicts survive leaving the Editor. **M1-20's haptics are the largest single block** — above all whether the 100 ms coalescing window reads as *late*. **M2's pacing knobs:** whether 2 s of arrival and 1.5 s of clear read as beats or as waiting; whether 0.3 s of fade covers a room swap; whether a pillar *reads* as cover at a phone's size (a Spitter holding fire looks identical to one that has not noticed you, and that difference is the whole mechanic); whether eleven overlapping additive rings read as separate countdowns or one smear. **M2-15a adds a cost rather than a feel question:** what `Physics.SyncTransforms()` costs per frame at the concurrency cap, which is a flush of the frame's dirty transforms and has only ever run on a desktop. If it turns out to matter, the narrower fix is to flush only when a cone or sweep is actually pending. **And the one row that is a *correctness* question rather than a feel one:** kill the app from recents mid-stage, relaunch, tap `Continue`. **Stopping Play is not a kill** — it disposes the scopes in order and lets every `finally` run — so M2-13b's atomic write has never been tested against the thing it exists for. |
| **Known issues** | **The physics-sync issue is fixed and the `FrameOrderTests` intermittency is gone** — [M2-15a](tasks/M2-15a-physics-sync.md), M3 ledger row 3 struck. It was a real bug for four tasks and read as a flaky fixture the whole time; the tell, visible in the notes from the start, was that the *ordering* assertions never failed — only the one checking that physics agreed with them. **What is still open is its price:** the flush has never been measured on a phone, and joins the device rows below. **First open item, smaller and the owner's:** whether GD §16.4's palette earns a file of its own — [M3 ledger row 6](ROADMAP.md#carry-forward-into-m3). **One doc contradiction, open on purpose.** The owner's retune puts the Oathbound at 3 m/s while **GD §6.1's *"5.4–6.2 m/s"* band and Characters.md §3's `140 / 5.4` row still carry the old numbers**, so the starter class sits below its own stated floor. Moving the band is a statement about the Gravecaller (5.6) and the Emberwright (6.2), neither of which is built; [parking lot](ROADMAP.md#parking-lot) holds it and **M5-02** is the first task that cannot avoid it. **One environmental failure to know about and not to chase:** `BootSmokeTests` can fail on *"Unhandled log message: `[Log] Error reason is 'NoSubscription'`"* — Unity's AI Assistant package logging into a PlayMode test, and only when the Editor is signed out. The APK crash has its own task. Disk sits near full — an IL2CPP Android build wants several GB of headroom. **A `ProjectSettings/` change is now refused by the pre-commit hook unless `ALLOW_PROJECT_SETTINGS=1` is set, and an unstaged one is warned about on every commit** — Unity backfills defaults behind you and holds the rewritten copy in memory until restarted ([Traps.md §5](../Traps.md)), and M2-15 found that the "check the diff before committing" habit only ever reached a session following a spec (ledger row 7, closed at M3-00a). The override is for a change made on purpose, like M2-11a's `Cover` layer. |
| **Where the rules live** | The watch list is gone, split by kind: **[Traps.md](../Traps.md)** for things in the toolchain that lie to you (the unfocused Editor, the MCP's refusals, VContainer, test measurement, the "an API that echoes your value back has not agreed to honour it" family), and **[Architecture.md §18](../Architecture.md#18-invariants)** for rules the code depends on — orderings, boundary rules, why a field is `internal`, and the soft spots no task owns yet. **Read Traps.md before debugging a probe; read §18 before changing an ordering.** A future obligation with an owning task is a [ledger](ROADMAP.md#carry-forward-into-m3) row; without one, a parking-lot line. **M2's ledger is closed — all fourteen rows** — and [M3's holds nine](ROADMAP.md#carry-forward-into-m3): eight opened at M2-15, of which two are already closed (3 by M2-15a, 7 by M3-00a), plus **row 9, opened by M3-00b** — a node's name and description are `LocKey`s and nothing in the build resolves one, so GD §13.1's two-second readability rule cannot be judged at M3-15 against `skill.oathbound.consecrate`. **M3-00c opened none and moved five**, which is what a screen group does to a ledger rather than a failure to close anything: **row 1** gained the table that closes it on paper, **row 2** gained two more bumps (M3-07b's v3 and M3-09c's **first ever profile migration**), **row 5** gained an owner and the rule that its tests pin behaviour rather than change it, and **rows 6 and 9 both roughly doubled** — eight colour readers, five text readers, one of which localisation cannot fix. Numbers are never reused across a milestone's table, so an old entry citing "row 5" still points at the right thing. |

---

## How to write an entry

Append one entry per merged task, newest at the bottom. **Twelve lines or fewer.** The spec's *As built* footer owns the full account of what deviated and why; this entry is the index — what happened, and where each lesson was filed. (The archived logs average 7 KB an entry against a template that said "one line"; the cap is what keeps this file readable.)

```markdown
### YYYY-MM-DD · M0-03 · Domain events
**Built:** one or two lines — what exists now that didn't.
**Deviations:** none | *n*, in the spec's As built. Name here only one that changes a decision.
**Learned:** one line each, ending with where it was filed — Traps §n · AR §18.x · ledger row n · spent (stays here).
**Follow-ups:** tasks created, ledger rows added, or "none".
```

The PR number lives in the merge commit — do not write it here. Current State is written **as of the PR's merge**: the task the PR closes is *Last merged*. If a deviation changes a decision in `Architecture.md`, it needs a superseding ADR — say so here and link it.

**A durable lesson does not go in Current State.** A toolchain trap goes in [Traps.md](../Traps.md); a rule the code now depends on goes in [Architecture.md §18](../Architecture.md#18-invariants); a thing a named task must handle goes in the [carry-forward ledger](ROADMAP.md#carry-forward-into-m3), and one with no owner yet in the ROADMAP parking lot, one line. The entry says *what happened*, and links.

**At the end of a milestone**, move its entries to `archive/PROGRESS-M<n>.md`, promote anything durable out of them first, and leave this Log holding only the milestone in progress. **M2 is the first milestone this was done for on time** — M0's and M1's were archived a milestone late and at 381 KB between them, which is the failure the rule was written against.

---

## Log

_M0, M1 and M2 are archived: [M0](archive/PROGRESS-M0.md) (20 entries) · [M1](archive/PROGRESS-M1.md) (21 entries) · [M2](archive/PROGRESS-M2.md) (27 entries)._

### 2026-09-13 · M2-15a · `Physics.SyncTransforms()`, at the seam

**Built:** one line in `RunTicker.Tick`, between the bodies step and the fact phase. `Physics.autoSyncTransforms` is 0 project-wide, so moving a transform did not move the collider physics holds — AR §18.1's frame order was honoured by the *call order* and quietly not by the code, and a cone could miss an enemy that stepped into it that frame. Landed **after the `m2` tag**, by M2-15's own rule that a bug found at acceptance becomes `M2-15a` with its own PR.

**Deviations:** none. One decision the ruling did not contain — *which* seam, since the two cosmetic steps sit in the middle. It went below them: a projectile view and a telegraph ring touch no collider and ask physics nothing, and flushing above them would have grouped them with the physical half of the frame they are explicitly not part of.

**Learned:** **an intermittent test failure was a real bug the whole time, and the intermittency is why it took four tasks.** It read as a flaky fixture at M2-12a, M2-13b, M2-14a and M2-15 — different counts every run, always the same two `FrameOrderTests` rows, always the `ConeReport` assertion and never an ordering one. That last detail was the tell, and it was in the notes from the start: the *ordering* assertions never failed, only the one that checked physics agreed with it. Filed as its own [AR §18.1](../Architecture.md#181-ordering) row. **Proof of an intermittent fix is repetition, not a green run** — three consecutive 11/11 against 9/11 immediately before. Spent (stays here).

**Follow-ups:** **[M3 ledger row 3](ROADMAP.md#carry-forward-into-m3) struck** — the owner ruled it rather than carrying it into M3's specs, so M3-00a inherits a settled ordering. **The flush's cost is unmeasured** and joins the device rows. Next is **M3-00a**.

### 2026-09-13 · M3-00a · Specs for M3-01…M3-05, the tree as data

**Built:** seven specs where the ROADMAP had five titles — [M3-01a](tasks/M3-01a-xp-and-levels.md) XP, levels and the kill that pays; [M3-01b](tasks/M3-01b-save-format-v2.md) save format v2, one bump for the whole tree; [M3-05](tasks/M3-05-effect-registry.md) the effect registry and `ModifyStat`; [M3-02a](tasks/M3-02a-skill-specs.md) `SkillSpec`, `TriggerSpec`, `SkillTreeSpec`; [M3-02b](tasks/M3-02b-skill-authoring.md) their authoring and the boot lists; [M3-03](tasks/M3-03-tree-rules.md) `TreeRules` and `SkillTree`; [M3-04](tasks/M3-04-offer-generator.md) the offer. Build order 01a → 01b → 05 → 02a → 02b → 03 → 04, because a node carries an `IEffect` before it can be a spec. **Ledger row 7 closed with a hook, not a habit:** `.githooks/pre-commit` refuses a staged `ProjectSettings/` change without `ALLOW_PROJECT_SETTINGS=1` and warns about an unstaged one.
**Two splits, both before starting:** M3-01 at the format seam (the bump is its own review — ledger row 2's point), M3-02 at the core/authoring seam (eleven files together). **Eleven files in this PR** — seven specs, two plan files, the hook and its README — past the five-file rule, docs and shell only, M2-00c's shape and called out the same way.
**Deviations:** none from the brief. Two questions went to the owner and both came back as recommended: **the tree is layered** — two nodes a tier × four tiers + a keystone, nine a branch, twenty-seven a class (M3-02a rule 7) — and **CH §5.2 ships as authored with the depth divergence flagged** (M3-01a rule 9).
**Learned:** **doing the arithmetic on the design doc found two contradictions** — CH §5's "8 a branch, 27 a class" (3 × 8 is 24), whose gating read with one node a tier makes the level-up offer the same three heads every time; and CH §5.2's curve, which against GD §12.1's budget fills the tree by stage 20, not 30 → both parking lot; the first was the shape decision the whole group hangs on. **XP = 3 × threat cost makes a stage's XP a function of its budget alone**, whatever the seed drew — a property worth more than the number → M3-01a rule 2. **A field's writer and reader may be two tasks apart if the bump is one**, and the trade has to be written where the field is → M3-01b rule 1. **A `[SerializeReference]` list has no type picker in Unity 6's Inspector**, so effect authoring is a ScriptableObject per primitive → M3-02b rule 1 (and Traps §5 if it ever bites someone).
**Follow-ups:** ledger row 7 struck; row 2 has its four fields and its two owners (01b, 03); row 1 has the number M3-12 authors against. Four parking-lot lines. **AR §5's `Progression` row is corrected by M3-01a**, not here — `XpCurve` is content. Next is **M3-00b**.

### 2026-09-13 · M3-00b · Specs for M3-06…M3-08, casting and choosing

**Built:** five specs where the ROADMAP had three titles — [M3-06](tasks/M3-06-skill-runner.md) the `SkillRunner`, CH §4.1's 40 % floor as one function two things obey, and auto-cast over the blackboard; [M3-07a](tasks/M3-07a-auto-manual-and-slots.md) the toggle, four slots that are positions rather than a list, and the two `IPlayerCommands` members; [M3-07b](tasks/M3-07b-save-format-v3.md) save v3; [M3-08a](tasks/M3-08a-level-up-flow-and-pause.md) `IProgressionCommands`, the lazy draw, Overflow and `RunPause`; [M3-08b](tasks/M3-08b-level-up-screen.md) three cards and a tap. Build order is ID order, after M3-04.
**Two splits, both before starting, both at a seam an earlier task already argued:** M3-07 at the **format** seam, because the owner ruled Auto/Manual survives a kill and M3-01b rule 1 said that makes it a v3 with its own review; M3-08 at the **core/screen** seam. **Seven files in this PR** — five specs and two plan files — past the five-file rule, docs only, M3-00a's shape and called out the same way.
**Deviations:** none from the brief. Four questions went to the owner; three came back as recommended (**toggles are saved**; **pause gates the tick**, GD §11.4's *"idle the simulation"* read literally; **one screen that re-draws** for a double level-up) and the fourth — where a manual cast lives — **was already answered in a doc**: AR §6 has carried `CastSkill(slot)` and `SetAutoCast` on `IPlayerCommands` since M0-09, so it was never open. The residual half, CC §6.2's full-slots prompt, is ruled: **core throws and the UI asks**, `SpendLevelUp`'s precedent.
**Learned:** **reading the code the spec claims to know moved a rule.** `IncomingProjectiles` is written by `ProjectileSystem.Tick` and nowhere else — *after* combat — so the runner has to tick **above** the projectile step or CC §6.4's Bulwark becomes a shield raised over a wound that already landed; the staleness is the mechanic → M3-06 rule 7, an AR §18.1 row when it is built. **The laziness rule has a second, sharper reason than M3-04 gave:** the boundary capture happens near the *end* of the tick, so drawing anywhere inside it would record a position the draw had advanced and killing the app would be a free reroll → M3-08a rule 1. **Overflow is derivable and needs no field** — `Level − 1 − TakenNodeCount − PendingLevelUps` — which is the exact mirror of M3-03 rule 6's "pending cannot be derived", and it keeps v3 to one field → M3-08a rule 9. **A gated pause splits GD §7.3's stage-length number in two**, since a paused frame contributes no `Dt` → ledger row 8 rewritten. Spent (stays here).
**Follow-ups:** **ledger row 9 opened** — node text is an unresolved `LocKey` and M3-15 cannot judge GD §13.1's two-second rule against a key; **row 6 gained a fourth reader** (M3-08b's four kind tints) and **row 8 gained the play-time / stopwatch distinction**. **M3-09 grew** by the View Tree toggle, the full-slots prompt, drag-to-reorder and the first-active hint; **M3-13**'s palette scope is now known before it is worked. The M3 task count was 22 here and 21 in the ROADMAP; the tables are the authority and now hold **23**. Next is **M3-00c**.

### 2026-09-13 · M3-00c · Specs for M3-09…M3-12, the screens and the first nodes

**Built:** twelve specs where the ROADMAP had four titles — [M3-09a](tasks/M3-09a-pause-screen.md) the pause gate and `PauseReason.Menu`'s first caller; [M3-09b](tasks/M3-09b-skills-screen.md) the Skills list, `TriggerText` and CC §6.2's full-slots prompt; [M3-09c](tasks/M3-09c-profile-v2-and-first-active-hint.md) profile v2 and the one-time hint; [M3-09d](tasks/M3-09d-tree-view.md) the tree view, opened from two doors; [M3-10a](tasks/M3-10a-skill-slot-buttons.md) S1–S4 and the poll that makes a tap a command; [M3-10b](tasks/M3-10b-progression-hud.md) the XP strip, the level, the auto-cast row and the Overflow toast; [M3-11a](tasks/M3-11a-bulwark-and-timed-effects.md) Bulwark and `TimedEffects`; [M3-11b](tasks/M3-11b-consecrate-and-zones.md) Consecrate and `ZoneSystem`; [M3-11c](tasks/M3-11c-skill-views.md) both views and **ledger row 5**; [M3-12a](tasks/M3-12a-addressable-stats.md) five addressable stats; [M3-12b](tasks/M3-12b-cooldown-and-knockback-primitives.md) two primitives; [M3-12c](tasks/M3-12c-oathbound-tree-v1.md) the twelve nodes and the TTK test. Build order is ID order, after M3-08b.
**Four splits, all before starting.** M3-09 four ways (the hint's split is ledger row 2's rule: per-install state means a profile bump means its own review); M3-10 at the corner — buttons that take input, readouts that do not; M3-11 core / core / view, M3-08's seam; M3-12 three ways rather than the ruling's two, because five addressable stats, two primitives and twelve assets are three reviews and the first two are prerequisites. **Fourteen files in this PR** — twelve specs and two plan files — past the five-file rule, docs only, M3-00a's and M3-00b's shape and called out the same way.
**Deviations:** none from the brief. Five questions went to the owner and all five came back as recommended: **Bulwark is timed shield points** (a barrier would have made `ProjectileSystem` test every bolt against a world object); **the tree view ships**; **a trigger line is a `LocKey` per (field, comparison) pair plus a formatted threshold**; **six stat lines, four rule changes and the two Actives**; and **the hint is worth a profile bump**. One ruling M3-00c made itself and said so: **drag-to-reorder is not in V1**, on M3-07a's own terms, and M3-07b rule 1's slot *positions* mean adding it later costs a port member and no bump.
**Learned:** **reading the code moved five specs, and one of them will not compile as written.** `LocKey`'s member is `.Key`; M3-08b rule 7 says `.Value` → resolved in that task's *As built*. **Three screens each needed a narrow read that did not exist** — seconds (M3-09b), availability (M3-09d), and by-slot fills (M3-10a) — and in every case the alternative was the presentation layer re-deriving something core already owns: `CooldownRules`' floor, CH §5's gating, `TryIndexOf`. That is AR §18.2's *reads, never the handle* generating work rather than preventing it, and the work is the right price. **A one-field DTO teaches a bad habit:** `HapticsSettings` authors the whole `PlayerProfile` from the one field it knows, which is correct until there are two and then silently erases the other → M3-09c rule 3, a `ProfileStore`, `With` helpers, and the test row that would have caught it. **The code had already promised this milestone things** — `Health.Heal`'s remarks name Consecrate and guarantee it will not reset the Aegis delay, and `SkillButton`'s tooltip already says *"the four skill slots of M3-10 are 60"* — so two rules were transcribed rather than decided. **A small tree makes Overflow large:** twelve nodes fill at level 13, leaving **29** Overflow levels by stage 30 against M3-08a rule 8's fourteen, which is what actually closes ledger row 1 → M3-12c rule 8, and a warning that M7-04's twenty-seven will shift it back. Spent (stays here).
**Follow-ups:** **no ledger row opened and five moved** — row 1 has a table (3 / 4 / 5 hits), row 2 has two more bumps including the first profile migration the harness has ever run, row 5 has an owner and a don't-bless-a-regression rule, rows 6 and 9 roughly doubled to eight colour readers and five text readers. **Three parking-lot lines:** drag-to-reorder, node icons, and the CH §5 tree-table correction, which **M3-12c will not promote after all** — v1 is three branches of four with no keystone, so it never authors the nine-node shape. The M3 count is **31**, and the Milestones row now agrees with the tables. Next is **M3-00d**, and then the code.

_Next M3 entry below._

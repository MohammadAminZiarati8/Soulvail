# Soulvail — Progress

**The log.** Where we are right now, and what each merged task actually did. Updated **in the same PR** as the implementation — never in a separate "update docs" commit.

The map is [ROADMAP.md](ROADMAP.md). The specs are in [tasks/](tasks/).

**Closed milestones are archived, not deleted:** [M0](archive/PROGRESS-M0.md) · [M1](archive/PROGRESS-M1.md) · [M2](archive/PROGRESS-M2.md) · [M3](archive/PROGRESS-M3.md) · [M4](archive/PROGRESS-M4.md). The log below covers the milestone in progress only. This file used to be 381 KB and could not be opened whole by any tool; keeping it to one milestone is what stops that happening again — **M2 was the first milestone archived on time, M3 the second and M4 the third, which is what turns a rule into a habit.**

**Current State is a brief, not a second log, and it is capped.** After M3-15 this file was 228 KB with an *empty* Log; by M4-05b it was 298 KB, and 256 KB of that was Current State — thirty-four *Verified* rows back to M3-00b, an eight-deep task chain, and four rows that were appended to for a whole milestone and never rewritten, one of them still opening with a sentence about M3-06. On 2026-09-20 the block was cut to under 10 KB: M3's *Verified* rows went to the [M3 archive](archive/PROGRESS-M3.md#verification-chain), M4's were folded into their Log entries as a **Verified** line, and every other row was rewritten as the present with links to the history. The rules that keep it there are under [How to write an entry](#how-to-write-an-entry) — two on 2026-09-20, and a third at M4-07, which bounds the three rows that only ever grew.

---

## Current State

_Updated: 2026-09-20, as of M5-01's merge._

| | |
|---|---|
| **Milestone** | **M5 — Second class: 3 / 15, and the first code is in.** M0–M3 complete and tagged `m0`–`m3`; **`m4` is the owner's to tag**, on Editor evidence plus one playtest, device rows deferred and multi-touch named apart. **The feel verdict from M4: the Warden is a wall — it killed the player and was not killed.** M5 is specced whole; twelve build tasks remain, in ID order. |
| **Last merged task** | **[M5-01](ROADMAP.md#m5--second-class) — a projectile weapon and CC §3.7's lead.** Machinery, no content: `ProjectileLead`, a `ShotSide` on every shot, `ProjectileSystem.Land` split in two, three defaulted shot numbers on `WeaponSpec`, and a damage frame that offers a `Projectile` instead of a `ConeHitIntent`. **Five deviations, two of which change a decision** — `ProjectileImpacted.HitPlayer` is now `Hit`, and `IncomingProjectiles` counts inbound shots only so a player's own bolts cannot auto-cast Bulwark. [AR §18.1](../Architecture.md#181-ordering) gained the *take the shot* step and a row for it. No ledger row added or discharged. |
| **Next task** | **[M5-02](tasks/M5-02-gravecaller-and-bone-bolt.md) — the Gravecaller, and the numbers a second class forces us to settle.** Size **M**, dependencies met. It authors the first weapon that walks through M5-01's machinery — Bone Bolt at 9 damage, 4.0/s, 40 m/s, 0.8 m — and settles the class speed band GD §6.1 and CH §3 disagree about. **A defect is already known in this spec and was reported rather than edited at M5-00a:** rule 10 ships `character.gravecaller.name` unresolved, and `ContentValidationTests.EveryLocKey_ResolvesInEnglish` sweeps every `CharacterDefinition`, so an English row has to ship with the asset. **Nothing about a second class is reachable in play until [M5-07](tasks/M5-07-class-select-screen.md)**, which is stated in six specs so an unverifiable intermediate build is never reported as broken. |
| **Verified** | **2 228 EditMode / 0 / 0 and PlayMode 16 / 0 / 0**, both through `TestRunnerApi` with the Editor focused; the chain of counts is each Log entry's **Verified** line, M4's now in the [M4 archive](archive/PROGRESS-M4.md). Console swept clean, zero new analyzer warnings, `dotnet format whitespace --verify-no-changes` green over every touched file. **The one thing a suite could not have told us was asked directly:** the Oathbound asset was converted in the Editor after `WeaponSpec` gained three fields, and comes out unmoved. `ProjectSettings/TimeManager.asset` re-serialised itself again and was reverted before handover ([Traps §5](../Traps.md)). |
| **What works** | **A run is a descent that reaches a boss, pays for dying, and survives the app closing.** Boot → Menu → `Descend` or `Continue` → arenas sealed by threat-budgeted, telegraphed waves, a door when they are dead, one stage deeper each time. Three archetypes on one shared body; two arenas drawn by seed and stage. **Levelling:** a kill is experience, a threshold is a level and a pick, a pick stops the run and offers three of the Oathbound's twelve nodes; Consecrate and Bulwark cast on Auto or from four thumb slots, with the screens around them. **The boss:** every fifth stage, authored on `Descent.asset`, is the Warden of Ash — 4 200 HP, a shockwave whose front edge bites, cracks that arm and do not track, three then four Husks across a 1.5 s invulnerable beat at 66 % and 33 % — under a segmented bar whose seams sit at the asset's own thresholds. **Death pays** `10·(deepest stage) + 50·(bosses killed)` as one `ShardsAwarded` on the death tick, banked in `PlayerProfile` v3 and drawn on a full-screen run-end screen with one button out. **Saves:** `run.json` v3 at the opening frame and every boundary, deleted on death; `profile.json` v3, one writer. Every colour is `Palette`, every string a `LocKey`. The milestone-by-milestone account is in the [archives](archive/). |
| **What does not work yet** | **The Warden has been fought once and did not die.** M4-07's playtest reached stage 5, was killed by the boss, and the owner's call was *"it's good for now"* — so the wall exists and is taller than the player. **Whether *not dying* is 4 200 HP outrunning real damage or a health bar that never moves is undistinguished** ([row 8](ROADMAP.md#carry-forward-into-m5)), and the session produced none of [row 2](ROADMAP.md#carry-forward-into-m5)'s three numbers. **One element of the UI is measurably too small** — S1–S4's labels at **7.9 dp or less**, 66 % of Android's floor and unbounded below because auto-sizing can shrink them to 8 pt ([row 1](ROADMAP.md#carry-forward-into-m5), corrected at M5-00a and placed on [M5-05b](tasks/M5-05b-decoy-view-and-the-look.md) at M5-00b); the rest of the two screens clears it. The **redesign** the owner asked for — every taken node shown, an Auto skill marked by something orbiting it — still waits on M7's icons. **Nobody can see the lifetime Shard total or spend it**: the sum is on disk and nothing reads it, deliberately, until the hub that orders a run-end screen against a writer exists (M5-07 or M6-02); spending is M6-09. GD §14.1's third term is absent by ruling and the doc now says so. **The arena only reacts** — hazard damage has no core system, so the brazier costs the player nothing. **A boss cannot run a `SkillRunner`**: seven `TriggerField` members mean nothing to an enemy, and M7's Archon brings the answer. **A boss stage completes when the boss is down whatever its adds are doing** ([row 7](ROADMAP.md#carry-forward-into-m5)). **Not saved, by ruling:** cooldowns, granted shields, live zones. **Unbuilt by design:** no audio, no Sanctum, no Elite in any roster, one class, one boss, two arenas. |
| **Known issues** | *(1)* **`FrameOrderTests.Ticker_RunsTheStepsInOrder` fails about one run in ten** — 4 / 20 alone and 1 / 20 behind `BootSmokeTests` at M4-07, both ordinary against M3-08a's 10 %; always the physics half, and nothing in the game is known to be wrong. The instrumentation that turns a failure into *late sync* or *wrong wedge* is on **[M5-05a](tasks/M5-05a-wight-views-and-concurrency.md)**, [ledger row 4](ROADMAP.md#carry-forward-into-m5). *(2)* **Every "ring" is a square**: `VFX_TelegraphRing.prefab` is an untextured Quad; one circle texture fixes all five decals; art, M7. *(3)* **`ArenaView.OnValidate` warns "0 spawn point(s)" for both arena prefabs on every domain reload**; they author 8 and 7, and reload-time `Transform` references are fake-null ([Traps §1](../Traps.md)). Found at M4-01a, nobody's. *(4)* **`PlayerStat` lies by name** — it carries `ContactDamage`, which `PlayerStats.Resolve` refuses; renaming it `StatId` moves nine assets' serialized ordinals; the owner's ruling, owed since M4-01a. *(5)* **M3-14c's nine labels were never witnessed in Play.** *(6)* **Environmental:** a PlayMode row can fail on *"`Error reason is 'NoSubscription'`"* (Unity's AI Assistant, Editor signed out) — seen three times in M4-07's 40 runs, on `Frame_PausedTicksCostNoSimulatedTime`; `ProjectSettings/TimeManager.asset` re-serialises whenever the Editor touches the project, so check and revert before every handover ([Traps §5](../Traps.md)); the disk sits near full. |
| **Reference device** | **None, and the emulator is not a fallback.** BlueStacks 5 **installs the APK and crashes on launch** in its own Vulkan driver, not game code. **Nothing outside the Editor has ever run.** [M0-20a](tasks/M0-20a-apk-runs-on-bluestacks.md) owns it; a phone removes the question. **And the Editor is not a stand-in for layout either:** `Screen.dpi` reads **120** here against a phone's 400, so every dp-sized element ever looked at was drawn at **30 %** of its device size ([Traps §9](../Traps.md)). |
| **Deferred — device-only** | **All of it is [ledger row 3](ROADMAP.md#carry-forward-into-m5)** — eight questions added in M4 alone, five tags, nothing ever run outside the Editor. Two are named apart. **Multi-touch risks a *feature*, not a verdict**: if Android does not deliver a thumb on the stick and a thumb on S3 at once, manual casting does not work at all. **Kill-from-recents is the one correctness row** — mid-stage, or straight after a death now that a Shard is banked — because stopping Play is not a kill and M2-13b's atomic write has never met the thing it exists for. The full list is on the ledger row rather than here. |
| **Where the rules live** | **[Traps.md](../Traps.md)** for things in the toolchain that lie to you — read it before debugging a probe. **[Architecture.md §18](../Architecture.md#18-invariants)** for rules the code depends on — read it before changing an ordering. **[ADRs](../adr/)** for why. An obligation with an owning task is a **[ledger](ROADMAP.md#carry-forward-into-m5)** row and leaves only when its owner's *As built* says so; one with no owner is a **[parking-lot](ROADMAP.md#parking-lot)** line. **M5's ledger: eight rows, none discharged, all eight on an unmerged owner** — 1 and 3 on M5-05b, 4 on M5-05a, 5 split across M5-02 / M5-06b / M6-10, 6 on M5-04a, and 2, 7 and 8 on M5-08's playtest. M5-00a ruled six, M5-00b re-placed them onto the splits and re-owned row 8; **a ruling is not a discharge**.<br>**Every carried row names an unmerged owner**, which is M4-07 rule 11, written after M4-00a merged owning three rows and discharging none. |
---

## How to write an entry

Append one entry per merged task, newest at the bottom. **Twelve lines and 3 000 bytes or fewer, measured** — a line here is a paragraph, so the line cap alone bounded nothing: M4's entries ran 3.3 to 7.4 KB under it, and they stay as they are, archiving with the milestone; the byte cap applies from M4-06. The spec's *As built* footer owns the full account of what deviated and why, under its own 6 000-byte cap in [the template](tasks/_TEMPLATE.md); this entry is the index — what happened, how it was checked, and where each lesson was filed. **No superlatives**, here or in the *As built*: *"the first task in the project to…"* is history, not information — say what exists and link. The newest entry's size, in one line:

```sh
awk '/^### 20/{e=""} {e=e $0 "\n"} END{printf "%s", e}' Docs/plan/PROGRESS.md | wc -c
```

```markdown
### YYYY-MM-DD · M0-03 · Domain events
**Built:** one or two lines — what exists now that didn't.
**Verified:** one line — the suite count and its delta against the previous entry, the runs, the Console sweep, the format check. The chain of counts lives here, not in Current State.
**Deviations:** none | *n*, in the spec's As built. Name here only one that changes a decision.
**Learned:** one line each, ending with where it was filed — Traps §n · AR §18.x · ledger row n · spent (stays here).
**Follow-ups:** tasks created, ledger rows added, or "none".
```

The PR number lives in the merge commit — do not write it here. Current State is written **as of the PR's merge**: the task the PR closes is *Last merged*. If a deviation changes a decision in `Architecture.md`, it needs a superseding ADR — say so here and link it.

**A durable lesson does not go in Current State.** A toolchain trap goes in [Traps.md](../Traps.md); a rule the code now depends on goes in [Architecture.md §18](../Architecture.md#18-invariants); a thing a named task must handle goes in the [carry-forward ledger](ROADMAP.md#carry-forward-into-m5), and one with no owner yet in the ROADMAP parking lot, one line. The entry says *what happened*, and links.

**Current State is overwritten, not appended.** Every row states the present and links to where the history is — the Log entry, the spec's *As built*, an archive, a ledger row. A row never says *"and as of M3-06…"*; that sentence is the Log's. *Last merged* names the task of the newest Log entry and no older one — there is no task chain — and the *Verified* row carries only the latest count, because the chain is the **Verified** line of each entry. A handover that makes a row longer has usually put history in it: move the history, keep the sentence that is true today.

**Current State is capped at 10 000 bytes, measured rather than estimated** — the twelve-line cap's device, one section over. It reached 256 KB at M4-05b before this rule existed, and was 9 567 bytes when the rule was written. One line settles it before a handover:

```sh
awk '/^## Current State/,/^## How to write an entry/' Docs/plan/PROGRESS.md | wc -c
```

Over the cap, rewrite the row that grew; do not move the cap.

**The three rows that only ever grew are bounded too, and this is the rule that bounds them** — [M4-07](tasks/M4-07-acceptance-and-tag.md) rule 5, the rolling treatment the task chain already had. The block cap alone was not enough: it says *something* must shrink and never which, so the row rewritten is whichever is easiest rather than whichever grew.

- **What works** is the **present tense, one clause a subsystem** — what a player can do today, never how it got there. When a milestone closes, its sentence is compressed to a clause and the detail stays in its archive, which is where it already is.
- **What does not work yet** and **Known issues** are **capped at nine numbered items each**. A tenth is not appended: the weakest is promoted out first. **An item leaves the moment it acquires an owning task** — it becomes a [ledger](ROADMAP.md#carry-forward-into-m5) row — **or the moment it is ruled to have none** — it becomes a [parking-lot](ROADMAP.md#parking-lot) line.
- **An item that repeats a ledger or parking-lot entry states its *symptom* in one clause and links.** Duplication is what made these rows grow: at M4-06 three of nine *Known issues* were second copies of rows that already had a home, each written out in full, and each re-edited whenever its real home moved. M4-07 took six of the nine out on that rule and the row halved.

**At the end of a milestone**, move its entries to `archive/PROGRESS-M<n>.md`, promote anything durable out of them first, and leave this Log holding only the milestone in progress. **M2 is the first milestone this was done for on time** — M0's and M1's were archived a milestone late and at 381 KB between them, which is the failure the rule was written against. M3 was the second and M4 the third.

---

## Log

_M0, M1, M2, M3 and M4 are archived: [M0](archive/PROGRESS-M0.md) (20 entries) · [M1](archive/PROGRESS-M1.md) (21) · [M2](archive/PROGRESS-M2.md) (27) · [M3](archive/PROGRESS-M3.md) (35, plus M2-15a, which is M2's task merged after that archive closed) · [M4](archive/PROGRESS-M4.md) (11)._

### 2026-09-20 · M5-00a · Specs for M5-01…M5-04, the Gravecaller

**Built:** five specs where the ROADMAP had four titles — [M5-01](tasks/M5-01-projectile-weapon-and-leading.md) the shot and CC §3.7's lead · [M5-02](tasks/M5-02-gravecaller-and-bone-bolt.md) the class and Bone Bolt · [M5-03](tasks/M5-03-shroudstep-and-corpse-decoy.md) the blink and the corpse · [M5-04a](tasks/M5-04a-minion-agents-and-registry.md) a friendly body · [M5-04b](tasks/M5-04b-rise-and-minion-stats.md) Rise and a third `IStatBlock`. Build order is ID order; M5 moves 11 → 13. M5-00b specs M5-05…M5-08.

**Verified:** docs only — no C#, no compile, no suite; the baseline stays M4-07's **2 197 EditMode / 0 / 0** and **PlayMode 16 / 0 / 0**. Every type, member and asset field each rule names was grepped first, and four rulings changed because of it. **A branch-point defect was found:** `dev` is at the M4-06 merge and carries no M5 ledger, so this branch is cut from `m4-07-acceptance` and must merge after it.

**Deviations:** *1*. **M5-04 split at the body/passive seam before starting** — a friendly body is AR §3 and §9, Rise is CH §3.2 and ADR-0011, and the one title counted six files before the tests. M5-01 was weighed for the same split and kept whole at four.

**Learned:** **`ProjectileSystem` is one-directional** — `Land` only ever tests the player — so a player's shot costs a `ShotSide`, not an asset → M5-01 rule 4. · **CH §3.2's Bone Bolt breaks GD §6.2's primary balance invariant**: 7 against a 36 HP Husk is six hits at stage 1, and Rise cannot rescue it because a Wight needs a kill first → M5-02 rule 2 authors **9**. · A decoy makes an enemy choose a target a milestone before `EnemyBlackboard` expects one → M5-03 rule 2; the rename is refused at ~40 sites → parking lot. · **`IRandom.Drops` is drawn from by nothing**, so Rise costs no sixth stream and no **v4** → M5-04b rule 1. · A Wight needs **no new `PlayerStat` member** → M5-04b rule 8.

**Follow-ups:** **six ledger rows ruled and placed on unmerged owners, none discharged; two corrected against the assets.** [Row 1](ROADMAP.md#carry-forward-into-m5) is **twelve serialized values, not four** — auto-sizing floors at 8 pt, so 7.9 dp is an upper bound — ruled in at 28 pt → M5-05. Row 3 is **six prefabs, not three views**, so one keyword is a look change → M5-05. Row 4 → M5-05, the only M5 task that opens `FrameOrderTests`. Row 5 splits: (ii) → M5-02, (i) → M5-06 with `ModeDefinition` ruled as its asset. Row 6 → M5-04a, across a phase threshold. **Row 7 ruled: change nothing** — both candidate fixes move something worse — so M5-08 observes. Row 8 re-owned to M5-00b. **Parking lot:** M5-07a's spec is M5-00b's, not M5-00a's as it has said since M3-03; a line opens for the blackboard rename; the speed band's promoter fired (**3.0 / 3.1 / 3.4 m/s**). **M5-00b inherits a concentration it must split.**

### 2026-09-20 · M5-00b · Specs for M5-05…M5-08: the views, the tree, class select, acceptance

**Built:** eight specs where the ROADMAP had five titles, all linked from [the table](ROADMAP.md#m5--second-class). **Three splits, each ruled before starting against a counted Files table:** M5-05 → **05a** the frame / **05b** what can be seen; M5-06 → **06a** what a node may address / **06b** the twelve nodes, M3-12a's seam one milestone on; M5-07a → **07a-i** the index space / **07a-ii** the moment, spelled `-i`/`-ii` on M3-11a's precedent because `M5-07b` would read as a half of M5-07. ID order; M5 moves 13 → **15**.

**Verified:** docs only — the baseline stays M4-07's **2 197 EditMode / 0 / 0** and **PlayMode 16 / 0 / 0**. Every type, member and asset field each rule names was grepped first, and five rulings changed because of it; every `tasks/` link resolves.

**Deviations:** *1*. **Three splits where the brief predicted one** — M5-05 was named in advance, M5-06 and M5-07a were found by counting, at six and seven files. The ROADMAP's count was also **one over its own table**, 13 against twelve rows, and is now 15.

**Learned:** **`OfferGenerator.Draw` indexes `drawnPerBranch[branch]` off `TryLocate`, which answers −1 for a foreign node** — CH §5.4's borrowed branch is an `IndexOutOfRangeException` on the first offer that draws one → M5-07a-i. · **The splash choice is derivable from `TakenNodeIds`**, so **no `RunSnapshot` v4**, at the cost of one re-ask on a resume before the first borrowed pick → M5-07a-ii rule 6. · **`TriggerField` has no `MinionCount`**, so Exhume's trigger cannot be authored → M5-06a rule 6, with M3-14b's unbuilt **starve check** beside it. · **GD §16.4 makes a Wight the player's own cyan**, so scale is the only separator → M5-05a rule 8. · **`PlayerProfile` v3 has no unlock set**, so *"a second **unlocked** class"* reads as *authored* until M6-09 → M5-07 rule 6.

**Follow-ups:** **eight [ledger](ROADMAP.md#carry-forward-into-m5) rows re-placed onto the splits, none carried silently.** Rows 1 and 3 → **M5-05b**, the half visible in an Oathbound run the day it merges; 4 → **M5-05a**, which opens `FrameOrderTests` because the frame gained a step; 5(i) → **M5-06b**, ripple counted at one production site and seven test ones; 2 and 7 → **M5-08**'s checklist. **Row 8 is carried with a written reason rather than placed:** the played minute has not happened and a docs task cannot make it, so choosing between *a number* and *a bug* would invent the work — re-owned to **M5-08**, which plays. **One finding outside this task's Files table:** M5-02 rule 10 ships `character.gravecaller.name` unresolved and `EveryLocKey_ResolvesInEnglish` sweeps every `CharacterDefinition`, so that row would be red. **Parking lot:** the branch-index promoter fired and names M5-07a-i; M5-08 rule 11 takes the id-drift pass owed since M2-15.

### 2026-09-20 · M5-01 · A projectile weapon, and the lead that makes it hit

**Built:** `WeaponKind.Projectile` stopped being a comment. `ProjectileLead.Solve` is CC §3.7's two-iteration lead as a pure function of four numbers, XZ only, failing by returning the unled aim point rather than a NaN. `ProjectileSystem.Land` split on a new `ShotSide`: an `AtPlayer` shot is M2-07a's code unmoved, an `AtEnemies` shot walks `EnemyRegistry.Alive` and damages every living agent inside the radius — a bolt is a small blast. `WeaponSpec` gained three defaulted shot numbers with kind-conditional validation; `PlayerCombat`'s damage frame became two branches, one emitting a `ConeHitIntent` and one offering a `PendingShot`; `RunSession.Tick` takes that shot between the combat step and the skills block, so it flies on the tick it was decided and cannot land on it.

**Verified:** **2 228 EditMode / 0 / 0** (+31 on M5-00a's 2 197) and **PlayMode 16 / 0 / 0**, both through `TestRunnerApi`. Console swept clean, zero new analyzer warnings, `dotnet format whitespace --verify-no-changes` green over all thirteen touched files. The Oathbound asset was converted in the Editor after the change and comes out unmoved — `Cone`, 13, 3.0, 8 m, 60°, 0.4, three new fields at zero. `ProjectSettings/TimeManager.asset` re-serialised itself again and was reverted before handover (Traps §5).

**Deviations:** 5, in the spec's *As built*. Two change a decision, and one edits AR §18.1. **`ProjectileImpacted.HitPlayer` was renamed `Hit`** — rule 4 and the *Public API* both write `Hit`, and after this task the old name is a lie, since an `AtEnemies` bolt setting `HitPlayer` describes an arrival that never went near the player; eight reads moved with it. **`CombatBlackboard.IncomingProjectiles` now counts `AtPlayer` shots only**, which the spec does not state: it is CC §6.4's Bulwark trigger, and counted raw a Gravecaller's four bolts a second would auto-cast Bulwark off the player's own fire for a whole run.

**Learned:** the tick order gained a step, so [AR §18.1](../Architecture.md#181-ordering) gained it too — a task that adds an ordering the code depends on and leaves §18 describing the old one has filed nothing. A field named for one side of a fight is a defect waiting for the other side to exist — filed as the two renames above, and as the reason to look at every `*Player*` flag a widening touches. `Enum.IsDefined` boxes, so a frame-path guard over a two-member enum is two comparisons — spent (stays here). `Stat` clamps nothing and `Projectile`'s door throws, so a live stat reaching a throwing constructor needs the clamp `ConeAngle` already models — spent.

**Follow-ups:** none. No ledger row is added or discharged; M5's eight rows stand where M5-00b placed them.

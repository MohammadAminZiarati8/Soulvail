# Soulvail — Progress archive: M4, First boss and run end

**Closed at [M4-07](../tasks/M4-07-acceptance-and-tag.md), archived on time — the third milestone running,
which is what turns a rule into a habit.** M0's and M1's logs were archived a milestone late and at 381 KB
between them; M2's was the first done at its own acceptance, M3's the second, and this is the third.

Durable lessons were promoted **before** these entries moved, which is rule 5's ordering: toolchain traps
to [Traps.md](../../Traps.md), rules the code depends on to [Architecture.md §18](../../Architecture.md#18-invariants),
and obligations with an owning task to the [M5 carry-forward ledger](../ROADMAP.md#carry-forward-into-m5).
What is below is the index of what happened, not the rules that came out of it.

**Every *Verified* row is inside its own entry**, one line each, and the chain of counts runs
**1 981 → 2 197 EditMode** across the milestone. M3's thirty-three rows, which lived in Current State until
2026-09-20, are at the end of [PROGRESS-M3.md](PROGRESS-M3.md#verification-chain); M4 never accumulated an
equivalent block, because the rule that stopped it landed mid-milestone.

**The milestone in one line:** a run acquired a wall and a receipt — every fifth stage stands the Warden of
Ash up, 4 200 HP across three phases with an invulnerable beat and adds at 66 % and 33 %, under a segmented
bar whose seams are the asset's own thresholds; and dying pays `10·(deepest stage) + 50·(bosses killed)`,
banked in `PlayerProfile` v3 and drawn on a screen that replaces the death overlay rather than stacking on
it. Eleven tasks, **2 197 EditMode rows / 0 / 0**, **16 PlayMode / 0 / 0**.

**M4's ledger closed seven of seven: two discharged by their owners, one distributed, four carried into
[M5's](../ROADMAP.md#carry-forward-into-m5).** The tables and the ledger itself are in
[ROADMAP-M4.md](ROADMAP-M4.md).

---

## Log

### 2026-09-18 · M4-00a · Specs for M4-01…M4-04, the boss

**Built:** five specs where the ROADMAP had four titles — [M4-01a](../tasks/M4-01a-combatant-stats-and-triggers.md) the stat an effect can aim at · [M4-01b](../tasks/M4-01b-boss-agent-framework.md) phases, the beat and adds · [M4-02](../tasks/M4-02-warden-behaviours.md) the Warden's three attacks · [M4-03](../tasks/M4-03-boss-arena-and-views.md) its body and the arena hazard · [M4-04](../tasks/M4-04-segmented-boss-bar.md) the segmented bar GD §16.2 has wanted since M2-15. **Build order is ID order.** M4-00b specs M4-05…M4-07.

**Verified:** docs only — no code changed, so no suite run; the baseline stayed M3-15's **1 981 EditMode / 0 / 0** until M4-01a.

**Deviations:** *1*. **M4-01 split at the effects/AI seam before starting**, against the ROADMAP's single title, because the owner's requirement — *"the boss can get buff and debuff, skills, and everything that player can get"* — is a question about `Core/Effects`, and a phase machine is a question about `Core/Ai`. Two arguments, two reviews; M3-12's three-way split is the precedent.

**Learned:** **the combat machinery was already almost entirely target-agnostic and nobody had checked.** `Health` is one class for both bodies, `EnemyAgent` already carries two `Stat`s, `GrantedShieldPool` is keyed by `object`, `EffectRegistry.Apply` takes an `object source`, `SkillRunner`'s constructor names no player — and **`EnemyAgent.IsVulnerable` already exists**, so GD §9.1 rule 3's invulnerable beat needs no edit to the damage path. **Exactly two couplings block a boss with buffs and skills**: `ModifyStat` names a `PlayerStat` with the handler holding *the* `PlayerStats`, and `EnemyBlackboard` is a perception blackboard with no health fraction while `TriggerSpec` reads the player's `CombatBlackboard` → both are M4-01a's Files table. · **`EnemySpawned` gains no `IsBoss` flag**, and the nearby precedent was re-examined rather than copied: M3-13b's defaulted `IsElite` was right for a *look*, and a boss is not one — the phase count rides on `BossPhaseChanged` instead → spent in M4-01b rule 7. · **M4-02 deliberately contradicts M3-11b on capacity overflow**: a ninth zone throws, a fifth shockwave drops, because an add-heavy boss phase is where a ceiling actually gets hit → spent in M4-02 rule 8.

**Follow-ups:** **no ledger row moves, and one is inherited loudly.** [Row 1](ROADMAP-M4.md#carry-forward-into-m4) — the unreadable HUD — was ruled out of M4 by the owner right after the tag, so **M4-04's rule 8 forbids it from ticking a readability row**: it is the first task since M3-15's verdict to add a readout, and it lands a full-width band directly above M3-10b's 4 dp XP strip. Row 3 gains four device questions (telegraph legibility at phone size — GD §9.1 rule 7, which says to test *before calling it done* — the beat reading as a beat rather than a freeze, overlapping transparent VFX at fill-rate, and whether 75–120 s of one enemy holds attention). **One parking-lot line opened on the owner's words:** a **stage editor**, promoted when a designer wants stage 7 to differ from stage 6 for a reason no curve expresses — not M4's, because building an editor for one roster entry is tooling that outlives its content. **And one refusal recorded rather than left to be rediscovered:** no parallel `EnemyStat` enum.

### 2026-09-18 · M4-01a · A stat an effect can aim at, and a trigger a boss can read about itself

**Built:** `IStatBlock` — one address book, two implementations: `PlayerStats` (declaration plus `Has`, nothing else moved) and the new `CombatantStats` over an `EnemyAgent`, answering `MaxHp`, `MoveSpeed` and `ContactDamage` and **refusing everything else by name rather than minting a `Stat`**. `ModifyStat` gained a `StatTarget` defaulted to `Player`; `ModifyStatHandler` gained `Aiming(self)`, a scope that aims it for one call, and **a `Self` effect applied outside one throws rather than falling back to the player**. `EnemyBlackboard` gained `HpFraction` and `ShieldFraction`, written in `EnemySystem.Perceive` beside `DistanceToPlayer`. **It is used by nothing and moves no number in any run this build plays.**

**Verified:** **2 005 EditMode / 0 / 0, three runs**, against M3-15's 1 981 — +24, landing to the row (16 `StatBlockTests`, 7 `EnemyTriggerTests`, 1 `OathboundTreeTests`); **PlayMode 16 / 0 / 0 twice**; two red runs on the record, one deliberate (rule 5's fallback, 13 / 3) and one that killed a wrong premise about a corpse's blackboard; nothing under `Data/`, no prefab, no scene; Console 11 errors / 25 warnings, every one a fixture exercising a validation path, no compiler or analyzer warning; `dotnet format` exit 0 over thirteen files; `TimeManager.asset` reverted.

**Deviations:** *3*, in the spec's [As built](../tasks/M4-01a-combatant-stats-and-triggers.md). The one that changes a decision: **`PlayerStat` gained `ContactDamage`**, against the Files row that says it gains nothing, because two named test rows require `CombatantStats` to answer it and the Tests table wins. `PlayerStats.Resolve` therefore refuses a *legal* member for the first time, and **the enum's name is now the one thing about it that lies** — raised for the owner rather than renamed, because `ModifyStatDefinition` serialises it by ordinal into nine authored assets and a rename is a move.

**Learned:** **Unity writes a line for a serialised default** — `_stat: 0` is in every shipped asset and `MaxHp` **is** 0 — so "no diff because Unity omits defaults" is false; the diff is absent because **Unity does not rewrite an asset on disk when the script that reads it grows a field**. Measured with an in-memory clone serialised outside `Assets/`, which writes `_target: 0` in full — **Traps §1** (an API's silence is not agreement either). **A corpse's blackboard is frozen, not zeroed**: `EnemySystem.Perceive` skips the dead, so a dead boss's `HpFraction` is its last living reading — pinned by `Blackboard_ACorpseKeepsItsLastReading` and handed to **M4-01b**. **Proving a refusal red first paid for itself twice**: it showed the three rows that catch a fallback, and it caught a wrong assertion of ours about `DepthScaling`'s invisible depth-1 modifiers — **spent**.

**Follow-ups:** **no ledger row closes or opens.** Two things are handed to **M4-01b** in its *Next task* row: how a `SkillRunner` reads an enemy's blackboard (M4-01a rule 7 only put the fields there), and the corpse-fraction rule above. One thing is handed to **the owner**: rule on renaming `PlayerStat` to `StatId`, which is a move touching every authored asset's serialised enum. One pre-existing `ArenaView.OnValidate` false warning was found and deliberately not fixed — **Traps §1**'s family, described in *As built* for whoever next touches that file.

### 2026-09-18 · M4-01b · A boss is a combatant with phases: 66/33, an invulnerable beat, and adds

**Built:** `BossSpec` / `BossPhaseSpec` / `AddWave` — a boss as authored data, naming an ordinary `EnemySpec` for its body rather than restating one (ADR-0006). `BossPhases` — the whole of GD §9.1 rule 3 with no agent in it: latched one-way crossings, a beat that opens on the crossing tick and is not run down on it, and a backwards walk over the thresholds so one hit crossing two lands in the deeper phase and reports **once**. `BossBehaviour` — the thin part: reads `Health.Fraction`, clears its adds, raises the beat, summons the new phase's adds on a derived ring, publishes, and delegates the fighting to an `inner` behaviour that is null until M4-02. `BossEvents` — `BossPhaseChanged` (carrying `ofPhases`), `BossBeatStarted`, `BossBeatEnded`. Nine additive edits: `ModeSpec` gained a boss roster and `TryGetBossFor`, `ContentCatalog` gained `Boss`, `EnemySpec` gained `EnemyBehaviourKind.Boss`, `EnemySystem` gained `SpawnBoss` and `DespawnAtEndOfTick`, `SpawnDirector` gained one branch, `StageFlow` one line, `ModeDefinition` an authored field. **2 058 / 0 / 0, +53, three runs; PlayMode 16 / 0 / 0 twice.**

**Verified:** **2 058 EditMode / 0 / 0, three runs**, against 2 005 — +53, landing to the row (31 `BossSpecTests`, 22 `BossPhasesTests`); **PlayMode 16 / 0 / 0 twice**; first run red at 2 057 / 1, `Descent_EveryYamlKeyBindsToAField` writing `_bossRoster: []` into `Descent.asset` as it failed; Console swept after a clean rebuild of every assembly (`RequestScriptCompilation(CleanBuildCache)`): zero errors, zero warnings; `dotnet format` exit 0 over fourteen files; `TimeManager.asset` reverted.

**Deviations:** *3*, argued in the spec's [As built](../tasks/M4-01b-boss-agent-framework.md). *1*. **`Boss_FightLengthIsInsideTheBand` asserts a window rather than an asset**, because `Warden.asset` does not exist and [M4-02's own Files table](../tasks/M4-02-warden-behaviours.md) owns it — two specs disagreed and the one that owns the file won. The row pins **3 038 – 4 861** authored HP at stage 5, **4 740** once the two beats are priced in, every number computed through shipped code. *2*. **`Descent.asset` gained `_bossRoster: []`** — one line, authoring nothing, forced by a red run. *3*. **The director rows live in `BossPhasesTests.cs`**, the Files table allowing only two fixtures.

**Learned:** **three things, and two of them are corrections to what the code already said.** *(a)* **`EnemySystem.ApplyDamage` does not read `IsVulnerable`** — it never has; the flag is the *targeting* answer and `Health.SetExternalInvulnerable` is the *damage* answer. The spec named only the first, and the beat would have been cosmetic. *(b)* **The backwards walk `EnemySystem.Tick` has predicted since M2-08 came due, and the prediction was wrong to take** — reversing the pass would change the order every enemy acts in, which is the order `TargetScorer`'s tie-break reads and a seeded run replays; a deferred despawn queue costs one array and changes nothing. *(c)* **M4-01a's “Unity does not rewrite an asset because a script grew a field” is true of the disk and not of a test that forces a reserialise** — `Descent_EveryYamlKeyBindsToAField` went red on the new field and **wrote the fix into the asset while failing**, so the working tree carried the diff before the message had been read. That is a useful property of a destructive assertion and a surprising one; any later task adding a serialized field to a round-trip-tested definition should expect it.

**Follow-ups:** **no ledger row closes or opens.** Three things are handed to **M4-02** by name in its *Next task* row: the HP window this task computed, `BossBehaviour`'s `inner` seam, and the `BossDefinition` ScriptableObject M4-01b deliberately did not ship — an authoring type with no asset is the guess AR §6 bans, so it belongs with the assets it authors. **M4-01b's manual verification steps 1–3 go with them**, because they need a stage 5 that has a boss on it. One thing is handed further: **the seven `TriggerField` members that mean nothing to an enemy** are M7's Archon's to answer, and until then `TriggerSpec` stays player-only on purpose. And one rough edge is stated rather than solved: **a boss stage completes when the boss is down, whatever its adds are doing**, so an add can still hit the player through `Clear` and `Gate` — M4-02 or M4-03 to revisit with the fight in front of them.

### 2026-09-19 · M4-02 · The Warden of Ash: a shockwave you walk out of, a fissure you stand off, and Husks

**Built:** `ShockwaveSystem` — an expanding ring anchored to the ground the boss stood on, whose **front edge** bites once per body and whose ceiling is exact safe ground. `FissureSystem` — a crack placed under the player's feet at the instant it is cast, which arms for the authored telegraph, bites once, then closes. `WardenBehaviour` — five states, both attacks, cooldown-and-range selection with one coin off `IRandom.Misc`. `BossDefinition` — the authoring shell M4-01b deliberately did not ship. And **the content that makes stage 5 a boss stage at all**: `Warden.asset` (4 200 HP, 22 damage, 0.9 s telegraph), `WardenBoss.asset` (1 / 0.66 / 0.33, 1.5 s beat, 3 and 4 Husks), `Descent.asset`'s *every 5th* row, `enemy.warden.name`, and both onto `BootScope.prefab`.

**Verified:** **2 093 EditMode / 0 / 0, three runs**, against 2 058 — +35, landing to the row (18 `WardenBehaviourTests`, 16 `ShockwaveAndFissureTests`, 1 `ContentValidationTests`); first run red at 2 088 / 5 — three were `OathboundTreeTests` refusing the newly named `boss.warden` exactly as its M3-12c comment predicted, two were rows counting frames where `StateMachine` defers a transition; Console after a clean rebuild: zero errors, zero warnings; `dotnet format` exit 0 over thirteen files; `TimeManager.asset` reverted.

**Deviations:** *4*, argued in the spec's [As built](../tasks/M4-02-warden-behaviours.md). The one that changes a decision: **`inner` cannot be built by the caller of `SpawnBoss` and the signature stayed untouched anyway.** Every `IEnemyBehaviour` holds the agent it drives — `EnemyTickContext` has no *self* on it — and the agent does not exist until `SpawnBoss` has spawned one, so `SpawnDirector` cannot construct the thing it would pass. `EnemySystem` gained a trailing optional `Func<EnemyAgent, BossSpec, IEnemyBehaviour>`, `SpawnBoss` reads `inner ?? _bossInner?.Invoke(…)` so an explicit one still wins, and `RunSession.Start` supplies it. **Every boss gets a `WardenBehaviour` and V1 has one boss**; the line becomes a dispatch at M7's Choirmother and says so where it would be edited. The other three: `BossDefinition.cs` is an uncounted sixth file, `WardenBoss.asset` breaks the file-name convention because the spec chose the name (so `BossDefinition` is kept out of `ContentValidationTests`' three naming sweeps), and `RunSession.RequireAuthored` gained a boss-roster loop the Files table does not list.

**Learned:** **three things.** *(a)* **A band test is frame-rate dependent and an edge test is not** — `|distance − radius| ≤ half` steps over a body whenever `speed × dt` exceeds the thickness, which the shipped 8 m/s and 1.2 m survive at 30 fps *today* and would not survive a retune, silently. The front edge is monotonic, so `thickness` became a drawing width rather than a hit tolerance — **AR §18.4**. *(b)* **Three places in the codebase claimed `RunSession.Start` resolves a boss roster and none of them did it** — `ModeDefinition`, `ContentCatalog` and `BootScope` all say a run naming an unauthored boss is refused before it is announced; it would in fact have thrown out of `ContentCatalog.Boss` a hundred seconds into stage 5. Six lines, and it is what made the red run legible — **spent**. *(c)* **`OathboundTreeTests` predicted its own failure in a comment** written at M3-12c: it ships the whole enemy roster *"because `RunSession.Start` resolves the whole roster before it announces anything"*, and the boss roster caught it the same way one milestone later — **spent**.

**Follow-ups:** **no ledger row closes or opens.** M4-01b's manual steps 1–3 were carried into this spec's list, because they needed a stage 5 with a boss on it; steps 4 and 5 are device rows and stay on [row 3](ROADMAP-M4.md#carry-forward-into-m4). **Half of GD §9.1 rule 1 ships and the spec says so**: the rule wants a visual *and* an audio cue, there is no audio system in the project, and M7 owns the pass. **The manual steps could not be run as written, for the second consecutive task and the same structural reason — and the owner found it, not the handover.** M4-01b's steps 1-3 needed a boss that did not exist; M4-02's steps 1-2 need a *view* that does not, because nothing in `Soulvail.Game` subscribes to `ShockwaveEmitted` or `FissureArmed`. The spec's list is rewritten to test both rules by their effect on the health bar, the original wording is kept struck through for **M4-03** to re-run verbatim, and the lesson is that **a manual step naming a thing the player *sees* is only runnable if some view subscribes to the event behind it** - a question the writer of a handover can check with one grep and this one did not. **M4-03** inherits a ring and a crack that are entirely eventful — `ShockwaveEmitted` carries the origin, speed and ceiling, `FissureArmed` the position, radius and arm — so nothing it draws needs a read on `RunState`.

### 2026-09-19 · M4-03 · What the Warden looks like, and the arena that helps it

**Built:** `ShockwaveView` — a pooled expanding ring that integrates its own radius off `ShockwaveEmitted`'s origin, speed and ceiling and retires on `MaxRadius / Speed`. `FissureView` — one body in two states, the arm a disc opening towards the circle core will test and the bite a **1.35×** overshoot collapsing to nothing, so the two differ by *shape and direction* rather than by brightness. `BossBeatView` — a pulsing shell parented to the boss's own transform, in `Palette.Neutral`, retiring on `BossBeatEnded` or on the length the event carries. `BossViews` — one census over three pools taking all seven subscriptions, stepped by `RunTicker` on the snapshot's clamped `Dt` beside the bolts, the rings and the zones. `ArenaView` gains hazards, `Arena_Pillars` gains a brazier a slam knocks over and `Arena_Tiered` gains none (rule 6). Three prefabs, dressed into `Run.unity`. **The point of the task in one line: until it, nothing in `Soulvail.Game` subscribed to any of M4-02's five hazard events or M4-01b's two beat events, and the owner playtested a boss whose every attack was invisible.**

**Verified:** **2 124 EditMode / 0 / 0, five green runs** (two after the premultiply change), against 2 093 — +31, all `BossViewTests`; first run red at 2 121 / 3, all three the fixture's arithmetic; Console four errors, all `LocalJsonSaveStore`'s deliberate corrupt saves, zero analyzer warnings; `dotnet format` exit 0 over eleven files; **a camera capture of all four bodies over a floor** found the premultiply bug and set the three prefabs' alphas. Nobody has played to stage 5.

**Deviations:** *3*, argued in the spec's [As built](../tasks/M4-03-boss-arena-and-views.md). The one that changes the Files table: **`BossViews.cs` is a fourth file the table does not list**, because three pooled bodies need a listener and this project's shape (`ZoneView` ↔ `ZoneViews`) would have made that three more files and three more `RunTicker` arguments. Five new code files; size **M** unchanged by the ROADMAP's own criterion. The one that *removes* work: **`Warden.prefab`, `M_WardenAsh.mat` and `Warden_PrefabIsDressed` have no subject and were dropped** — `Warden.asset` already authors its tint and `bodyScale` **2.2**, `BuildLookBook` reads every definition in the boot list and `Warden.asset` is in it, and `EnemyViews` applies the look on every rental, so **rule 1 was already true of the shipped build and `EnemyViews.cs` is untouched**. Checked in the Editor before a line was written, which is what the owner asked for. The third: **`ArenaSpec` does not exist** — M2-11a shipped `ArenaView` in `Soulvail.Game` and no core arena type at all — so the hazard is authored on the view, and `ArenaView`, `RunTicker` and three unrelated fixtures took additive edits the table does not list.

**Learned:** **three things, and two of them are about files this task did not write.** *(a)* **`M_TelegraphRing.mat` blends premultiplied alpha without URP's `_ALPHAPREMULTIPLY_ON` keyword**, so the shader never scales the albedo: every view sharing it writes the palette's colour unscaled and therefore draws at **full brightness whatever its alpha says** — the alpha only decides how much floor shows through. A camera capture is what found it (the beat's shell was an opaque grey ball over the boss). M4-03's three views premultiply; `ZoneView`, `TelegraphRingView` and `BulwarkView` do not, and one keyword on one material makes all six agree — the owner's call rather than this task's. *(b)* **Every *"ring"* in this game is a square**: `VFX_TelegraphRing.prefab` is an untextured built-in Quad, and the spawn telegraph, the blast ring and the Consecrate ground have all been squares since M2-12b. The two new decals inherit it rather than departing from it, because one round hazard beside four square ones is worse than five square ones; one circle texture fixes all five, and it is art. *(c)* **Rule 6's hazard is authored and knocked over and costs the player nothing, and the task says so out loud** — hazard *damage* needs an id, a circle and a number in core, and there is no core file in this table. A visible reaction that claims no damage is honest; a serialized field with no reader is what M4-02 refused for `BossDefinition`.

**Follow-ups:** **no ledger row closes; [row 3](ROADMAP-M4.md#carry-forward-into-m4) gains four questions and a finding.** GD §9.1 rule 7's phone-size question is asked of a telegraph for the first time in the project and **the rule is unmet rather than ticked**; GD §11.3's fill-rate ceiling is reachable for the first time (a ring, up to eight cracks, a Consecrate zone and a spawn telegraph can overlap); whether the arm/bite shape change reads at thumb distance; and whether the shell says *not taking damage* or merely hides the boss. The premultiply finding rides there too. **M4-02's manual steps 1 and 2 are quoted verbatim into this spec's list as steps 1a and 1b** — they are this task's acceptance, a subscriber for both events now exists, and **nobody has played to stage 5**: every manual step is the owner's.

### 2026-09-19 · M4-04 · The segmented boss bar: a phase you can see coming

**Built:** GD §16.2's last unbuilt row — a full-width band across the top of the HUD, one segment per phase, **one fill across all of them** with the phases as opaque seams drawn over it. `BossBarView` is dumb and `HudPresenter` drives it from five events; `RunScope` registers nothing new. `Palette.Boss` (`#CCC4B7`) is the tenth colour.

**Verified:** **2 145 EditMode / 0 / 0, twice consecutively on the final code**, against 2 124 — +21, all `BossBarViewTests`, the only existing-fixture edit two assertions appended to `BossPhasesTests.Phase_IsAnnouncedWhenTheBossStandsUp`; first run red at 2 126 / 19 — the fixture's Warden authored `targetPriority: 9` and `EnemySpec` refuses above 8; Console five errors / thirty-five warnings, all deliberate, none naming a type this task touched; `dotnet format` exit 0 over eight files; `Hud.prefab` diff 264 insertions, zero deletions; the band driven through a live canvas on the saved asset read its seams at 2 047.3 and 639.3 of 2 560 (0.8 and 0.25 exactly). Nobody has played to stage 5.

**Deviations:** *2*, both in the spec's [As built](../tasks/M4-04-segmented-boss-bar.md#as-built). **The one that changes a decision: `BossPhaseChanged` gained `IReadOnlyList<float> EntersBelow`.** Rule 4 puts the marks at the phases' own thresholds and **the event carried only the count, so nothing in `Soulvail.Game` could have known where a mark goes** — a `ContentCatalog` on the HUD reverse-looking-up the boss whose body an `EnemySpawned` named is the seam `EnemySpawned.IsElite`'s own remarks refuse. So the fact rides the event, which is `ShieldGranted.Total`'s argument; the ROADMAP's sizing rule waves an event-struct field through by name. **It is the first event in the project to carry a collection**, and it is safe because `BossPhases` wraps its array once at construction — publishing allocates nothing and its allocation row is untouched.

**Learned:** **the shipped boss could not have told rule 4 from the thing rule 4 forbids.** `WardenBoss.asset` authors 1.0 / 0.66 / 0.33 and even spacing gives 0.6667 / 0.3333 — **7 px and 4 px** apart on a 1080-wide safe area, which is three seam-widths and one. So the mapping is driven at 1.0 / 0.8 / 0.25 and asserted *both* ways (at the thresholds, and `Is.Not.EqualTo(2/3)`), and `Bar_LearnsItsMarksFromARealFight` drives it from a real `BossPhases` so the fixture cannot agree with itself → spent in the As built. · **A colour argued by elimination rather than chosen.** A boss is not the player, not a reward, not corruption and **may not be `Danger`** (rule 6), so it is GD §16.4's *everything else* and the only freedom left is **value** — `Palette.Neutral` brightened ×1.85, which is `PlayerBlocked`'s move one band over. A separate member was necessary rather than tidy: `Neutral` is already an enemy's bar *and* M4-03's beat shell → M3-13a's tenth-colour rule, paid. · **`PaletteTests` needed no row** — `ColourMembers()` reflects over every public static `Color`, so two existing sweeps covered the new member the moment it existed → spent. · **Unity 6.3 rewrites `ProjectSettings/TimeManager.asset` into a `serializedVersion: 2` count/rate pair worth exactly 0.02 as soon as the Editor touches the project.** A no-op re-serialisation, reverted here, and it will come back for whoever opens the Editor next → [Traps §5](../../Traps.md).

**Follow-ups:** **rule 8 honoured: no readability row ticked, and the arrangement stated as numbers** — 0–4 dp XP strip, 5–15 dp band, 16 dp down the HP row, two 1 dp gaps. `Hud_StripAndBarDoNotOverlap` asserts all four edges **plus the collision the spec did not name** (the HP row, which a full-width element reaches first), so **manual step 4 was answered before it was written and no number had to move**. Whether that is *legible* is [row 1](ROADMAP-M4.md#carry-forward-into-m4)'s and **M4-07's**, and it is three serialized dp fields when the answer comes. [Row 3](ROADMAP-M4.md#carry-forward-into-m4) gains two device questions: whether a band, a 4 dp strip and a landscape notch coexist in the safe area, and **whether the beat's dim reads as *"you cannot hurt it"* rather than as a HUD glitch** — the beat is a `CanvasGroup` alpha rather than a second colour, on the palette's own alpha rule. No ledger row closes.

### 2026-09-19 · M4-00b · Specs for M4-05…M4-07 — run end, the payout, and acceptance

**Built:** four specs where the ROADMAP had three titles — [M4-05a](../tasks/M4-05a-shard-payout.md) the payout arithmetic · [M4-05b](../tasks/M4-05b-profile-v3-and-shard-writer.md) `PlayerProfile` v3 and the writer that banks it · [M4-06](../tasks/M4-06-run-end-screen.md) the run-end screen and the overlay it replaces · [M4-07](../tasks/M4-07-acceptance-and-tag.md) acceptance and the `m4` tag. The three title rows gained Size and Depends on; M4's task count moves 10 → 11. Docs only: no code, no compile, no suite.

**Verified:** docs only — four specs and two plan files, no C#, no suite run; the baseline stays M4-04's **2 145 EditMode / 0 / 0**. What was verified is the documents: every ROADMAP table re-counted, every new `tasks/` link resolves, and every type each new spec names was grepped before its rule was written — the method rule this milestone earned after M4-03 was written against an `ArenaSpec` that does not exist.

**Deviations:** *1*. **M4-05 split at the arithmetic/persistence seam before starting**, against the ROADMAP's single title — the same place M3-01 and M3-07 split, a feature then the format bump that makes it survive an app kill. [Row 5](ROADMAP-M4.md#carry-forward-into-m4) asks the bump for *its own review*, and a task that also contains a formula is not that.

**Learned:** **the title assumed the wrong event, and `RunEnded` gains nothing.** `RunSession.End` is a deliberate no-op-if-not-running so scope disposal can call it blind, which is why `SaveWriter.cs:78`, `HudPresenter.cs:50` and `PausePresenter.cs:64` have each already refused that event — a payout riding it would pay a player for quitting and pay again on every teardown. M4-04's *"a readout's facts ride the event"* still applies, to a **new** event: `ShardsAwarded`, published only inside `RunSession.Tick`'s `IsDead` branch, between `PlayerDied` and `RunEnded` → the specs, and this row. **And the grep that shrank M4-05a from a format bump to three files: bosses killed needs no counter.** A stage is only left once `SpawnDirector.IsStageComplete` is true, and on a boss stage that property *is* `_bossCleared` (`SpawnDirector.cs:243`) — so every boss stage below the current depth had its boss killed, `ModeSpec.TryGetBossFor` says which those were, and the payout is a pure function of depth and mode that survives a resume for free. A counter would have lived on `RunState`, hence in `RunSnapshot`, hence in a **v4** → spent. **The method rule this milestone earned held:** M4-03 was written against `ArenaSpec`, which does not exist, and M4-04's rule 4 assumed `BossPhaseChanged` carried thresholds it did not — both cost a deviation. Every type named in these four specs was grepped first, and two rulings changed because of it → spent.

**Follow-ups:** **four ledger rows were re-owned, because M4-00a merged without discharging any of them and nothing in the protocol noticed for five tasks.** [Row 1](ROADMAP-M4.md#carry-forward-into-m4) (the unreadable HUD) → **M4-07 to judge, with a measurement rather than a shrug**; the fix has no M4 owner and that is the owner's standing ruling. [Row 2](ROADMAP-M4.md#carry-forward-into-m4) (three playtest numbers) → **M4-07 to produce or to carry with a written reason** — it sits behind row 1 and there is no fixed UI to run it against, so carrying twice is the likely honest outcome. [Row 4](ROADMAP-M4.md#carry-forward-into-m4) (`FrameOrderTests`, one isolated run in ten) → **M4-07, as a numbered checklist row with M3-08b's two filters written out at n ≥ 20 each side**; a standalone diagnosis task was considered and refused, because there is no bug to fix and such a task could only report. `M4-07a` if it finds one. [Row 6](ROADMAP-M4.md#carry-forward-into-m4) (content discipline) → **M4-07 to distribute**, and it gains a fourth reader on purpose: M4-05a rule 7 puts GD §14.1's 10 and 50 in `const`s, because there is no asset a payout belongs on. [Row 3](ROADMAP-M4.md#carry-forward-into-m4) gains two device questions from M4-06 — whether five labels and three numbers read at thumb distance, and whether one button clears a cutout on both rotations — **eight questions, four tags, nothing ever run outside the Editor**. [Row 5](ROADMAP-M4.md#carry-forward-into-m4) now names M4-05b. **Two rulings the owner asked for, made here rather than deferred:** GD §14.1 ships **two terms of three** — the archetype term is a *lifetime* fact needing a set of `ContentId`s on the profile, and deferring it **over**-pays a returning player rather than under-paying them, which is the exact opposite of the Shard total, where not writing the number destroys it; and **Shards are persisted with nothing to buy**, because an unread colour costs nothing to add later while an unwritten total is data gone. M4-07 annotates GD §14.1 so the doc stops claiming three.

### 2026-09-19 · M4-05a · Death pays: the Shard payout, computed where the run ends

**Built:** GD §14.1's *"earned on death regardless of outcome"* as a number the game computes. `ShardPayout` is a pure static function of a depth and a `ModeSpec` — `10·(deepest stage) + 50·(bosses killed)` — and the run's death branch publishes exactly one `ShardsAwarded` carrying the total and both terms. Two new files, one event struct, one publish site. **Nothing in `Soulvail.Game` changed and nothing is drawn**: the event goes into a hub nobody is listening on, which is M4-01b's shape, and [M4-06](../tasks/M4-06-run-end-screen.md) is the half that reads it.

**Verified:** **2 162 EditMode / 0 / 0, twice consecutively on the final code**, against 2 145 — +17, landing to the row (12 cases in `ShardPayoutTests`, 5 in `RunSessionTests`); first run red at 2 161 / 1 on `SpitterBehaviourTests`' whole-wire assertion, which is the deviation; compile confirmed the way [Traps §3](../../Traps.md) asks — `typeof(ShardPayout)` and both `const`s read back through `Unity_RunCommand` on an unfocused Editor; Console five errors / forty-three warnings, all deliberate, the dump grepped for every type this task touched; `dotnet format` exit 0 over six files; `Payout_AllocatesNothing` runs through `AllocationAssert.None` over 10 000 iterations. No manual verification exists and the spec says so.

**Deviations:** *1*, in the spec's [As built](../tasks/M4-05a-shard-payout.md#as-built). **`SpitterBehaviourTests.Session_ShotThatKills_EndsTheRunWithNoIntent` gained one line** — it asserts the *whole wire* of the death tick as an ordered array of types and was the only row in the project that did, so the first suite run came back `2 161 / 1` reading *"Expected `RunEnded`, but was `ShardsAwarded`"* at index 3. `typeof(ShardsAwarded)` went in between `ProjectileImpacted` and `RunEnded`. A small additive edit the ROADMAP's sizing rule does not count, so the counted total is still the table's two new files — and it turned out to be the strongest evidence in the suite that the payout is on the **death** path rather than on `End()`: a bolt killed a player in a fixture with no interest in Shards, and the run was paid for it.

**Learned:** **both of M4-00b's load-bearing rulings were still true of the code, and checking them cost two greps rather than a deviation.** `End` is still a no-op-if-not-running and still the only publisher of `RunEnded`; `SpawnDirector.IsStageComplete` still returns `_bossCleared` on a boss stage at the exact line the spec named. **Two of the three refusal sites had drifted and the As built says so** — `SaveWriter`'s `Subscribe<PlayerDied>` is at `SaveWriter.cs:82` rather than 78, and `PausePresenter` cites `SaveWriter.cs:128` rather than its own line 64 — neither changing a decision → spent. · **`Run_AwardsNothingWhenTheScopeIsTornDown` was written second, before any of the arithmetic**, so rule 1's refusal was a failing-then-passing row rather than a comment → the method the spec asked for, paid. · **Nothing in `RunSessionTests` could kill a player**, because its Husk is `Static` and `Static` does nothing by definition — so the payout rows needed a fixture device: one `Chaser` with 400 contact damage spawned at the player's feet, and **no snapshot reporting at all**, because `EnemySystem.Ingest` only overwrites the agents a snapshot names, so a body nobody reports stays where the plan put it → spent. · **The publish site looks the mode up rather than holding it** (`_catalog.Mode(State.ModeId)`), and pays one redundant walk of an authored array on the one frame a run ever ends, instead of re-declaring GD §14.1's arithmetic at the call site → stated out loud in the As built, because it looks like an oversight. · **`ProjectSettings/TimeManager.asset` was re-serialised again** — the seventh time in eleven tasks. Reverted here → [Traps §5](../../Traps.md).

**Follow-ups:** **no ledger row opens and none closes.** [Row 6](ROADMAP-M4.md#carry-forward-into-m4) already records this class as its knowing fourth reader — M4-00b wrote that in, so rule 7's `const`s needed nothing added and **no asset was invented for a payout**. [Row 5](ROADMAP-M4.md#carry-forward-into-m4) is untouched and still [M4-05b](../tasks/M4-05b-profile-v3-and-shard-writer.md)'s: **this task persists nothing at all**, which is the whole reason the split exists. **Rule 4's under-payment is now pinned rather than described**: a player who kills the boss on stage 5 and dies there before walking through the door is paid **50 rather than 100**, asserted in both directions, and it is an M4-07 checklist line and the M5 ledger's if anybody minds. **Rule 6's missing term has a row guarding it**: `Payout_HasNoArchetypeTerm` asserts 10 and `Is.Not.EqualTo(35)` at stage 1, so nobody can add GD §14.1's third term without reading why it was left out — and when it ships it needs no new run tracking, because `ModeSpec.TryGetIntroduction` already authors which archetype arrives at which stage. **No manual verification exists and that is the spec's own ruling**, not an omission: nothing in `Soulvail.Game` changed, nothing is drawn, and the Console and the suite are the whole of this task's evidence. **Nobody can see a Shard yet** — M4-05b banks it, M4-06 draws it.

### 2026-09-19 · M4-05b · `PlayerProfile` v3: the first thing a dead run leaves behind

**Built:** the payout M4-05a computes now survives the app being killed. `PlayerProfile` is **v3** with one `int` — `Shards`, a lifetime total, guarded non-negative — plus `WithShards`; `MigrateProfile` gained the `if (version < 3)` step below the `< 2` one and writes `shards: 0` **unconditionally**; `ProfileMirror` gained a `shards` key; and `ShardWriter` hears `ShardsAwarded`, reads `ProfileStore.Current.Shards`, **adds**, and hands the profile back through the single writer. **The field, its step, its adapter key and its fixtures shipped in one PR**, which is [ledger row 5](ROADMAP-M4.md#carry-forward-into-m4)'s whole ask and the row is discharged here. **`RunSnapshot.CurrentVersion` stayed 3** — the two numbers are now *equal and mean different things*, said out loud in `SaveMigrations`' remarks and in `ProfileGate_AcceptsOneToThreeRefusesFour` rather than left to be discovered. **Two new files; sixteen more changed, almost all of them the compiler-guided ripple** of a constructor gaining a fourth argument.

**Verified:** **2 177 EditMode / 0 / 0, twice consecutively on the final code**, against 2 162 — +15, landing to the row (4 `SaveDtoTests`, 1 `SaveMigrationTests`, 1 `LocalJsonSaveStoreTests`, 8 `ShardWriterTests`, 1 `InstallerTests`), with four rows renamed rather than joined and a third guard folded into `Store_RoundTripsAProfile`; first run green — a fourth constructor argument is caught by the compiler at twenty-five call sites and by no assertion; compile confirmed through `Unity_RunCommand` on an unfocused Editor (both `CurrentVersion`s read 3, a v1 profile carrying 999 migrated to `shards: 0`); Console 16 errors / 51 warnings across two runs, all deliberate, the one line naming this task's code being `Writer_SurvivesAFailedWrite`'s own `LogAssert.Expect`; `dotnet format` exit 0 over eighteen files; the spec's five manual steps are the owner's, step 5 (kill from recents after a death) **[device]**; `TimeManager.asset` reverted, the eighth time in twelve tasks.

**Deviations:** *3*, all additive, in the spec's [As built](../tasks/M4-05b-profile-v3-and-shard-writer.md#as-built). **(1) `RunTicker.cs` is a sixth file the Files table does not name, and rule 6 does** — a VContainer `Lifetime.Scoped` registration nobody resolves is **never constructed**, so `RunInstaller.Register<ShardWriter>` alone would have shipped a writer that was registered, never built, never subscribed, and every run would have ended paying nothing with no error anywhere. Rule 6 already required the fix in as many words (*"`SaveWriter`'s shape **and its argument**"*), so the constructor gained `ShardWriter shardWriter`, discarded into `_` beside the `saveWriter` line doing the same job. **This is the deviation that would have been a shipped bug rather than a tidiness issue**, and it was found by reading rule 6 against the Files table rather than by any test — nothing in the suite can observe a service that was never asked for. **(2) `RunScope_ComposesWithTheWriter` went into `InstallerTests`** rather than a file the tests row names: the claim is about the two installers *together*, and that is the only fixture in the project that builds both. **(3) Rule 2 names a row that has never existed** — there is no reflection row in `ProfileStoreTests`; the door is `SaveDtoTests.Profile_HasNoConstructorThatOmitsAField`, which is what the Tests table names, and it went from three parameters to four. Spec versus code, and the code won.

**Learned:** **the trap row 5 was opened for is real and this task walked right up to it.** A PR with only the field compiles, runs green and merges — `Fixture_V1Profile_DecodesToTheExpectedProfile` would have kept passing, because a v1 document still decodes and a missing step is indistinguishable from an identity until a player's file is already wrong. What makes the step *checkable* is `ProfileGate_AcceptsOneToThreeRefusesFour` asserting the literal numbers: phrased against `CurrentVersion` it would have passed unchanged through the bump → the row earns its keep for the second time. · **The harness's claim held exactly, which is what row 5 predicted and this is where it was collected**: `Profile_ChainAndGateMirrorTheRun` now loops over **three** versions and not one assertion changed — the single edit the bump forced on it is the constructor argument every call site gained → spent. · **The v1 → v2 step names `shards: 0` too, and that is not the v2 → v3 step repeated**: a step has to name every parameter the constructor takes, and naming the field it knows nothing about with the value the next step writes anyway is what keeps the two independent of each other's order → written into the comment beside it, because it looks like duplication. · **Three rows were renamed rather than joined by new ones**, each on a precedent the run format set at M3-07b: identity moves up with the current version (`MigrateProfile_V3_IsIdentity`), what used to be identity becomes a step (`MigrateProfile_V2_GainsNoShards`), and *"what this build writes"* becomes *"decodes to"* on the adapter side → the net row count from four renames is zero, which is why **+15** lands. · **`Profile_HasNoShards` was a row that had to be renamed rather than deleted**: its subject is the property list, and what it refuses moves on as each field arrives — it is now `Profile_HasNoUnlocks`, holding GD §14.2's M6-09 fields to the same bar. · **The first suite run was green**, unlike M4-05a's: nothing in the project asserts a profile's whole shape the way `SpitterBehaviourTests` asserts a death tick's, so a fourth constructor argument is caught by the compiler and by no assertion anywhere. **The spec predicted *"M3-09c's fourteen sites again"* and there are twenty-five** — the format has had two writers and three fixtures added to it since, and the figure was carried forward rather than recounted. · **`ProjectSettings/TimeManager.asset` re-serialised again** — the eighth time in twelve tasks. Reverted here → [Traps §5](../../Traps.md).

**Follow-ups:** **[row 5](ROADMAP-M4.md#carry-forward-into-m4) closes and no row opens.** The bump is done, honoured rather than waived, and the two `With` helpers M3-09c built were used by a second feature for the first time — `ShardWriter` never calls the constructor, and the reflection row went one parameter wider rather than gaining a convenience overload. **Rule 8's ruling is the one most likely to be questioned later and is restated in the As built as built rather than as argued**: this build banks a number **no player can spend**, GD §14.2's unlocks are M6-09's and the Sanctum is M6-02's, and the rule it is measured against is *"no field before a consumer"* — of which there are two, the writer that landed today and [M4-06](../tasks/M4-06-run-end-screen.md)'s screen. **Nobody can still *see* a Shard** — a run now pays, banks and survives an app kill, and draws nothing; M4-06 is the half that reads it. **The five manual steps were not performed and the spec says which**: four are Editor checks on `profile.json` in the persistent data path, and step 5 — kill from recents immediately after a death — is **[device]** and is the one *correctness* question on [row 3](ROADMAP-M4.md#carry-forward-into-m4)'s list, now with a second file behind it. **Nothing in a scene, a prefab or an asset was touched.**

### 2026-09-20 · M4-06 · The run-end screen, and the overlay it replaces

**Built:** a player can see a Shard. `RunEndPresenter` opens on `ShardsAwarded` — not `RunEnded`, not `PlayerDied` — draws the payout beside its two terms and offers one button back to the Menu. `RunEnd.prefab` is a full-screen canvas at `sortingOrder` 200: five table words, three figures, the Shard total `Palette.Essence` and the other two `Palette.Neutral`. **The death overlay is gone from `HudPresenter` rather than stacked under it** — three handlers, five fields, two `LocKey`s and three dependencies left, `Construct` went five parameters to two, `Update` is one call. The screen is a **required** `RunScope` component, because rule 2 took the only exit off the HUD. `English.asset` gains three rows; the two death keys move.

**Verified:** **2 197 EditMode / 0 / 0, twice consecutively on the final code**, against 2 177 — +20, landing to the row, all in `RunEndPresenterTests`; first run green. Console after a clean rebuild of every assembly: zero errors, two warnings, both the known `ArenaView.OnValidate` false positives (issue 4). `dotnet format` exit 0 over eight files; the layout measured through a live canvas on the saved asset; `TimeManager.asset` reverted.

**Deviations:** *3*, all about where a test row lives, none about behaviour — spec's [As built](../tasks/M4-06-run-end-screen.md#as-built). The one worth knowing: **the Files table names `HudPresenterTests`, which has never existed**, so the two HUD rows went to this task's own fixture.

**Learned:** **the tripwire was decided by counting rather than feel** — three `Label`/`Figure` pairs on one prefab, no `PayoutRow` wanted, no split → spent. · **`TestRunnerApi.RegisterCallbacks` plus a file write in one MCP command is refused as *"user interactions are not supported"*; the same callbacks logging instead are not** — and no harness is needed, because **Unity writes `TestResults.xml` to the persistent data path on every run**, with the count and every failing row's message → [Traps §4](../../Traps.md). · **The Editor's safe-area inset is 0 on all four edges**, so a `SafeAreaFitter` is inert in every Editor measurement ever taken here — the insets rule 11 asked for are a device question in an Editor number's clothes → [row 3](ROADMAP-M4.md#carry-forward-into-m4).

**Follow-ups:** **no ledger row closes and none opens.** [Row 1](ROADMAP-M4.md#carry-forward-into-m4) is inherited rather than escaped for the second time this milestone: **no readability row is ticked**, the arrangement is stated as point sizes and pixel extents, and M4-07 says whether a screen made of words can be read. **Rule 8's refusal is the likeliest to be questioned later**: no lifetime total, because this screen and `ShardWriter` hang off one event and the hub orders neither — `Screen_DrawsNoLifetimeTotal` refuses it by construction. **The six manual steps are the owner's**; step 6 is **[device]**.


### 2026-09-20 · M4-07 · M4 acceptance: a wall to hit, a receipt for hitting it, and the ledger judged

**Built:** no code, no tuning, and **none of row 2's three playtest numbers, for the second acceptance running** — but a **feel verdict** from the fight: the owner reached stage 5, **was killed by the Warden and did not kill it**, and called it *"good for now"*. *Does a run have a wall to hit* is **yes**. **Row 1 is answered with a measurement:** seven of eight text elements clear Android's 12 sp floor; **S1–S4's labels do not** — 18 pt is **7.9 dp**, **66 %** of the floor, inside a 60 dp circle. **Row 4's experiment killed its own hypothesis**, **row 6 is distributed**, GD §14.1 annotated. `m4` is accepted on **Editor evidence plus that run**, device rows deferred, multi-touch named apart.

**Verified:** **2 197 EditMode / 0 / 0 ×3**, **+0** against M4-06; **PlayMode 16 / 0 / 0 ×3**, first full run since M4-01b; **63 further PlayMode runs** for row 4. Console after the EditMode runs: 20 errors / 50 warnings, all deliberate, **no `CS`/`UNT`/`IDE`**; `dotnet format` exit 0; six assemblies; `git diff m3 HEAD -- ProjectSettings/` **empty**; `TimeManager.asset` reverted.

**Deviations:** *2*, in the spec's [As built](../tasks/M4-07-acceptance-and-tag.md#as-built). M4's tables and ledger went to [ROADMAP-M4.md](ROADMAP-M4.md) and `Traps.md` took three findings — neither file is in the Files table; the M0–M3 pattern, and rule 5's instruction to promote first.

**Row 4:** `FrameOrderTests` alone **4/20 (20 %)**; with `BootSmokeTests` first **1/20 (5 %)**. Both ordinary against M3-08a's 10 % (P(X≥4) = 0.13; P(X≤1) = 0.39) and not different from each other (Fisher two-tailed **p ≈ .34**). The **opposite sign** to M3-08b's prediction, so the hypothesis is **retired** and the row carries a new next step. Every failure was `Ticker_RunsTheStepsInOrder` on M2-15a's message — **still the physics half**. No bug, no **`M4-07a`**.

**Learned:** **the Editor is not a legibility instrument** — `Screen.dpi` reads **120**, so every dp-sized element here was drawn at **30 %** of device size → [Traps §9](../../Traps.md). · **A `TestRunnerApi` loop fits inside one submitted command** — `Execute` from `EditorApplication.update`, counters in `SessionState`, a token so stale sinks unregister → [§4](../../Traps.md). · **`BossBehaviour.Tick` has no `AllocationAssert` row** where its four neighbours do → ledger.

**Follow-ups:** **M4's ledger closed seven of seven — 5 and 7 discharged, 6 distributed, 1–4 carried into [M5's](../ROADMAP.md#carry-forward-into-m5), which opens with eight rows.** Row 8 is the playtest's: whether *not dying* is 4 200 HP outrunning real damage or a bar that never moves is **undistinguished**, and M4-01b's 106.7 s assumed damage nothing interrupts. Two rulings: the **under-payment stays a spec footnote**; the **premultiply finding carries an owner**, M5-00a. **M5's table gains `M5-00a`/`M5-00b`**; 8 → 11.

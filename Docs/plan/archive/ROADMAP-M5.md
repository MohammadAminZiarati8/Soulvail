# Soulvail — Roadmap archive: M5, Second class

_Moved verbatim from [ROADMAP.md](../ROADMAP.md) at [M5-08](../tasks/M5-08-acceptance-and-tag.md), when `m5` was accepted — the fourth milestone archived at its own acceptance. Links are re-based to this folder. A stub heading stays in the ROADMAP for every heading here, so links into them still resolve. The milestone's log is [PROGRESS-M5.md](PROGRESS-M5.md)._

**The ledger below is kept as it stood; how each row closed is the table that follows it.** What carries is [M6's table](../ROADMAP.md#carry-forward-into-m6), and every carried row there names an unmerged owner — M4-07 rule 11, still holding.

**One task in the table below was created *by* the acceptance and merged after this archive closed.** [M5-08a](../tasks/M5-08a-splash-offers-what-install-refuses.md) is M5-08 rule 8's *"a bug found here becomes `M5-08a` with its own PR"*, on M2-15a's precedent — a milestone's task merged after that milestone's archive closed. It shipped before the `m5` tag by the owner's ruling, so the tag marks a build whose half-tree moment works; **its log entry is in M6's PROGRESS rather than here**, because this file was written first.

---

## M5 — Second class

**Goal:** a second class the player can pick, and the machinery a class that is not the Oathbound needs.
**Done when:** M5-08's checklist passes and `m5` is tagged.

**M5 opened with a spec group and that group is closed: M5-00a and M5-00b between them replaced every title below with a spec.** Two things forced it rather than made it tidy, and each is now a named task — the branch-index limitation is [M5-07a-i](../tasks/M5-07a-i-the-runs-tree-widens.md) and the minion `IStatBlock` was answered across [M5-04b](../tasks/M5-04b-rise-and-minion-stats.md) and [M5-06a](../tasks/M5-06a-what-a-legion-node-may-reach.md). **The `TreeRules` / `SkillTree` branch-index limitation has an owner for the first time** — [M3-03](../tasks/M3-03-tree-rules.md)'s constructor takes a single `SkillTreeSpec` and every branch-shaped member is an index into that one tree, which is correct for one class and has nowhere to put CH §5.4's borrowed branch; the parking lot named **M5-07a** as its promoter and **M5-00a** as the task that specs it from M3-03 until M5-00a corrected it to **M5-00b**. And **M5-04a/b's Wights are the first minions anything has asked `IStatBlock` for** — [M4-01a](../tasks/M4-01a-combatant-stats-and-triggers.md) shipped `CombatantStats` over an `EnemyAgent` answering three members and refusing the rest by name, and a friendly minion is the first caller that is neither the player nor an enemy.

**Two corrections this table carries, both found at M4-07.** `M5-07a` has been in it since M3-00c and was never counted, so M5's task count was **8** against a table of nine rows; with the two spec tasks it was **11**, and **M5-00a's split of M5-04 makes it 13**. And **M5-02** is the standing owner of the **Oathbound speed-band contradiction** — GD §6.1's 5.4–6.2 m/s band against the shipped 3 m/s — by the owner's ruling re-stated at M3-15 and again at M4-07; it is the first task that has to author a second class's speed and therefore the first that cannot avoid the band. [M5-02](../tasks/M5-02-gravecaller-and-bone-bolt.md) rule 1 resolves it: the band scales by the retune's own factor to **3.0–3.4 m/s**.

**A third correction, found at M5-00b: the count above was one over its own table.** *13* was written against twelve rows — seven specced plus five titles — so the arithmetic in the paragraph above lands at 12 and not 13 however it is read. **With M5-00b's three splits the table holds fifteen rows and the header now says 15**, counted rather than incremented.

**M5 is specced whole, and M5-00b split three of its five remaining titles.** M5-00a split **M5-04** at the body/passive seam and weighed **M5-01** for a split and kept it whole at four counted files. M5-00b split three more, each **before** starting and each because a counted Files table exceeded five:

- **M5-05 → [M5-05a](../tasks/M5-05a-wight-views-and-concurrency.md) / [M5-05b](../tasks/M5-05b-decoy-view-and-the-look.md)**, at the seam of **what can be seen**. M5-05a is the frame — a census, an intent door, an apply step, and the PlayMode fixture that asserts orderings — and **nothing it ships can be looked at in play**, because no run this build can start raises a Wight until M5-07. M5-05b is the half a person stands in front of: a corpse, four labels at a readable size, and one keyword. **The three ledger rows are distributed rather than piled on one half** — [row 4](#carry-forward-into-m5) to M5-05a, which opens `FrameOrderTests` because the frame gained a step; [rows 1](#carry-forward-into-m5) and [3](#carry-forward-into-m5) to M5-05b, because both are visible in an Oathbound run the day they merge and the decoy is not.
- **M5-06 → [M5-06a](../tasks/M5-06a-what-a-legion-node-may-reach.md) / [M5-06b](../tasks/M5-06b-gravecaller-tree-v1.md)**, at M3-12a/M3-12c's seam one milestone on: what a node **may address** against the content that walks through it. M5-06a answers the `StatTarget` question [M5-04b](../tasks/M5-04b-rise-and-minion-stats.md) rule 9 carried here — `StatTarget.Minions` aimed at a run-level *recipe* rather than at the bodies — and adds `TriggerField.MinionCount` and the `RaiseMinions` verb Exhume casts. M5-06b authors the twelve nodes and carries [ledger row 5(i)](#carry-forward-into-m5). **Its rule 3 rules on the title's own three Actives: Exhume ships, Tether and Rot Nova do not** — each needs a primitive of M3-11a's or M3-11b's size, and Rot Nova's trigger reads a `TriggerField.Veilrot` nothing writes.
- **M5-07a → [M5-07a-i](../tasks/M5-07a-i-the-runs-tree-widens.md) / [M5-07a-ii](../tasks/M5-07a-ii-the-half-tree-moment.md)**, at the index space against the moment. M3-11a-i/ii's spelling, because `M5-07b` would read as a half of M5-07, which it is not. **M5-07a-i is the forcing question** the parking lot has carried since M3-03 and none of M5-00a's five specs answers.

| ID | Task | Size | Depends on | Status |
|---|---|---|---|---|
| M5-00a | Specs for M5-01…M5-04 — the projectile weapon, the Gravecaller's body, and its minions | S | — | ☑ |
| M5-00b | Specs for M5-05…M5-08 — the views, the tree, class select and acceptance | S | 00a | ☑ |
| M5-01 | [A weapon that throws something, and the lead that makes it hit](../tasks/M5-01-projectile-weapon-and-leading.md) | M | M2-07a | ☑ |
| M5-02 | [The Gravecaller, and the numbers a second class forces us to settle](../tasks/M5-02-gravecaller-and-bone-bolt.md) | M | M5-01 | ☑ |
| M5-03 | [Shroudstep, and the first thing in this game that is not the player](../tasks/M5-03-shroudstep-and-corpse-decoy.md) | S | M5-02 | ☑ |
| M5-04a | [Wights: a body on the player's side](../tasks/M5-04a-minion-agents-and-registry.md) — [row 6](#carry-forward-into-m5) | M | M5-02 | ☑ |
| M5-04b | [Rise, and an address book for something that is neither the player nor an enemy](../tasks/M5-04b-rise-and-minion-stats.md) — [row 9](#carry-forward-into-m5) | M | M5-04a | ☑ |
| M5-05a | [Wights on screen, and the frame step a body costs](../tasks/M5-05a-wight-views-and-concurrency.md) — CH §8 q1's concurrency ruling, and [row 4](#carry-forward-into-m5) | M | M5-04a | ☑ |
| M5-05b | [The corpse, twelve font values, and one keyword that dims six prefabs](../tasks/M5-05b-decoy-view-and-the-look.md) — [rows 1](#carry-forward-into-m5) and [3](#carry-forward-into-m5) | M | M5-03, M5-05a | ☑ |
| M5-06a | [What a Legion node may reach: a target, a trigger and a verb](../tasks/M5-06a-what-a-legion-node-may-reach.md) | M | M5-04b | ☑ |
| M5-06b | [The Gravecaller tree v1, and what a level is worth](../tasks/M5-06b-gravecaller-tree-v1.md) — [row 5(i)](#carry-forward-into-m5) | M | M5-02, M5-06a | ☑ |
| M5-07 | [Class select: the first screen that asks the player a question before a run](../tasks/M5-07-class-select-screen.md) | M | M5-02, M5-06b | ☑ |
| M5-07a-i | [The run's tree stops being one tree](../tasks/M5-07a-i-the-runs-tree-widens.md) — the forcing question | M | M5-06b | ☑ |
| M5-07a-ii | [The half-tree moment, and the branch a run borrows](../tasks/M5-07a-ii-the-half-tree-moment.md) (CH §5.4) | M | M5-07, M5-07a-i | ☑ |
| M5-08 | [M5 acceptance, tag `m5`](../tasks/M5-08-acceptance-and-tag.md). Also [row 2](#carry-forward-into-m5)'s three numbers, [row 7](#carry-forward-into-m5)'s gate observation and [row 8](#carry-forward-into-m5)'s played minute | S | everything | ☑ |
| M5-08a | [The splash offers what the install refuses](../tasks/M5-08a-splash-offers-what-install-refuses.md) — found *by* M5-08 under its rule 8 | S | M5-08 | ☑ |

### Carry-forward into M5

**What M5's specs must absorb.** Opened at [M4-07](../tasks/M4-07-acceptance-and-tag.md) on [M2's terms](../ROADMAP.md#carry-forward-into-m2), unchanged through four milestones: every row names the task that must deal with it, and **a row leaves only when its owner's *As built* says it is answered.** Four rows are carried from [M4's table](../ROADMAP.md#carry-forward-into-m4), one is M4 row 6 redistributed, two are M4-07's own checklist misses, and one is what its playtest found. Ranked by what it costs to fix later rather than now.

**Rule 11 is new, and it is why this table reads differently from M4's.** M4-00a merged owning rows 1, 2 and 4 and discharged none of them, and nothing in the protocol noticed for five tasks. **So every row below names an *unmerged* owner, or says explicitly that it has none and why** — and *"the next acceptance"* is a legitimate owner only where the row is a **verdict** rather than a fix.

| # | Finding | Owner | Cost of leaving it |
|---|---|---|---|
| 1 | **The HUD's four skill-slot buttons carry a word at 7.9 dp, and that is the whole of what "the UI cannot be read" now means.** M4-07 measured the row M3-15 could only shrug at. Both canvases are `1920 × 1080` at match 0.5, so **a point size is reference px and scales by the canvas, while every dp-sized element scales by the density** — two units on one object, which is why nobody caught it. At the project's own reference density (1080 px short edge at 2.5 px/dp, the source of its *"~432 dp landscape safe area"*), on a 2340 × 1080 screen: run-end title **42.4 dp**, Shard figure **28.3 dp**, its five labels and two figures **21.2 dp**, its exit hint **14.1 dp**; HUD level **17.7 dp**, HP readout **15.9 dp**, Overflow toast **15.0 dp**; **S1–S4's labels 7.9 dp**. Android's floor for body text is 12 sp; Material's comfortable figure is 14 sp. **Seven of eight clear the floor. The eighth is at 66 % of it**, and it is the one [M3-14a](../tasks/M3-14a-localizer-and-english-table.md) put a *word* on precisely because M3-10a rule 9's icons do not exist — the word is there and is half the size it needs to be, inside a 60 dp circle. **The legibility half is therefore one serialized `m_fontSize` on four objects of `Hud.prefab`**, which is smaller than M3-15's estimate of a tuning session and very much smaller than a redesign. **The redesign half has not moved and is not this row's to move:** every taken node shown, Active or Passive, an Auto skill marked by something orbiting it, still blocked on **M7's icons** because M3-10b rule 8's 24 dp cells hold neither an icon nor a word — sequencing is **icons → readout → rotation**, and the owner's standing ruling since the `m3` tag is that the redesign is neither M4's nor M5's. **And the boss bar, measured rather than asserted:** marks **2 dp** wide, the authored 0.66 / 0.33 sitting **15.6 px and 7.8 px** from even spacing on a 2340 px band — 3.1 and 1.6 mark-widths, M4-04's *"three seam-widths and one"* confirmed at real width. A 2 dp seam is visible; whether it reads as *"a transition is coming"* is a played question and belongs to [row 2](#carry-forward-into-m5). **One thing did not survive contact with the measurement: the Editor cannot check any of this.** `Screen.dpi` reads **120** here, so every dp-sized element ever looked at in this Editor was drawn at **30 %** of its device size ([Traps §9](../../Traps.md)) — legibility in this project is arithmetic, not observation, until a phone exists. **Ruled at M5-00a: fix it, and the mechanism is three times what this row says.** The four `Label` objects have **`m_enableAutoSizing: 1` with `m_fontSizeMin: 8` and `m_fontSizeMax: 18`**, so the fix is `m_fontSize`, `m_fontSizeBase` **and** `m_fontSizeMax` — **twelve serialized values, not four** — and **7.9 dp is an upper bound rather than the size**: a word too long for its rect already renders smaller, with an auto-size floor of 8 pt, which is **3.5 dp**. The ruled target is **28 pt → 12.3 dp**, the smallest value that clears Android's floor inside a 60 dp circle; if the word will not fit at 28, the answer is a shorter `LocKey` or M7's icon, **never a smaller font**, which is what put this row here. **Owner, placed at M5-00b: [M5-05b](../tasks/M5-05b-decoy-view-and-the-look.md)** rules 5–7, which is the half of M5-05's split that a person stands in front of — the labels are on screen in every Oathbound run, so the before and after can be held side by side the day it merges. **Its rule 6 is the half this row did not have: `m_fontSizeMin` stays 8**, because raising the floor to 28 turns auto-sizing off and a long word then overflows the circle instead of shrinking inside it — so the guard is a row rather than a value, `Slots_TheShippedWordsFitAtTwentyEight`, measuring *Consecrate* against the label's own rect at 28 pt. **The redesign itself still has no owner, by the owner's standing ruling, and its blocker is M7's icons**. **The legibility half is DISCHARGED at [M5-05b](../tasks/M5-05b-decoy-view-and-the-look.md).** Twelve values on four objects, 18 → 28 on `m_fontSize`, `m_fontSizeBase` and `m_fontSizeMax`, `m_fontSizeMin` left at 8 so a long word still shrinks rather than overflowing the circle. Five rows in `SkillBarPresenterTests` hold it: the twelve values, the floor still on, the arithmetic (**12.36 dp**, computed from the canvas's own match-0.5 scale and the reference density rather than from `Screen`, which lies here), and `Hud_NothingElseMoved` over the other seven text elements *and their set*. **`Slots_TheShippedWordsFitAtTwentyEight` measured rather than assumed, and it is tight:** *"Consecrate"* needs **143.6 px of S1's 150** at 28 pt. **One correction to this row falls out of that measurement** — auto-sizing at the shipped 8–18 was already rendering both shipped Actives at a full 18 pt, so **7.9 dp was the actual size, not an upper bound**; the upper-bound caveat is real for a future longer name and was not true of today's content. **The redesign half is untouched and still has no owner** | **High, and it compounds with every feature** — unchanged from M4, except that the cheap half is now priced, and priced three times higher than this row said. |
| 2 | **Three M3 rows still want a number a playtest produces, and the reason they carry a second time is blunter than the reason they carried the first.** Carried from [M4 row 2](../ROADMAP.md#carry-forward-into-m4): **row 1's played hit counts** at stages 1 / 15 / 30 against the arithmetic's 3 / 4 / 5 (GD §12.4); **row 8's two labelled timings** at stages 1, 5 and 10, stopwatch **and** `RunState.Time`, where the gap between them **is** GD §13.1's interruption budget; and **row 9's two-second read** on twelve real English descriptions. M4-00b sequenced them behind row 1 on the theory that there was no *fixed* UI to run them against. **M4-07's playtest reached stage 5 and produced none of the three**, which is the more useful fact: the session answered whether the wall exists and stopped there, so what blocks these numbers is not the UI but that **nobody has yet gone looking for them**. **One of the three is not blocked by legibility and never was:** hit counts are *counted in the arena*, not read off the HUD, so they can be produced by the next session whatever happens to row 1. The other two need the screens to be readable. **Carrying a row twice is a decision; carrying it silently is the failure this table exists to prevent**, and the decision is written here rather than implied. | **M5-08**, as a verdict — legitimate under rule 11 because this row is a judgement rather than a fix. **The hit-count third of it is owed to the first playtest of any build** and does not wait for M5 | **Medium, and it decays.** These are the numbers that say whether the tree is tuned at all, and every milestone that adds content before they are taken makes a bad answer more expensive to act on. |
| 3 | **The device debt, carried whole from [M4 row 3](../ROADMAP.md#carry-forward-into-m4), with multi-touch still named apart.** `m0`, `m1`, `m2`, `m3` and now `m4` are all tagged on Editor evidence, and **nothing has ever run outside the Editor**. [M3-10a](../tasks/M3-10a-skill-slot-buttons.md)'s **multi-touch is the one row that risks a *feature*** rather than a verdict: if Android does not deliver a thumb on the stick and a thumb on S3 at once, manual casting does not work at all, and no Editor probe can say so. **Kill-from-recents is the one *correctness* row** — mid-stage, or straight after a death now that a Shard is banked — against an atomic write (M2-13b) built for exactly that case and never tested against it, and now with a second file behind it (M4-05b). The rest, re-stated rather than re-listed: haptics and M1-20's 100 ms window, touch latency, real frame rate, thermal, landscape flip, sustained fps at the concurrency cap, **the Profiler's no-per-frame-`GC.Alloc` check unmet since M1-21**, `Physics.SyncTransforms()`' per-frame price, M2's pacing knobs, M3's six, and **M4's own eight** — a 14 m ring and a 5 m crack at six inches; GD §11.3's fill-rate ceiling with a ring, eight cracks, a zone and a telegraph at once; the arm-and-fire shape change; the beat shell reading as *not taking damage*; a 10 dp band, a 4 dp strip and a cutout coexisting; the beat's dim reading as a rule rather than a glitch; five labels and three numbers at thumb distance; and one button clearing a cutout on **both** rotations. **M4-07 added the reason none of this can be pre-answered:** `Screen.dpi` is **120** in this Editor against a phone's 400, so every dp measurement ever taken here is 30 % of device size ([Traps §9](../../Traps.md)) — the Editor is not a weak instrument for these questions, it is the wrong one. **And one row that is a *finding* rather than a question, carried with an owner at last:** `M_TelegraphRing.mat` blends premultiplied without URP's `_ALPHAPREMULTIPLY_ON`, so `ZoneView`, `TelegraphRingView` and `BulwarkView` draw at **full brightness whatever their alpha says**. One keyword on one material; the owner's call since M4-03, and it now has a task to be ruled in. | The first hardware session; **M8-xx** if no phone arrives sooner. **Ruled at M5-00a: add the keyword, and it is six prefabs rather than three views.** `M_TelegraphRing.mat`'s GUID is referenced by `VFX_TelegraphRing`, `VFX_Shockwave`, `VFX_Fissure`, `VFX_BossBeat`, `VFX_Bulwark` and `VFX_ConsecrateZone` — so one keyword dims **every telegraph, both boss hazards, the beat shell, Bulwark and the Consecrate zone at once**, which makes it a look change across the whole VFX language rather than a one-line correction. It is therefore **[M5-05b](../tasks/M5-05b-decoy-view-and-the-look.md)'s** (rules 8, 9; placed at M5-00b on the split), beside the new views, where someone is already judging world-space translucency and can see the before and after together — and specifically the half that ships `VFX_ConsecrateZone` and `VFX_Bulwark`'s before-and-after in an **Oathbound** run, which is the only run this build can start. It must not be folded into a task whose reviewer has no reason to look. **The finding is DISCHARGED at [M5-05b](../tasks/M5-05b-decoy-view-and-the-look.md) — and the ruling above was wrong, which that task's own probe found before implementing it.** `Universal Render Pipeline/Unlit` **declares no `_ALPHAPREMULTIPLY_ON` in Unity 6.3**: its keyword space holds `_SURFACE_TYPE_TRANSPARENT`, `_ALPHATEST_ON` and `_ALPHAMODULATE_ON`, and no premultiply branch at all. `EnableKeyword` accepts the name, files it under **`m_InvalidKeywords`**, warns nowhere, and leaves `IsKeywordEnabled` answering false — so **the fix this row prescribed for three milestones could never have changed a pixel**, and applied literally would have shipped a green suite and an unchanged game. **The symptom was diagnosed correctly and the cause was the blend factors:** `_SrcBlend: One` over a fragment that outputs straight colour contributes rgb at full strength while halving only the background, which *is* "full brightness whatever its alpha says". **The fix is two fields — `_Blend: 1 → 0`, `_SrcBlend: 1 → 5` (SrcAlpha)** — the entire delta between this material and `M_Reticle.mat`, the same shader as written by URP's own Inspector. `TelegraphRingsTests` carries `Telegraph_AlphaMeansWhatItSays` (which asserts the keyword is **absent**) and `Telegraph_SixPrefabsShareOneMaterial`. Filed as [Traps §1](../../Traps.md) and [§9](../../Traps.md). **The look change itself is still unjudged** — it is manual steps 2 and 3, and a *device* question past that. **The rest of row 3 is device debt and does not close at M5**; M5 adds four questions of its own — 37 bodies in one arena against GD §11.3 (M5-05a rule 5), a cyan Wight against a cyan player (M5-05a rule 8, CH §3.2's watch item), two class cards at thumb distance (M5-07), and the splash's two pages (M5-07a-ii) | **High and compounding, unchanged for four milestones.** Each milestone adds rows and none retire, so the first device session gets larger and its failures get harder to attribute to the milestone that caused them. |
| 4 | **`FrameOrderTests` still fails about one run in ten, the experiment finally ran, and it killed its own hypothesis.** [M3-08b](../tasks/M3-08b-level-up-screen.md) proposed running `BootSmokeTests` and `FrameOrderTests` together against `FrameOrderTests` alone, because `BootSmokeTests` loads `Run.unity` in the same PlayMode process first and *"something inside a single run that moves between rows"* was the only hypothesis left standing. **M4-07 ran it at n = 20 a side.** Alone: **4 failures in 20 (20 %)**, plus 3 green pilot runs. Together: **1 in 20 (5 %)** — plus three runs red on `Frame_PausedTicksCostNoSimulatedTime` carrying the Unity AI Assistant's `NoSubscription` log, which is environmental and not this row. **Against M3-08a's measured 10 % baseline both are ordinary**: P(X ≥ 4) = **0.133**, P(X ≤ 1) = **0.392**. **And they are not different from each other**: Fisher's exact, two-tailed, **p ≈ 0.34**. **The sign is the point.** Running `BootSmokeTests` first made the row fail *less*, which is the opposite of what the hypothesis predicted, so **the hypothesis is retired rather than supported or merely unproven**. Every failure was `Ticker_RunsTheStepsInOrder` carrying M2-15a's exact message — *"the cone found nothing where the body had just walked to"*, expecting `int[1]` and getting an empty list — so it is **still the physics half**, and the tell is now sharper than it has ever been stated: **assertion 3, which reads the body's `Transform`, passes on the same frame assertion 5's physics sweep fails.** The body moved; the query did not see it. **Nothing in the game is known to be wrong, there was no bug to fix, and so no `M4-07a` exists.** **The new next step, which is what this row owes rather than a sixth re-run:** instrument the row instead of re-running it — on failure, re-issue the same cone query a second time in the same frame. **If the second query finds the body, the sync at the seam is late; if it does not, the wedge is in the wrong place.** That is one `if` in one fixture, and it converts every future failure into a diagnosis, where thirteen tasks of tallies have converted none. **Placed at M5-00a: M5-05; narrowed at M5-00b to [M5-05a](../tasks/M5-05a-wight-views-and-concurrency.md) rules 9 and 10**, which is the half of the split that adds the frame step. No task in M5-01…M5-04b opens `Tests/PlayMode` — all five are Core and `Tests.Core`, and none changes `RunTicker`'s frame order, which is what this fixture asserts. **M5-05a does**: a Wight is a body, so `RunTicker` gains a minion-apply step, its constructor grows an argument the fixture builds by hand, and `FrameOrderTests` has to be opened whatever else happens. The instrumentation goes in beside it — `RecordingCore` already builds the `ConeHitIntent` it emits, so keeping the last one is one field, the fixture's `cone` **local becomes a field**, and re-issuing `Query` on an empty report is one `if` before the assertion. The message then says *late sync* or *wrong wedge* rather than restating what was expected. **M5-05a rule 10 says in advance that a green milestone is luck rather than evidence**, so the row cannot be struck for not having fired. A standalone diagnosis task is still refused for M4-00b's reason: there is nothing to fix, so such a task could only report | **DISCHARGED at [M5-05a](../tasks/M5-05a-wight-views-and-concurrency.md), and it fired on the second run of six.** The instrument is one field, one `if` and the fixture's `cone` local promoted to a field, exactly as ruled. **The answer is *wrong wedge*:** the same cone, re-asked in the same frame against the settled physics scene, also found nothing — so **the sync at the seam is not late**, and the only hypothesis left is geometry. The message then grew the measurement the ruling did not ask for — distance from the apex, metres along the facing, metres across it, against the wedge's own range and angle — because *"wrong place"* is a shrug without them. **The standing hypothesis, written down and deliberately not acted on:** `RecordingCore` puts the apex at the body's *expected post-move point*, so a 60° wedge there is a half-plane with **zero margin**, and a body that lands a hair **short** is outside it while assertion 3's 1e-2 tolerance still passes. The next failure prints the number that settles that, and **fixing it is a change to what the project's one frame-order assertion means** — a ruling, not an implementation detail. Fourteen tasks old, one diagnosis, no fix, and **a green milestone is still luck rather than evidence** (M5-05a rule 10). |
| 5 | **Content discipline: five `const`s across four sites, and the count was wrong for a whole milestone.** Distributed from [M4 row 6](../ROADMAP.md#carry-forward-into-m4), with every site re-checked against the code rather than quoted forward. **(i) Overflow is *two* constants, not one** — `LevelUpFlow.OverflowDamage` **and** `OverflowMaxHp`, both `0.02f`, and the row has said "the 2 %" singular since M3-15. So ADR-0006's *"every number is in an asset"* is short by two, and retuning the number that supplies more than half a deep run's power is a rebuild rather than an Inspector edit. `ModeDefinition` is the asset it would live on, beside the rest of the run's shape. **(ii) `TimeToKillTests` feeds the *spec's* table rather than the curve**: `overflowLevels: 7` at stage 15 where the shipped `XpCurve` produces **8**. Both give 4 hits, so no row is red — which is exactly why it survived M3-15 and M4-07 both. A test whose inputs describe a curve should derive them from it. **(iii) `Boot.unity:179` still carries a raw `m_text: Soulvail`** that nothing resolves, documented in `TableLocalizerTests`' remarks as out of scope and owned by nobody until now, so AR §11.5's *"no raw user-facing string anywhere"* is one string short of true. **(iv) is struck rather than carried:** `ShardPayout.PerStage` / `PerBoss` were added as `const`s knowingly at M4-05a rule 7 and are **correct as built** — a payout is not a mode, a character or an enemy, there is no asset it belongs on until M6-02's Sanctum exists, and inventing one would be M6 arriving early and unspecified. It becomes a [parking-lot](../ROADMAP.md#parking-lot) line promoted by M6-02. **Ruled and split at M5-00a, because the two are not the same size.** **(ii) is DISCHARGED at [M5-02](../tasks/M5-02-gravecaller-and-bone-bolt.md).** `TimeToKillTests.LevelAt` walks GD §12.1's budget priced in Husks through a real `LevelTracker` and `XpCurve`; `OverflowLevelsAt` subtracts the tree. `Ttk_OverflowLevelsAreDerivedFromTheCurve` asserts **8 at stage 15** — not M3-12c rule 8's 7 — and **29 at stage 30**, where the table turns out to have been right, so both band rows now take a derived input and neither hit count moved. The stage-30 row was re-derived as well, which the ruling did not ask for and which is where the confirmation came from. **(i) → M5-06, and the asset is ruled to be `ModeDefinition`**, beside the rest of the run's shape: it is not a one-line change, because `LevelUpFlow` gains a constructor argument and every construction site ripples, and it is a change to what a *level* is worth, which belongs beside the task that authors a second class's nodes rather than folded into one about a weapon. **Narrowed at M5-00b to [M5-06b](../tasks/M5-06b-gravecaller-tree-v1.md) rules 8–11** — the content half of M5-06's split — **with the ripple counted rather than estimated: one production call site (`RunSession`) and seven in `LevelUpFlowTests`, plus three reads of the const in `TimeToKillTests` and one citation in `HudPresenter`'s remarks.** The two `const`s are **deleted rather than defaulted**, because a default beside an authored value is a second place the number lives. **(i) is DISCHARGED at [M5-06b](../tasks/M5-06b-gravecaller-tree-v1.md).** Both `const`s are gone; `OverflowSpec` is a `readonly struct` beside `RosterEntry` in `ModeSpec.cs`, `ModeDefinition` draws it as an `Overflow` foldout beside the XP one, `Descent.asset` carries **0.02 and 0.02**, and `RunSession.Start` hands `mode.Overflow` to the flow. **The counted ripple was right and short by two:** the production site and the seven test ones were exactly as stated, but `ModeSpec`'s new argument is **optional and last** — forty-two call sites, and placing it beside `xp` where it belongs would have moved all of them — so an unauthored mode grants **nothing**, which reddened `RunSessionResumeTests`' two magnitude rows and needed one edit there. The two `TimeToKillTests`/`LevelUpFlowTests` reads could not be re-pointed at the asset as rule 10 asked: `Soulvail.Tests.Core` cannot reach `AssetDatabase` (M0-10), so they hold the literal and meet `GravecallerTreeTests.Overflow_ComesFromTheMode` at it — `KeenCenser.asset`'s arrangement since M3-12c. **Row 5 now stands on (iii) alone** — (i) here, (ii) at M5-02, (iv) struck — and leaves when M6-10's *As built* says so. **M6-10** for (iii), which is one field and one row on the table it already owns | **Low each, medium together.** None is wrong today. All are the kind of thing discovered by the *next* person to trust the rule. |
| 6 | **`BossBehaviour.Tick` has no allocation row, and it is the only M4 tick path without one.** M4-07's own checklist asked for `AllocationAssert` across `BossBehaviour`, `WardenBehaviour`, `ShockwaveSystem`, `FissureSystem` and `ShardPayout`. Four are covered — `BossPhasesTests.Phase_TickAllocatesNothing`, `WardenBehaviourTests.Tick_AllocatesNothing`, `ShockwaveAndFissureTests.Tick_AllocatesNothing` (both systems in one row) and `ShardPayoutTests.Payout_AllocatesNothing`. **The fifth is not.** `BossBehaviour` is the class that reads `Health.Fraction` every frame, clears its adds, raises the beat and **summons a phase's adds on a derived ring**, and it is measured only *through* its collaborator and its `inner`, each of which has its own row. The add-summoning path is per-phase rather than per-frame, which explains why nobody noticed and is not an argument for leaving it — AR §14's ban is about the frame path, and `Tick` is on it. **Placed at M5-00a: [M5-04a](../tasks/M5-04a-minion-agents-and-registry.md) rule 13**, which opens `Tests/Core/Ai` for `MinionSystemTests`. The row is `Boss_TickAllocatesNothing` in `BossPhasesTests`, and the spec requires it to drive the boss **across a phase threshold** so the clear-and-summon branch runs — a row that only ticks a boss at full health measures the `inner` behaviour, which already has its own. Nothing in `BossBehaviour.cs` changes. **DISCHARGED at [M5-04a](../tasks/M5-04a-minion-agents-and-registry.md).** The row drives a full fight cycle a thousand times — a crossing into phase 1 that summons four Husks, fifteen ticks of beat, a crossing into phase 2 whose `ClearAdds` queues four `DespawnAtEndOfTick`, then a `Reset` back to the top — and measures **zero**. So `EnterPhase`, `ClearAdds`, `Summon` and both publishes are clean, and **there was no finding**: `BossBehaviour.cs` is untouched, as the ruling said it would be. The pooling this leans on is the registry's own — four add agents are built on the first cycle and handed back out for ever after — which is the property that was actually unproven | **Low to add, medium to leave.** One row now; after M7's Choirmother makes `BossBehaviour`'s `inner` a dispatch it is two bosses' worth of unmeasured frame path. |
| 7 | **A boss stage completes when the boss is down, whatever its adds are doing** — so an add can hit the player through `Clear` and `Gate`. Found at [M4-01b](../tasks/M4-01b-boss-agent-framework.md), which handed it to *"M4-02 or M4-03 to revisit with the fight in front of them"*; neither did, and neither was wrong to skip it, because the fight had not been played. **It has now** — M4-07's playtest reached stage 5 — **and the boss was not killed, so the stage never completed and this row was never reached.** It is still unobserved, one milestone later and for a different reason. `SpawnDirector.IsStageComplete` returns `_bossCleared` on a boss stage — the same property M4-05a derives the payout from — so the fix is not free: requiring the adds cleared too changes both the pacing of a boss stage and the meaning of the property the payout reads. **This is a mechanism, not a number**, and it is the kind of thing a played run decides. **Ruled at M5-00a: do not change `IsStageComplete`; hand it to M5-08's playtest as a named observation.** Two fixes were weighed and both refused. **Requiring the adds cleared** is not local: `_bossCleared` is what lets `StageIndex` advance, and `ShardPayout.BossesKilled` is a function of `deepestStage`, so the change moves boss-stage pacing *and* the depth the payout is computed from — for a failure mode nobody has observed, because M4-07's playtest never killed the boss. **Despawning the adds when the boss dies** is cheaper and worse: a despawn is not a kill, so `EnemySystem`'s XP path never runs and the player is silently robbed of the experience for three Husks they were already fighting. **So the observation is the work**: M5-08's checklist carries *"on a boss stage, was the player hit by an add after the boss fell?"*, and a **yes** promotes this to a task with a real fix behind it | **Low today, medium at M7.** One boss authors adds; a second (M7-03) and Elites (M7-02) make it a rule rather than an instance. |
| 8 | **The Warden was fought once and did not die, and nobody has distinguished a number from a bug.** The [M4-07](../tasks/M4-07-acceptance-and-tag.md) playtest reached stage 5, fought the boss, **did not kill it and was killed by it**, and the owner's call was *"it's good for now"* — so M4's *does a run have a wall to hit* is answered **yes**, and this row is not about whether the milestone passed. **It is about which of two things is true, because they need opposite work.** Either 4 200 HP outruns the player's real damage at stage 5, which is a **number** and moves in an Inspector; or the boss's health does not fall at all, which is a **bug** and would mean the fight has never been winnable. **The arithmetic does not settle it, and it is worth saying why it looked as though it had:** M4-01b's **106.7 s** assumes *uninterrupted* damage, and a played fight spends time walking out of a shockwave and standing off a fissure — so 106.7 s is a floor, GD §9.1 rule 5's 75–120 s band has never been measured against a played run, and a fight that simply runs long is the expected outcome rather than a surprise. **The cheapest discriminator is one played minute, not a test:** hit the boss and watch whether the segmented bar moves at all. If it moves, this is a tuning row and the knobs are `Warden.asset`'s HP and `WardenBoss.asset`'s thresholds; if it does not, it is a bug and it is the first one M4 has. | **[M5-08](../tasks/M5-08-acceptance-and-tag.md)** to discriminate and to place — a verdict, legitimate under rule 11. **Carried at M5-00b, with the reason written rather than a placement invented:** the played minute still has not happened, M5-00b is a docs task that could not make it happen, and placing *a number* task or *a bug* task without knowing which would be guessing at the work. **What changed is that the row now names an owner that plays rather than one that merely follows** — M5-08 is a playtest of a run that reaches stage 5, and its checklist carries the minute, the boss-stage question of [row 7](#carry-forward-into-m5) (which needs the boss to die, so the two are answered by one run or neither is), and the note that a **second** class sharpens the discrimination for free: the Gravecaller does 36 DPS to the Censer's 39 plus an army, so a bar that moves for one class and not the other is the answer arriving by itself. **This is the third acceptance in a row to be named as a row's owner, and M5-08's own checklist says so** | **Low if it is a number, high if it is a bug.** A boss that cannot be killed makes every depth past 5 unreachable, which would make [row 2](#carry-forward-into-m5)'s stage-15 and stage-30 numbers unobtainable as well. |
| 9 | **Two things a Wight already needs, both blocked on the same missing thing: something that raises one.** Opened at [M5-04a](../tasks/M5-04a-minion-agents-and-registry.md), where they were found rather than predicted. **(i) The ordering row that spec's Tests table asks for could not be written.** `Run_MinionsTickAfterTheEnemiesAndAboveTheDeathCheck` needs a Wight standing inside a live `RunSession`; nothing produces one, `RunState.Minions` is `internal` because [AR §18.2](../../Architecture.md#182-the-boundary) says a live object is never handed out of `RunState`, and `Soulvail.Tests.Core` has no `InternalsVisibleTo`. Making it public for one test would have been the project's first violation of that invariant, so what shipped is the reachable half — 120 ticks of a Gravecaller run and 120 of an Oathbound, no throw, no intent — and the claim itself is untested. **The row is not new work at M5-04b: rule 4 already states the same ordering**, so what is owed is one integration row written once Rise can kill a Husk and stand a Wight on it. **(ii) A stage boundary does not clear the army.** `RunSession.End` does, `StageFlow.Advance` does not — and twenty seconds against a boundary's two seconds of gate and arrival makes a Wight **the one body in the game that can genuinely cross one**, which is [M5-03](../tasks/M5-03-shroudstep-and-corpse-decoy.md)'s decoy problem at seven times the duration and was found the same way. Inert today because nothing raises one; a Wight following the player into the next arena the day one does. | **DISCHARGED at [M5-04b](../tasks/M5-04b-rise-and-minion-stats.md), both halves.** **(i)** `RiseTests.Run_MinionsTickAfterTheEnemiesAndAboveTheDeathCheck` is written under M5-04a's own name, against a live session and without touching AR §18.2: a Frail at one metre and a Bomb at two, the Censer's swing answered through `ReportConeHits`, the drain raising a Wight **on the corpse** inside the Bomb's 1.5 m reach, and the Wight's strike killing the Bomb whose blast kills the player — `EnemyDied` → `PlayerDied` → `MinionStruck` → `MinionSpawned` → `RunEnded`, all on one tick, asserted by index. `Run_ARiseOnTheTickThePlayerDies` reads the same tick for rule 4's half. **(ii)** `StageFlow` gained an optional `MinionSystem` on `LureSystem`'s precedent and one `_minions?.Clear()` in `Advance`; `StageFlowTests.Advance_SweepsTheArmy` raises two at the door and asserts none survives the crossing. **It cost more than the row's "one line"** — a constructor argument, a field and a fixture field — which is the ordinary price of a class that holds what it clears. **One finding, owed to M5-05a and not a defect here:** the sweep is **silent**, like `EnemySystem.Clear`'s and `LureSystem.Clear`'s, so a Wight view built on `MinionSpawned` gets no `MinionDespawned` at a boundary and tears down on the stage events as the projectile and decoy views do. **Taken at [M5-05a](../tasks/M5-05a-wight-views-and-concurrency.md):** `MinionViews` releases every standing body on `StageArrived` — the event `StageFlow.EnterArrival` publishes immediately after the sweep it mirrors, and the one `ArenaPool` already swaps the room on. What it prevents, none of which throws: eight bodies standing in the *next* arena reporting under ids that no longer resolve, an empty pool so the next raise instantiates on the frame a kill landed, and id 1's entry overwritten by the next stage's first Wight — which leaks that body out of the census for the rest of the run. `MinionViewsTests.Minions_AreSweptAtAStageBoundary`, AR §18.1. **`ProjectileViews` has the identical gap and no answer** → parking lot. | **Low now, and (ii) becomes a visible bug the hour Rise merges.** (i) is the more expensive of the two to leave: an ordering nothing asserts is an ordering a later task can quietly move. |

